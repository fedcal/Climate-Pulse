---
phase: 01-foundation-slice-public-docs
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - packages/core/climatepulse_core/adapters/arpa_emilia.py
  - packages/core/climatepulse_core/adapters/ecmwf_open.py
  - apps/worker/climatepulse_worker/cli/__init__.py
  - apps/worker/climatepulse_worker/cli/backfill.py
  - apps/worker/climatepulse_worker/cli/__main__.py
  - tests/fixtures/__init__.py
  - tests/fixtures/cassettes/arpa_emilia_happy_path.yaml
  - tests/fixtures/cassettes/arpa_emilia_304.yaml
  - tests/fixtures/cassettes/arpa_emilia_404.yaml
  - tests/fixtures/cassettes/arpa_emilia_500.yaml
  - tests/fixtures/cassettes/arpa_emilia_timeout.yaml
  - tests/unit/adapters/__init__.py
  - tests/unit/adapters/test_arpa_emilia.py
  - tests/unit/adapters/test_ecmwf_open.py
  - tests/unit/cli/__init__.py
  - tests/unit/cli/test_backfill.py
autonomous: true
requirements: [ING-03, ING-04, ING-13]

must_haves:
  truths:
    - "ArpaeEmiliaAdapter subclasses WeatherSourceAdapter, source_id='arpa_emilia', cadence_seconds=900 (15min per D-19), is_grid_based=False"
    - "ArpaeEmiliaAdapter.fetch() parses dati-simc.arpae.it realtime.jsonl line-delimited JSON, maps BUFR codes (B12101, B13003, B13011, B11001, B11002) to wmo_codes via normalize.wmo.bufr_to_wmo_code, normalizes units via Pint, yields RawObservation tz-aware UTC"
    - "EcmwfOpenAdapter subclasses WeatherSourceAdapter, source_id='ecmwf_open', cadence_seconds=21600 (6h cycle), is_grid_based=True"
    - "EcmwfOpenAdapter.fetch() downloads via ecmwf-opendata Client to temp dir, decodes GRIB via cfgrib.open_datasets() (handles multi-typeOfLevel per Pitfall B), slices EU bbox (35-72 lat, -25-45 lon) via xarray sel(), yields one RawObservation per (grid_point, variable, step). D-10 6 variables × 4 cycles/day delivered (T, SP, 10u→wind_speed, 10v→wind_direction, TP, RH-derived-from-dewpoint via Plan 03 rh_from_dewpoint helper). No scope reduction — RH is GUARANTEED via Magnus formula derivation from 2t+2d per RESEARCH Q2 resolution."
    - "5 VCR cassettes per D-27 record the canonical ARPA scenarios: 200 happy, 304 ETag, 404, 500, timeout"
    - "climatepulse backfill <source> --from YYYY-MM-DD --to YYYY-MM-DD CLI exists (D-12 exact command shape), splits the window by day per ING-13, calls adapter.fetch + writer.write_batch per day, reports per-day row count, idempotent re-run produces zero new rows"
    - "ARPAE schema-drift handling (D-20): unknown BUFR codes set qc_flag=SCHEMA_VIOLATION, route the offending row to the dlq queue, flip canary metric to unhealthy via observability.health (no fail-loud, no silent skip)"
    - "ECMWF Pitfall A: NO use of area= kwarg on Client.retrieve(); EU bbox applied post-download via xarray.sel"
  artifacts:
    - path: "packages/core/climatepulse_core/adapters/arpa_emilia.py"
      provides: "Concrete ARPA Emilia-Romagna adapter satisfying ING-03"
      contains: "@register('arpa_emilia')"
    - path: "packages/core/climatepulse_core/adapters/ecmwf_open.py"
      provides: "Concrete ECMWF Open Data adapter satisfying ING-04 with cfgrib + EU bbox subsetting (Pitfall A + B)"
      contains: "@register('ecmwf_open')"
    - path: "apps/worker/climatepulse_worker/cli/backfill.py"
      provides: "Backfill CLI (climatepulse backfill <source> --from --to) per ING-13 + D-12"
      contains: "backfill"
    - path: "tests/fixtures/cassettes/arpa_emilia_happy_path.yaml"
      provides: "VCR.py recorded 200 OK cassette per D-27"
    - path: "tests/fixtures/cassettes/arpa_emilia_304.yaml"
      provides: "VCR.py recorded 304 Not Modified ETag cassette per D-27"
    - path: "tests/fixtures/cassettes/arpa_emilia_404.yaml"
      provides: "VCR.py recorded 404 cassette per D-27"
    - path: "tests/fixtures/cassettes/arpa_emilia_500.yaml"
      provides: "VCR.py recorded 500 cassette per D-27"
    - path: "tests/fixtures/cassettes/arpa_emilia_timeout.yaml"
      provides: "VCR.py recorded timeout cassette per D-27 (synthetic — vcrpy can simulate via no response)"
  key_links:
    - from: "packages/core/climatepulse_core/adapters/arpa_emilia.py"
      to: "packages/core/climatepulse_core/http/client.py"
      via: "self._http = PoliteHttpClient(source_id, host, redis_url, snapshot_dir)"
      pattern: "PoliteHttpClient"
    - from: "packages/core/climatepulse_core/adapters/ecmwf_open.py"
      to: "ecmwf-opendata client + cfgrib"
      via: "Client(source='ecmwf', model='ifs').retrieve(...) + cfgrib.open_datasets"
      pattern: "cfgrib"
    - from: "apps/worker/climatepulse_worker/cli/backfill.py"
      to: "packages/core/climatepulse_core/adapters/base.py"
      via: "get_adapter(source) -> adapter.fetch(window) -> writer.write_batch"
      pattern: "get_adapter"
---

<objective>
Concrete adapter implementations for ARPA Emilia-Romagna (ING-03) and ECMWF Open Data (ING-04), plus the user-facing backfill CLI (ING-13 + D-12), plus VCR cassettes locking the 5 canonical ARPA HTTP scenarios per D-27.

Purpose: These two adapters are the only sources Phase 1 needs (D-11 ARPA scope 5-10 stations × 30 days, D-10 ECMWF EU bbox × 5+ vars × 4 cycles/day). Phase 4 adds 3 more adapters by copying the patterns established here. The backfill CLI is part of the user-facing acceptance demo per D-12 ("re-running produces zero duplicates"). The VCR cassettes lock ARPA behavior into CI so we don't need to hit `dati-simc.arpae.it` on every test run.

Output: Two adapter modules under `packages/core/.../adapters/`, the `climatepulse backfill` CLI under `apps/worker/.../cli/`, 5 VCR cassette files, adapter unit tests, CLI tests. After this plan, the Walking Skeleton's ingestion side is complete; Plan 05 wires it into Celery worker + Beat + Compose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md
@.planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md
@.planning/phases/01-foundation-slice-public-docs/SKELETON.md
@.planning/phases/01-foundation-slice-public-docs/01-03-SUMMARY.md
@packages/core/climatepulse_core/adapters/base.py
@packages/core/climatepulse_core/http/client.py
@packages/core/climatepulse_core/normalize/wmo.py
@packages/core/climatepulse_core/normalize/timezone.py
@packages/core/climatepulse_core/storage/writer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ARPA Emilia-Romagna adapter — realtime.jsonl + storico monthly archives + VCR cassettes (ING-03 + D-27)</name>
  <files>packages/core/climatepulse_core/adapters/arpa_emilia.py, tests/fixtures/__init__.py, tests/fixtures/cassettes/arpa_emilia_happy_path.yaml, tests/fixtures/cassettes/arpa_emilia_304.yaml, tests/fixtures/cassettes/arpa_emilia_404.yaml, tests/fixtures/cassettes/arpa_emilia_500.yaml, tests/fixtures/cassettes/arpa_emilia_timeout.yaml, tests/unit/adapters/__init__.py, tests/unit/adapters/test_arpa_emilia.py</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 407-485 ARPAE endpoint schema + BUFR codes table; lines 1262-1265 Pitfall C null handling; lines 1200-1244 VCR pattern; Open Question #1 wind BUFR codes empirical)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-18 dati.arpae.it endpoint; D-19 15min cadence; D-20 schema-drift handling; D-27 5 scenarios)
  </read_first>
  <behavior>
    - ArpaeEmiliaAdapter.source_meta() returns SourceMeta(source_id='arpa_emilia', name='ARPA Emilia-Romagna SIMC', license='CC-BY 4.0', attribution='ARPAE SIMC — dati.arpae.it', terms_url='https://dati.arpae.it/dataset/dati-dalle-stazioni-meteo-locali-della-rete-idrometeorologica-regionale', default_qc_flag=0)
    - ArpaeEmiliaAdapter.discover_stations() parses one realtime.jsonl snapshot, extracts unique (lat, lon, B01019 name, B01194 network) tuples → list of Station(source_id='arpa_emilia', external_id=f"{network}__{lat}_{lon}", name=..., lat=..., lon=...)
    - ArpaeEmiliaAdapter.fetch(window) for realtime feed iterates JSONL lines, parses each record's "vars" dict, maps each known BUFR code to wmo_code, applies normalize_to_si (note: B12101 is already Kelvin per RESEARCH.md line 471 — NO Celsius conversion), handles null values via handle_null_value, yields RawObservation with observed_at=record["date"] parsed as UTC
    - Storico path: when window.since is older than 7 days ago, fetch monthly gzip files (YYYY-MM.json.gz) instead of realtime feed; same record schema
    - Unknown BUFR code triggers D-20 path: yield RawObservation with qc_flag=SCHEMA_VIOLATION + log structured warning + (downstream) the Celery task routes such rows to dlq queue
    - Null `"v": null` per Pitfall C → use handle_null_value → qc_flag=MISSING, value=0.0
    - 5 VCR cassettes: 200 happy, 304, 404, 500, timeout
  </behavior>
  <action>
    Create `packages/core/climatepulse_core/adapters/arpa_emilia.py`:
    - Module constants:
      - `REALTIME_URL = "https://dati-simc.arpae.it/opendata/osservati/meteo/realtime/realtime.jsonl"`
      - `STORICO_BASE_URL = "https://dati-simc.arpae.it/opendata/osservati/meteo/storico"`
      - `HOST = "dati-simc.arpae.it"`
      - `STORICO_THRESHOLD_DAYS = 7` (window older than 7d → storico; recent → realtime)
    - `@register("arpa_emilia")`
    - `class ArpaeEmiliaAdapter(WeatherSourceAdapter)`:
      - Class attrs: `cadence_seconds = 900` (D-19 15min), `polite_delay_ms = 1000`, `is_grid_based = False`
      - `__init__(self, settings: Settings | None = None)`: build `self._http = PoliteHttpClient(source_id="arpa_emilia", host=HOST, redis_url=settings.redis_url, snapshot_dir=settings.snapshot_dir)` using `get_settings()` default
      - `def source_meta(self) -> SourceMeta`: return the SourceMeta described in behavior
      - `async def discover_stations(self) -> list[Station]`: fetch one realtime snapshot, iterate JSONL lines, extract unique stations by (B01194 network, lat, lon) — `external_id = f"{network}__{lat:.4f}_{lon:.4f}"` (deterministic), `name = vars.get('B01019', {}).get('v') or 'unknown'`. Return list.
      - `async def fetch(self, window: FetchWindow) -> AsyncIterator[RawObservation]`:
        1. Decide realtime vs storico: if `window.since > now - 7d`, use realtime endpoint; otherwise use storico (iterate months between window.since.month and window.until.month)
        2. For each JSONL line: parse to dict; extract `observed_at = datetime.fromisoformat(record["date"].replace("Z", "+00:00"))` (already UTC per RESEARCH.md line 471); skip if outside window
        3. Build `station_external_id` same formula as discover_stations
        4. For each (bufr_code, payload) in record["data"][0]["vars"].items() (note: record["data"] is a list of "level/timerange/vars" groups — iterate all groups):
           - If `bufr_code in ('B01019', 'B01194', 'B05001', 'B06001', 'B07030', 'B33007')`: SKIP (metadata, not observation)
           - Look up `wmo_code = bufr_to_wmo_code(bufr_code)`. If None: yield RawObservation with `qc_flag=QcFlag.SCHEMA_VIOLATION`, `wmo_code=f"unknown:{bufr_code}"`, log warning with `structlog.get_logger().warning("arpae.schema_drift", bufr_code=bufr_code, station=station_external_id)` and continue. (D-20: NO fail-loud, NO silent skip — emit row with violation flag, downstream Celery task routes to dlq.)
           - Extract raw value `v = payload.get("v")`. If None: `value_si, qc = handle_null_value(wmo_code, None)` → emit `RawObservation(qc_flag=MISSING, value=0.0, ...)` per Pitfall C
           - Otherwise: normalize via `normalize_to_si(wmo_code, v, source_unit)` where `source_unit` is the ARPAE-feed unit (K for B12101 per RESEARCH.md line 471 — skip conversion since it's already SI; for B13003 it's `%`; for B11002 it's `m/s`; for B13011 it's `mm`). Maintain a `BUFR_SOURCE_UNITS` mapping at module top: `{"B12101": "kelvin", "B13003": "percent", "B11001": "degree", "B11002": "meter/second", "B13011": "millimeter_water", ...}`. Then run `validate_range(wmo_code, value_si)` → qc_flag
           - `yield RawObservation(source_id="arpa_emilia", observed_at=observed_at, station_external_id=station_external_id, wmo_code=wmo_code, value=value_si, qc_flag=qc_flag)`

    Record the 5 VCR cassettes per D-27:
    - **Happy path (`arpa_emilia_happy_path.yaml`)**: a real recorded request to `REALTIME_URL` returning 200 with 3-5 JSONL lines representing a typical record set. To record: run a one-shot `pytest --record-mode=once` against the live endpoint OR hand-author the cassette using the actual line-delimited JSON schema from RESEARCH.md lines 430-455 (the executor should attempt live recording first; if `dati-simc.arpae.it` is unreachable from the runner, hand-author with realistic data based on the verified schema and note this in the SUMMARY)
    - **304 (`arpa_emilia_304.yaml`)**: request with `If-None-Match: "etag123"` returns 304 No Content
    - **404 (`arpa_emilia_404.yaml`)**: request to a non-existent path under `STORICO_BASE_URL/9999-99.json.gz` returns 404
    - **500 (`arpa_emilia_500.yaml`)**: request returns 500 Internal Server Error (hand-author body)
    - **Timeout (`arpa_emilia_timeout.yaml`)**: hand-author a cassette body that vcrpy serves with delayed-no-response, OR use respx as a fallback per the open question A9 + Pitfall G — if vcrpy timeout simulation fails, leave the file as a `.respx-stub.yaml` placeholder and use respx in the test. Either way the test asserts the snapshot fallback writes a `.jsonld` file.

    Create `tests/unit/adapters/test_arpa_emilia.py`:
    - Configure pytest-recording: in conftest add `vcr_config = {"record_mode": "none", "cassette_library_dir": "tests/fixtures/cassettes"}`
    - `@pytest.mark.vcr("arpa_emilia_happy_path.yaml") @pytest.mark.asyncio async def test_fetch_happy_path_yields_observations`: instantiate adapter (with fakeredis-backed settings for the limiter), iterate `[o async for o in adapter.fetch(window)]`, assert >0 observations, assert all `o.qc_flag in (GOOD, MISSING, OUT_OF_RANGE)` (no SCHEMA_VIOLATION on canonical record), assert `o.observed_at.tzinfo is not None`
    - `@pytest.mark.vcr("arpa_emilia_304.yaml") async def test_etag_304_returns_no_new_data`: pre-seed ETag in aiocache; assert fetch yields zero observations on 304
    - `@pytest.mark.vcr("arpa_emilia_404.yaml") async def test_404_writes_snapshot_and_raises`: fetch raises after tenacity retries OR yields snapshot-backed data depending on adapter semantics; assert snapshot file exists in tmp dir
    - `@pytest.mark.vcr("arpa_emilia_500.yaml") async def test_500_retries_then_snapshots`: same expectation as 404 after retries
    - `async def test_500_timeout_writes_snapshot`: use respx if vcrpy timeout fails; same snapshot assertion
    - `async def test_unknown_bufr_emits_schema_violation`: hand-craft a JSONL line with `B99999` BUFR code; pass to adapter via a respx-mocked endpoint OR a hand-authored cassette; assert one yielded RawObservation has `qc_flag == QcFlag.SCHEMA_VIOLATION` and `wmo_code.startswith("unknown:")`
    - `async def test_null_value_handled`: line with `"B13011": {"v": null}` → yielded observation has `qc_flag == QcFlag.MISSING`, `value == 0.0`, `wmo_code == "total_precipitation"` (Pitfall C)
  </behavior>
  <verify>
    <automated>uv run pytest tests/unit/adapters/test_arpa_emilia.py -v -x</automated>
  </verify>
  <done>
    All 5 cassette files parse as valid YAML AND `uv run pytest tests/unit/adapters/test_arpa_emilia.py -v -x --record-mode=none` exits 0 including the timeout scenario (via respx if vcrpy async fails — RESEARCH Q4 resolved to use respx for all async httpx mocks). 5 cassettes exist on disk; ArpaeEmiliaAdapter satisfies ING-03; schema-drift (D-20) and Pitfall C are both verified by tests; `@register("arpa_emilia")` puts the adapter in the registry (verifiable via `get_adapter("arpa_emilia")`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ECMWF Open Data adapter — ecmwf-opendata + cfgrib EU bbox slice (ING-04 + Pitfall A + B)</name>
  <files>packages/core/climatepulse_core/adapters/ecmwf_open.py, tests/unit/adapters/test_ecmwf_open.py</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 487-545 ECMWF integration + bbox subsetting; lines 1250-1260 Pitfall A no server-side bbox + Pitfall B multi-typeOfLevel)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-10 EU bbox 5+ vars 4 cycles/day; D-21 cfgrib + libeccodes0; D-22 worker container only; D-23 routes to gridded_observations)
  </read_first>
  <behavior>
    - EcmwfOpenAdapter.source_meta() returns SourceMeta(source_id='ecmwf_open', name='ECMWF Open Data', license='CC-BY-4.0', attribution='ECMWF Open Data IFS HRES forecast', terms_url='https://www.ecmwf.int/en/forecasts/datasets/open-data', default_qc_flag=0)
    - cadence_seconds=21600 (6h between cycles 00/06/12/18Z); is_grid_based=True
    - discover_stations() returns empty list (grid-based has no stations — discovered via the grid itself)
    - fetch(window) for each cycle init_time in window:
      - Download GRIB to temp dir via Client(source='ecmwf', model='ifs', resol='0p25').retrieve(time=H, type='fc', step=[0,6,12,24], param=['2t','sp','10u','10v','tp','2r'], target=tmpfile) — D-10 5+ variables
      - Open with cfgrib.open_datasets(tmpfile, backend_kwargs={'indexpath': ''}) which auto-splits by typeOfLevel per Pitfall B
      - For each xarray Dataset returned: select EU bbox via .sel(latitude=slice(72,35), longitude=slice(-25,45)) (note descending lat in GRIB per Assumption A7)
      - For each variable in dataset and each (lat, lon) grid point and each forecast step: yield RawObservation(source_id='ecmwf_open', is_grid_based=True semantically — adapter writes lat/lon/lat_idx/lon_idx/init_time/step_h fields)
      - Delete the temp GRIB file in finally block (do NOT persist per RESEARCH.md line 544)
    - Per RESEARCH Q2 resolution: ECMWF IFS oper does NOT publish `2r` directly. Adapter downloads `2d` (2m dewpoint, K) alongside `2t` and derives `relative_humidity` per grid cell via Plan 03 `rh_from_dewpoint(t2m, d2m)`. D-10's 6-variable commitment is GUARANTEED, NO fallback to 5 variables.
    - All 6 yielded wmo_codes for any cycle: `air_temperature, surface_pressure, wind_speed, wind_direction, total_precipitation, relative_humidity` — assert count exactly 6 per cycle.
    - NO `area=` kwarg per Pitfall A — only post-download xarray.sel
  </behavior>
  <action>
    Create `packages/core/climatepulse_core/adapters/ecmwf_open.py`:
    - Imports: `from ecmwf.opendata import Client`, `import cfgrib`, `import xarray as xr`, `import tempfile`, `from pathlib import Path`
    - Module constants:
      - `EU_BBOX = {"lat_max": 72.0, "lat_min": 35.0, "lon_min": -25.0, "lon_max": 45.0}`
      - `REQUIRED_PARAMS = ["2t", "sp", "10u", "10v", "tp", "2d"]` (6 required per D-10; `2d` is the 2m dewpoint temperature used to DERIVE relative humidity via Plan 03 `rh_from_dewpoint` helper — RESEARCH Q2 resolution confirms ECMWF IFS oper does NOT publish `2r` directly so we MUST derive it)
      - `OPTIONAL_PARAMS = []` (intentionally empty — D-10's 6 variables are GUARANTEED via derivation; no fallback to 5 variables allowed)
      - `CYCLES = [0, 6, 12, 18]`
      - `FORECAST_STEPS = [0, 6, 12, 24]` (D-10)
      - `PARAM_TO_WMO = {"2t": "air_temperature", "sp": "surface_pressure", "10u": "wind_u_component", "10v": "wind_v_component", "tp": "total_precipitation", "2d": "dewpoint_temperature_intermediate"}` — NOTE: `10u`/`10v` are intermediate (computed into wind_speed + wind_direction post-extraction); `2d` is intermediate (combined with `2t` to derive `relative_humidity` post-extraction via `rh_from_dewpoint`). Final yielded `wmo_code` set is EXACTLY 6 canonical variables: `air_temperature, surface_pressure, wind_speed, wind_direction, total_precipitation, relative_humidity`. Intermediates (10u, 10v, 2d) are NOT yielded as RawObservations — they are consumed inside the adapter.
    - `@register("ecmwf_open")`
    - `class EcmwfOpenAdapter(WeatherSourceAdapter)`:
      - Class attrs: `cadence_seconds = 21600`, `is_grid_based = True`
      - `def source_meta(self) -> SourceMeta`: return SourceMeta as in behavior
      - `async def discover_stations(self) -> list[Station]`: return `[]` (grid-based; grid points are not "stations")
      - `async def fetch(self, window: FetchWindow) -> AsyncIterator[RawObservation]`:
        1. Compute list of (init_date, cycle_hour) tuples in [window.since, window.until]
        2. For each (init_date, cycle): use `params = REQUIRED_PARAMS` directly (no fallback — RESEARCH Q2 resolved that all 6 are required and available; `2d` and `2t` are core IFS oper params and always present). If Client.retrieve raises for ANY of these 6 params, the failure is terminal and adapter raises (canary flips unhealthy via Plan 03 health helper). NO silent param drop allowed — D-10's 6-variable commitment is non-negotiable.
        3. `with tempfile.TemporaryDirectory() as tmpdir`:
           - `tmpfile = Path(tmpdir) / f"ecmwf_{init_date:%Y%m%d}_{cycle:02d}z.grib2"`
           - Run `Client(source="ecmwf", model="ifs", resol="0p25").retrieve(time=cycle, type="fc", step=FORECAST_STEPS, param=params, target=str(tmpfile))` in a `loop.run_in_executor(None, ...)` since the client is sync
           - `datasets = await loop.run_in_executor(None, cfgrib.open_datasets, str(tmpfile), {"indexpath": ""})` (Pitfall B — multi-typeOfLevel split)
        4. For each ds in datasets:
           - `eu = ds.sel(latitude=slice(EU_BBOX["lat_max"], EU_BBOX["lat_min"]), longitude=slice(EU_BBOX["lon_min"], EU_BBOX["lon_max"]))` — Assumption A7 lat descending in GRIB
           - For each `data_var_name` in `eu.data_vars`: lookup wmo_code via PARAM_TO_WMO (key by short name from xarray); skip if unknown (log warning)
           - For each `step` (forecast hour) in `eu.coords.get("step", [0])`: select `slice_at_step = eu[data_var_name].sel(step=step)`. Compute `valid_at = init_time + step` (where init_time = `datetime.combine(init_date, time(hour=cycle), tzinfo=timezone.utc)`)
           - Iterate the 2D lat/lon grid via `for lat_idx, lat_val in enumerate(slice_at_step.latitude.values): for lon_idx, lon_val in enumerate(slice_at_step.longitude.values)`:
             - `value_raw = float(slice_at_step.values[lat_idx, lon_idx])`
             - Normalize via `normalize_to_si(wmo_code, value_raw, source_unit_from_grib_attrs)` — source units come from `slice_at_step.attrs.get("units")` (cfgrib populates this; e.g. "K", "Pa", "m s**-1")
             - `qc = validate_range(wmo_code, value_si)`
             - `yield RawObservation(source_id="ecmwf_open", observed_at=valid_at, station_external_id=None, wmo_code=wmo_code, value=value_si, qc_flag=qc, lat=float(lat_val), lon=float(lon_val), lat_idx=lat_idx, lon_idx=lon_idx, init_time=init_time, step_h=int(step))`
        5. After processing 10u + 10v grids: compute derived wind_speed = sqrt(u² + v²) and wind_direction = (atan2(-u, -v) * 180/pi) % 360 per grid point; yield as separate RawObservations with wmo_code='wind_speed' / 'wind_direction'. Discard the raw u/v rows (they were intermediate). Use numpy vectorized ops on the xarray DataArrays for efficiency.
        6. After processing 2t + 2d grids: import `rh_from_dewpoint` from `climatepulse_core.normalize.wmo`; for each (lat, lon, step) grid point compute `rh = rh_from_dewpoint(t2m_val_kelvin, d2m_val_kelvin)` and yield as `RawObservation(wmo_code='relative_humidity', value=rh, ...)`. Both 2t and 2d are kept (2t is yielded as `air_temperature`; 2d is intermediate and discarded after derivation). Use numpy vectorized ops; the Magnus formula vectorizes natively. NO step is allowed to silently skip RH — if rh_from_dewpoint raises on a grid cell, that cell yields a RawObservation with qc_flag=OUT_OF_RANGE rather than being omitted.
        7. Always delete tmpfile (the tempdir context manager handles this) — RESEARCH.md line 544 do NOT persist GRIB

    Create `tests/unit/adapters/test_ecmwf_open.py`:
    - All tests must run WITHOUT actually calling ECMWF or downloading 300MB files. Use mocking heavily.
    - `test_source_meta_correct`: assert SourceMeta fields
    - `test_discover_stations_returns_empty`: assert `[] == await adapter.discover_stations()`
    - `test_no_area_kwarg_used`: assert `Client.retrieve` is called WITHOUT an `area=` kwarg. Use `unittest.mock.patch.object(Client, "retrieve")`; inspect call args.
    - `test_eu_bbox_applied_post_download`: monkeypatch `Client.retrieve` to write a tiny synthetic GRIB-substitute (we use a tiny NetCDF via xarray + write to .grib2 path is too complex; INSTEAD, monkeypatch `cfgrib.open_datasets` to return a synthetic xarray Dataset with global coords and one variable `t2m` with units `K`). Assert the adapter calls `.sel(latitude=slice(72, 35), longitude=slice(-25, 45))` on the dataset. Verify by intercepting via a spy wrapper around the dataset.
    - `test_grid_observations_have_is_grid_based_marker`: from monkeypatched dataset, iterate `fetch()`; assert all yielded `RawObservation` have `lat`, `lon`, `lat_idx`, `lon_idx`, `init_time`, `step_h` populated (not None) and `station_external_id is None`
    - `test_ecmwf_six_variables`: per RESEARCH Q2 resolution, monkeypatch `Client.retrieve` to write a synthetic GRIB-substitute (or use a tiny xarray Dataset via the same monkeypatching strategy as `test_eu_bbox_applied_post_download`); the synthetic dataset includes `t2m`, `d2m`, `sp`, `u10`, `v10`, `tp` with units `K`, `K`, `Pa`, `m s**-1`, `m s**-1`, `m`. Iterate `[o async for o in adapter.fetch(window)]`; collect unique `o.wmo_code` set; assert it equals EXACTLY `{"air_temperature", "surface_pressure", "wind_speed", "wind_direction", "total_precipitation", "relative_humidity"}` (6 elements). This guards D-10's 6-variable commitment against silent regression.
    - `test_rh_derived_from_dewpoint_correct_value`: synthetic dataset with `t2m=293.15` (20°C) and `d2m=283.15` (10°C) constant grid; assert at least one yielded RawObservation has `wmo_code=='relative_humidity'` and `value == pytest.approx(52.5, abs=0.5)` (Magnus formula known reference, mirrors Plan 03 `test_rh_from_dewpoint_known_values`).
    - `test_no_fallback_on_missing_param`: monkeypatch `Client.retrieve` to raise the first time (simulate a downstream API change dropping `2d`); assert the adapter PROPAGATES the exception (does NOT silently fall back to a 5-variable set). D-10's 6-variable commitment is non-negotiable per BLOCKER 2.
    - `test_wind_uv_derives_speed_and_direction`: synthetic dataset with `u10=3.0` and `v10=4.0` constant grid; assert yielded `wind_speed` ≈ 5.0 m/s for every grid point and `wind_direction` ≈ 217° (atan2(-3, -4) * 180/pi normalized; verify exact value)
    - `test_no_grib_file_persisted_after_fetch(tmp_path, monkeypatch)`: spy on `tempfile.TemporaryDirectory.__exit__`; assert it was called (proves cleanup)

    NOTE: ecmwf-opendata + cfgrib + libeccodes0 are heavy. These tests must NEVER actually call out to ECMWF in CI. All tests rely on `unittest.mock.patch` for `Client.retrieve` and `cfgrib.open_datasets`. Integration testing against real ECMWF is a Phase 5 hardening concern; Phase 1 verifies the contract via mocks.
  </behavior>
  <verify>
    <automated>uv run pytest tests/unit/adapters/test_ecmwf_open.py -v -x &amp;&amp; uv run pytest tests/unit/adapters/test_ecmwf_open.py -k test_ecmwf_six_variables -v -x</automated>
  </verify>
  <done>
    All ECMWF adapter tests pass without any real ECMWF calls; adapter is registered via `@register("ecmwf_open")`; Pitfall A (no `area=` kwarg) is asserted by test; Pitfall B (cfgrib.open_datasets multi-typeOfLevel split) is the import target; EU bbox subsetting verified by test; wind u/v → speed/direction derivation works; RH derivation from 2t+2d via `rh_from_dewpoint` works (test_rh_derived_from_dewpoint_correct_value); EXACTLY 6 variables yielded per cycle (test_ecmwf_six_variables); no silent fallback to 5 variables (test_no_fallback_on_missing_param); no GRIB file persists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backfill CLI `climatepulse backfill <source> --from --to` — split-by-day, idempotent (ING-13 + D-12)</name>
  <files>apps/worker/climatepulse_worker/cli/__init__.py, apps/worker/climatepulse_worker/cli/backfill.py, apps/worker/climatepulse_worker/cli/__main__.py, tests/unit/cli/__init__.py, tests/unit/cli/test_backfill.py</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-12 exact command shape; ING-13 split by day; specifics — "Re-running the backfill command produces zero duplicate rows")
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (RESEARCH.md line 90 storico endpoint)
  </read_first>
  <behavior>
    - `climatepulse backfill arpae --from 2026-05-01 --to 2026-05-07` runs without error against a live DB
    - Source argument `arpae` is normalized to canonical `arpa_emilia` (D-12 uses short alias; map at CLI layer)
    - The window is split by day; one adapter.fetch + writer.write_batch call per day; per-day rows count printed
    - Re-running the same command produces 0 new rows (idempotency contract per CONTEXT.md "specifics")
    - --from and --to must parse ISO YYYY-MM-DD; invalid date → exit code 2 with friendly error
    - --to defaults to today if omitted; --from must be explicit
    - `climatepulse backfill --help` lists registered sources via `all_source_ids()`
  </behavior>
  <action>
    Create `apps/worker/climatepulse_worker/cli/backfill.py` using `argparse` (no extra deps needed; click/typer would also work but argparse is stdlib and sufficient):
    - Source alias map: `SOURCE_ALIASES = {"arpae": "arpa_emilia", "arpa": "arpa_emilia", "arpa_emilia": "arpa_emilia", "ecmwf": "ecmwf_open", "ecmwf_open": "ecmwf_open"}` — accepts both short and canonical
    - `def parse_args(argv: list[str] | None = None) -> argparse.Namespace`:
      - Positional `source: str` (REQUIRED; choices=list(SOURCE_ALIASES.keys()))
      - `--from FROM_DATE` (required; type=date.fromisoformat)
      - `--to TO_DATE` (optional; defaults to today; type=date.fromisoformat)
      - `--db-url` (optional; defaults to env DATABASE_URL via Settings)
      - `--redis-url` (optional; defaults to env REDIS_URL via Settings)
    - `async def run_backfill(source_id: str, since: date, until: date, db_pool, redis_client) -> int`:
      1. Import the adapter modules so `@register` runs (PEP 562 lazy import via `importlib.import_module("climatepulse_core.adapters.arpa_emilia")` + `..ecmwf_open` — needed because the registry is populated only on import)
      2. `adapter = get_adapter(source_id)`
      3. Build MetadataRepo, upsert source/variables/stations metadata (discover_stations(), upsert each)
      4. Load surrogate caches via MetadataRepo.load_*_cache()
      5. Build IdempotentWriter
      6. Compute day-by-day windows: for each `d` in `(since + day*i for i in range((until - since).days + 1))` — yield `FetchWindow(since=datetime.combine(d, time.min, tzinfo=utc), until=datetime.combine(d + 1day, time.min, tzinfo=utc))`. Per ING-13.
      7. For each daily window: `obs = [o async for o in adapter.fetch(window)]`; `rows_written = await writer.write_batch(adapter, obs)`; structlog.info("backfill.day", source=source_id, day=d.isoformat(), observations=len(obs), rows_written=rows_written); print to stdout `{d}: {rows_written} rows persisted`
      8. Return total rows written
    - `def main(argv: list[str] | None = None) -> int`:
      - Parse args; normalize source via SOURCE_ALIASES; build asyncpg pool + redis client; `asyncio.run(run_backfill(...))`; return 0 on success / 1 on exception. Print errors to stderr with `friendly_error` for ValueError/KeyError.

    Create `apps/worker/climatepulse_worker/cli/__main__.py` so `python -m climatepulse_worker.cli backfill ...` works:
    - Top-level dispatcher: `if len(sys.argv) > 1 and sys.argv[1] == "backfill": from .backfill import main; sys.exit(main(sys.argv[2:]))`. (Phase 5 will add `dlq` subcommand here.)

    Register the `climatepulse` console-script entrypoint in `apps/worker/pyproject.toml`:
    ```
    [project.scripts]
    climatepulse = "climatepulse_worker.cli.__main__:main"
    ```
    (Plan 01's pyproject must be updated by this task if not already done.)

    Create `tests/unit/cli/test_backfill.py`:
    - `test_source_alias_arpae_resolves_to_arpa_emilia`: parse_args(["arpae", "--from", "2026-05-01"]) → namespace + post-process gives source_id == "arpa_emilia"
    - `test_invalid_date_format_exits_2`: subprocess.run(["python","-m","climatepulse_worker.cli","backfill","arpae","--from","not-a-date"], capture_output=True); assert returncode == 2
    - `test_unknown_source_rejected`: parse_args(["unknown_src", "--from", "2026-05-01"]) raises SystemExit with code 2
    - `test_default_to_is_today`: parse_args(["arpae", "--from", "2026-05-01"]).to == date.today()
    - `test_run_backfill_splits_by_day`: monkeypatch `get_adapter` to return a stub adapter whose fetch yields 1 RawObservation per FetchWindow; monkeypatch writer.write_batch to count calls; `await run_backfill("arpa_emilia", date(2026,5,1), date(2026,5,3), pool, redis)` → assert write_batch called 3 times (one per day)
    - `test_run_backfill_idempotent_against_live_db`: integration-style; uses `db_pool` + `seed_metadata` fixtures from Plan 02; uses the stub adapter; runs run_backfill twice with same dates; second run returns 0 new rows (assert via `count(observations)` before/after second run)
  </behavior>
  <verify>
    <automated>uv run pytest tests/unit/cli/test_backfill.py -v -x &amp;&amp; uv run climatepulse backfill --help 2>&amp;1 | grep -q "backfill"</automated>
  </verify>
  <done>
    `climatepulse backfill` console script is callable; CLI parses the D-12 command shape; source aliases map correctly; daily window splitting works per ING-13; idempotency contract verified by integration test (re-run produces 0 new rows); `--help` is informative.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARPAE realtime/storico JSONL → ArpaeEmiliaAdapter | External feed could deliver unexpected BUFR codes or malformed JSON |
| ECMWF GRIB binary → EcmwfOpenAdapter (cfgrib decode) | Binary parsing surface; malformed GRIB could crash worker |
| CLI argument `source` → registry lookup | User input crosses; alias map is small and validated |
| CLI argument `--from/--to` → date parsing | User input; date.fromisoformat raises ValueError on invalid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Unknown BUFR code from ARPAE feed | mitigate | D-20: yield `qc_flag=SCHEMA_VIOLATION` + structured warning + downstream Celery routes to `dlq` queue; canary metric flips source unhealthy. Verified by test_unknown_bufr_emits_schema_violation |
| T-04-02 | DoS | Malformed GRIB crashes worker | mitigate | cfgrib.open_datasets exceptions caught at adapter boundary; offending file is deleted; structlog warning logged; canary metric flips unhealthy. Worker continues; no fail-loud |
| T-04-03 | Information Disclosure | CLI prints DB connection string in errors | mitigate | argparse error formatter does NOT echo args; only command name printed. asyncpg errors are caught and message sanitized before stderr write |
| T-04-04 | Tampering | CLI `source` argument | mitigate | argparse `choices=list(SOURCE_ALIASES.keys())` whitelist; unknown rejected with exit 2 |
| T-04-05 | DoS | ECMWF GRIB downloads (300 MB each × 4 cycles × 30 days backfill = 36 GB temp) | mitigate | Each cycle uses `tempfile.TemporaryDirectory` → automatic cleanup; sequential per-day processing limits peak disk to ~1.5 GB/day; documented in docstring |
| T-04-06 | Information Disclosure | VCR cassettes checked into repo | mitigate | Cassettes capture public open-data responses (no PII, no auth tokens); gitleaks scans them for accidental secret patterns |
| T-04-SC | Tampering | ecmwf-opendata 0.3.29 + cfgrib 0.9.15.1 install | mitigate | Both packages `[OK]` from RESEARCH.md Package Legitimacy Audit (lines 234-236); installed in `apps/worker` pyproject only (D-22) so API container is not affected |
</threat_model>

<verification>
- `pytest tests/unit/adapters/test_arpa_emilia.py` green with 5 cassette scenarios per D-27 (Task 1)
- `pytest tests/unit/adapters/test_ecmwf_open.py` green with mocked Client.retrieve + cfgrib.open_datasets — no real ECMWF call (Task 2)
- `pytest tests/unit/cli/test_backfill.py` green including idempotency property (Task 3)
- `climatepulse backfill --help` lists registered sources (Task 3 acceptance)
- ARPAE adapter schema-drift handling (D-20) emits SCHEMA_VIOLATION not silent skip (Task 1)
- ECMWF adapter does NOT pass `area=` to Client.retrieve (Pitfall A — Task 2)
- ECMWF adapter routes to gridded_observations via `is_grid_based=True` class attr (Task 2)
- Wind u/v are derived into wind_speed + wind_direction per grid point (Task 2)
</verification>

<success_criteria>
- ING-03 (ARPA Emilia-Romagna adapter), ING-04 (ECMWF Open Data adapter), ING-13 (backfill CLI) — fully shipped
- D-10 (ECMWF EU bbox + 6 variables [T, SP, wind_speed, wind_direction, TP, RH-derived-from-dewpoint] + 4 cycles/day) encoded in EcmwfOpenAdapter — no scope reduction
- D-12 (exact backfill command shape with --from/--to ISO dates + idempotent re-run) encoded in CLI
- D-20 (ARPAE schema-drift handling) verified by test
- D-27 (5 VCR cassettes) — all 5 exist on disk
- Pitfall A, B, C addressed and tested
- Plan 05 has: registered adapters + working backfill CLI → ready to wire into Celery worker, Beat schedule, and Docker Compose
</success_criteria>

<output>
Create `.planning/phases/01-foundation-slice-public-docs/01-04-SUMMARY.md` when done. Capture: whether cassettes were live-recorded or hand-authored (note any deviations from real ARPAE responses), confirmed BUFR codes for wind variables (resolves Open Question #1), whether `2r` (relative humidity) was available in ECMWF Open Data (resolves Open Question #2), and the final SOURCE_ALIASES map.
</output>

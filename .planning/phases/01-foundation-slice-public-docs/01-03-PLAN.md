---
phase: 01-foundation-slice-public-docs
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - packages/core/climatepulse_core/adapters/__init__.py
  - packages/core/climatepulse_core/adapters/base.py
  - packages/core/climatepulse_core/http/__init__.py
  - packages/core/climatepulse_core/http/client.py
  - packages/core/climatepulse_core/http/snapshot.py
  - packages/core/climatepulse_core/normalize/__init__.py
  - packages/core/climatepulse_core/normalize/wmo.py
  - packages/core/climatepulse_core/normalize/qc.py
  - packages/core/climatepulse_core/normalize/timezone.py
  - packages/core/climatepulse_core/storage/writer.py
  - packages/core/climatepulse_core/storage/repos.py
  - packages/core/climatepulse_core/observability/__init__.py
  - packages/core/climatepulse_core/observability/health.py
  - packages/core/climatepulse_core/settings.py
  - tests/unit/__init__.py
  - tests/unit/test_normalizer.py
  - tests/unit/test_timezone.py
  - tests/unit/test_qc.py
  - tests/unit/test_snapshot.py
  - tests/unit/test_polite_http.py
  - tests/integration/test_writer.py
autonomous: true
requirements: [ING-01, ING-02, ING-09, ING-10, ING-12, ING-14]

must_haves:
  truths:
    - "WeatherSourceAdapter ABC enforces async fetch() returning AsyncIterator[RawObservation], plus discover_stations() and source_meta()"
    - "@register decorator populates a module-level registry; get_adapter(source_id) returns an instance; duplicate registration raises ValueError"
    - "PoliteHttpClient honors per-host rate limit (pyrate-limiter Redis backend), sends a Climate Pulse User-Agent, checks robots.txt with 24h aiocache TTL, retries 5x with tenacity exponential backoff + jitter, and writes a JSON-LD snapshot on terminal failure"
    - "WMO normalizer round-trips every unit conversion losslessly via Pint (°C↔K, hPa↔Pa, knots↔m/s) — hypothesis property test passes 100+ random cases"
    - "Timezone helper converts naive Europe/Rome timestamps to UTC TIMESTAMPTZ; both 2026-03-29 02:30 (DST spring-forward gap) and 2026-10-25 02:30 (DST fall-back overlap) are handled deterministically — hypothesis property test passes"
    - "Idempotent Writer uses asyncpg copy_records_to_table into a temp staging table, then INSERT...SELECT...ON CONFLICT (station_id, variable_id, observed_at, source_id) DO UPDATE — verified by integration test that re-running ingests zero new rows"
    - "Writer routes RawObservation to observations OR gridded_observations based on the adapter's is_grid_based flag"
    - "Per-source last_successful_ingest_at metric is written to Redis (key `metrics:last_success:{source_id}` ttl 86400) after every successful Writer flush; canary task checks staleness > 2× cadence and flips source to unhealthy (ING-14)"
  artifacts:
    - path: "packages/core/climatepulse_core/adapters/base.py"
      provides: "WeatherSourceAdapter ABC + @register + get_adapter + all_source_ids + FetchWindow + RawObservation passthrough"
      contains: "class WeatherSourceAdapter"
    - path: "packages/core/climatepulse_core/http/client.py"
      provides: "PoliteHttpClient implementing ING-02 in full (UA, robots.txt, rate-limit, tenacity, ETag, snapshot fallback)"
      contains: "PoliteHttpClient"
    - path: "packages/core/climatepulse_core/http/snapshot.py"
      provides: "JSON-LD snapshot write/read + 30-day rotation per D-15/D-16/D-17"
      contains: "@context"
    - path: "packages/core/climatepulse_core/normalize/wmo.py"
      provides: "Pint-based unit converter for 7 WMO core variables to SI"
      contains: "UnitRegistry"
    - path: "packages/core/climatepulse_core/normalize/timezone.py"
      provides: "Europe/Rome naive -> UTC TIMESTAMPTZ converter with explicit DST handling"
      contains: "Europe/Rome"
    - path: "packages/core/climatepulse_core/storage/writer.py"
      provides: "IdempotentWriter using asyncpg copy_records_to_table + ON CONFLICT DO UPDATE; routes by is_grid_based"
      contains: "copy_records_to_table"
    - path: "packages/core/climatepulse_core/observability/health.py"
      provides: "record_success(source_id) writes to Redis; is_stale(source_id) returns bool for ING-14 canary"
      contains: "last_success"
    - path: "tests/unit/test_normalizer.py"
      provides: "hypothesis property tests for unit round-trips per D-26"
      contains: "@given"
    - path: "tests/unit/test_timezone.py"
      provides: "hypothesis property tests + explicit DST cases per D-26"
      contains: "2026-03-29"
  key_links:
    - from: "packages/core/climatepulse_core/storage/writer.py"
      to: "observations + gridded_observations hypertables"
      via: "asyncpg copy + INSERT ON CONFLICT routing by is_grid_based"
      pattern: "is_grid_based"
    - from: "packages/core/climatepulse_core/http/client.py"
      to: "Redis (rate-limit DB 2)"
      via: "pyrate-limiter RedisBucket"
      pattern: "RedisBucket"
    - from: "packages/core/climatepulse_core/http/client.py"
      to: "snapshot volume"
      via: "snapshot.SnapshotStore.write_jsonld on terminal failure"
      pattern: "snapshot"
    - from: "packages/core/climatepulse_core/observability/health.py"
      to: "Redis (metrics DB)"
      via: "SETEX metrics:last_success:{source_id}"
      pattern: "last_success"
---

<objective>
Build the entire core ingestion machinery — Adapter ABC + decorator-registry + polite HTTP client (including the full ING-02 contract: User-Agent + robots.txt + Redis-shared rate limit + tenacity + aiocache ETag + JSON-LD snapshot fallback) + WMO unit normalizer + timezone helper + idempotent writer routing to BOTH hypertables + per-source health metric. Property-based tests (hypothesis) lock the unit conversion and DST behavior.

Purpose: This plan delivers the contract-level core that ALL adapters in Plan 04 (and Phase 4 adapters: ARPA Lombardia/Veneto, METAR, Copernicus) will implement. Getting the ABC + writer + politeness right here means new adapters are single-file plugins (contributor guide DOC-06 in Phase 5 relies on this). The dual-hypertable routing (D-23) is encoded in `Writer.write_batch(adapter, observations)` reading `adapter.is_grid_based`.

Output: Eight `packages/core` modules (adapters/base.py, http/client.py, http/snapshot.py, normalize/wmo.py, normalize/qc.py, normalize/timezone.py, storage/writer.py, storage/repos.py, observability/health.py, settings.py) + six test files. After this plan, Plan 04 implements concrete ARPAE + ECMWF adapters by subclassing `WeatherSourceAdapter` — no further core changes needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md
@.planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md
@.planning/phases/01-foundation-slice-public-docs/SKELETON.md
@.planning/phases/01-foundation-slice-public-docs/01-01-SUMMARY.md
@.planning/phases/01-foundation-slice-public-docs/01-02-SUMMARY.md
@packages/core/climatepulse_core/domain/models.py
@packages/core/climatepulse_core/storage/models.py

<interfaces>
<!-- Contracts created here that Plan 04 (adapters) and Plan 05 (worker/compose/api) will consume. -->

From packages/core/climatepulse_core/adapters/base.py:
```python
from abc import ABC, abstractmethod
from dataclasses import dataclass
from datetime import datetime
from typing import AsyncIterator, Callable

_REGISTRY: dict[str, type["WeatherSourceAdapter"]] = {}

def register(source_id: str) -> Callable: ...
def get_adapter(source_id: str) -> "WeatherSourceAdapter": ...
def all_source_ids() -> list[str]: ...

@dataclass(frozen=True)
class FetchWindow:
    since: datetime   # tz-aware UTC
    until: datetime   # tz-aware UTC

@dataclass(frozen=True)
class SourceMeta:
    source_id: str
    name: str
    license: str
    attribution: str
    terms_url: str | None
    default_qc_flag: int

class WeatherSourceAdapter(ABC):
    source_id: str             # populated by @register
    cadence_seconds: int       # subclass declares
    polite_delay_ms: int = 1000
    is_grid_based: bool = False  # True routes to gridded_observations

    @abstractmethod
    async def discover_stations(self) -> list["Station"]: ...

    @abstractmethod
    async def fetch(self, window: FetchWindow) -> AsyncIterator["RawObservation"]: ...

    @abstractmethod
    def source_meta(self) -> SourceMeta: ...
```

From packages/core/climatepulse_core/storage/writer.py:
```python
class IdempotentWriter:
    def __init__(self, db_pool, sources_cache, variables_cache, stations_cache): ...

    async def write_batch(
        self,
        adapter: WeatherSourceAdapter,
        observations: list[RawObservation],
    ) -> int:
        """Returns number of rows persisted. Routes to observations or gridded_observations based on adapter.is_grid_based. Idempotent."""
```

From packages/core/climatepulse_core/http/client.py:
```python
class PoliteHttpClient:
    def __init__(self, source_id: str, host: str, redis_url: str, snapshot_dir: str): ...

    async def get_json(self, url: str, etag: str | None = None) -> tuple[dict | None, str | None]:
        """Returns (data, new_etag). 304 -> (None, etag). On terminal failure: writes snapshot, raises."""

    async def get_bytes(self, url: str, etag: str | None = None) -> tuple[bytes | None, str | None]: ...

    async def check_robots(self, url: str) -> bool: ...
```

From packages/core/climatepulse_core/observability/health.py:
```python
async def record_success(redis_client, source_id: str) -> None:
    """SETEX metrics:last_success:{source_id} 86400 <ISO timestamp>"""

async def get_last_success(redis_client, source_id: str) -> datetime | None: ...

async def is_stale(redis_client, source_id: str, cadence_seconds: int) -> bool:
    """True if no success seen within 2× cadence_seconds (ING-14 canary threshold)"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adapter ABC + decorator registry + FetchWindow + SourceMeta + central Settings</name>
  <files>packages/core/climatepulse_core/adapters/__init__.py, packages/core/climatepulse_core/adapters/base.py, packages/core/climatepulse_core/settings.py, tests/unit/__init__.py, tests/unit/test_adapter_registry.py</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 786-836 WeatherSourceAdapter ABC contract)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (Claude's Discretion section — adapter ABC interface contract)
  </read_first>
  <behavior>
    - register('arpa_emilia')(class) puts the class in registry, sets class.source_id = 'arpa_emilia'
    - get_adapter('arpa_emilia') returns a NEW instance (not the class)
    - Registering 'arpa_emilia' twice raises ValueError
    - all_source_ids() returns sorted list of registered ids
    - WeatherSourceAdapter cannot be instantiated (ABC) — subclass without required abstractmethods raises TypeError
    - FetchWindow rejects naive datetimes (must have tzinfo) — raises ValueError in __post_init__ — but since frozen dataclass, validation happens via __post_init__ which IS supported on frozen
    - Settings loads DATABASE_URL, REDIS_URL, SNAPSHOT_DIR from env with sensible defaults for dev
  </behavior>
  <action>
    Note: executor splits this TDD task into RED (write failing tests first) + GREEN (implement to pass) per TDD discipline; the test file in <files> is the RED artifact.

    Create `packages/core/climatepulse_core/adapters/base.py` per the `<interfaces>` block in this plan:
    - Module-level `_REGISTRY: dict[str, type[WeatherSourceAdapter]] = {}`
    - `def register(source_id: str)` decorator: validates `source_id in _REGISTRY` raises `ValueError(f"Duplicate source_id: {source_id}")`; otherwise assigns `cls.source_id = source_id` and `_REGISTRY[source_id] = cls`; returns the class unchanged
    - `def get_adapter(source_id: str) -> WeatherSourceAdapter`: looks up `_REGISTRY[source_id]` (raises `KeyError(f"Unknown source_id: {source_id}")` if missing), returns `cls()` (NEW instance)
    - `def all_source_ids() -> list[str]`: returns `sorted(_REGISTRY)`
    - `@dataclass(frozen=True) class FetchWindow`: `since: datetime`, `until: datetime`. Override `__post_init__` to assert `since.tzinfo is not None and until.tzinfo is not None and since <= until`; raise `ValueError` with specific message if violated. Frozen dataclasses DO support `__post_init__`.
    - `@dataclass(frozen=True) class SourceMeta`: `source_id: str, name: str, license: str, attribution: str, terms_url: str | None, default_qc_flag: int`
    - `class WeatherSourceAdapter(ABC)`: class attrs `source_id: str` (set by @register; do not initialize to None — let static analysis flag misuse), `cadence_seconds: int` (no default; subclass MUST set), `polite_delay_ms: int = 1000`, `is_grid_based: bool = False`. Three `@abstractmethod`s exactly per `<interfaces>` block: `discover_stations`, `fetch`, `source_meta`.

    Re-export `RawObservation, Station, Source, Variable, QcFlag` from `climatepulse_core.adapters` so adapter authors can import everything from one module.

    Create `packages/core/climatepulse_core/settings.py` using pydantic-settings 2.x:
    - `class Settings(BaseSettings)` with `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")`
    - Fields: `database_url: str` (no default — raise loud), `redis_url: str = "redis://localhost:6379"`, `snapshot_dir: str = "/var/lib/climatepulse/snapshots"` (D-15 default), `log_level: str = "INFO"`, `environment: Literal["development", "production"] = "development"`
    - Redis DB allocation as class constants (RESEARCH.md lines 884-889): `REDIS_DB_BROKER = 0`, `REDIS_DB_RESULTS = 1`, `REDIS_DB_RATELIMIT = 2`, `REDIS_DB_ETAG = 3`, `REDIS_DB_METRICS = 4` (add 4 for ING-14 metrics)
    - `@lru_cache(maxsize=1) def get_settings() -> Settings: return Settings()`

    Create `tests/unit/test_adapter_registry.py` with these tests (all `@pytest.mark.asyncio` where async):
    - `test_register_assigns_source_id_attribute`: define throwaway subclass, decorate with `@register("test_src_1")`, assert `cls.source_id == "test_src_1"`
    - `test_duplicate_registration_raises`: register `"test_src_2"` twice → second raises `ValueError` containing `"Duplicate"`
    - `test_get_adapter_returns_fresh_instance`: assert two `get_adapter("test_src_3")` calls return distinct instance ids
    - `test_unknown_source_raises_keyerror`: assert raises `KeyError`
    - `test_all_source_ids_sorted`: register 3 sources in random order; `all_source_ids()` returns sorted
    - `test_abstract_cannot_instantiate`: `pytest.raises(TypeError, WeatherSourceAdapter)`
    - `test_fetchwindow_rejects_naive_datetime`: `FetchWindow(since=datetime.now(), until=datetime.now())` (no tz) → `ValueError`
    - `test_fetchwindow_rejects_inverted_range`: tz-aware `until < since` → `ValueError`
    - `test_fetchwindow_accepts_utc`: valid construction succeeds

    Tests use fresh source_ids per test (no global registry pollution); add a `conftest.py` autouse fixture that clears `_REGISTRY` between tests if needed.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_adapter_registry.py -v -x</automated>
  </verify>
  <done>
    All 9 tests pass; `from climatepulse_core.adapters import WeatherSourceAdapter, register, get_adapter, all_source_ids, FetchWindow, SourceMeta, RawObservation, Station, Source, Variable, QcFlag` works; Settings can be instantiated with env vars or `.env`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WMO Normalizer (Pint) + QC + Timezone helpers — with hypothesis property tests (D-26)</name>
  <files>packages/core/climatepulse_core/normalize/__init__.py, packages/core/climatepulse_core/normalize/wmo.py, packages/core/climatepulse_core/normalize/qc.py, packages/core/climatepulse_core/normalize/timezone.py, tests/unit/test_normalizer.py, tests/unit/test_timezone.py, tests/unit/test_qc.py</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-26 hypothesis mandatory for Normalizer + Timezone; ING-09 7 WMO variables in SI via Pint; ING-10 DST tests March + October)
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 458-471 BUFR variable codes; lines 1262-1265 Pitfall C BUFR null values)
  </read_first>
  <behavior>
    - normalize_to_si(wmo_code, value, unit) returns (value_si, unit_si) with no precision loss for round-tripped conversions
    - Round-trip property: for any random float in physical range, °C → K → °C returns input within Pint precision (use math.isclose with rel_tol=1e-9)
    - Same property for hPa↔Pa (factor 100), knots↔m/s (factor 0.5144...), mm↔kg/m² (1:1 in water equivalent)
    - validate_range(wmo_code, value) returns QcFlag.OUT_OF_RANGE if value outside physical bounds (e.g. air_temperature < 180 K or > 340 K → out of range)
    - to_utc(naive_dt, tz_name='Europe/Rome') returns tz-aware UTC datetime
    - to_utc raises NonExistentTimeError for 2026-03-29 02:30 Europe/Rome (DST spring-forward gap)
    - to_utc raises AmbiguousTimeError for 2026-10-25 02:30 Europe/Rome (DST fall-back overlap)
    - to_utc_strict(naive_dt) wraps to_utc with explicit handling: gap → returns next valid time + 1 hour and tags qc_flag, ambiguous → returns first occurrence (DST=true)
  </behavior>
  <action>
    Note: executor splits this TDD task into RED (write failing hypothesis + explicit tests) + GREEN (implement helpers) per TDD discipline.

    Create `packages/core/climatepulse_core/normalize/wmo.py`:
    - Module-level `UREG = pint.UnitRegistry()` (Pint 0.25.3); add custom units if needed: `UREG.define('millimeter_water = kg / m**2 = mmw')` for precipitation 1:1
    - WMO variable catalog dict `WMO_VARIABLES: dict[str, dict]` keyed by canonical wmo_code with values containing `unit_si`, `physical_range` tuple (min, max in SI), `bufr_codes: list[str]`:
      - `air_temperature`: unit `K`, range (180.0, 340.0), bufr [`B12101`]
      - `relative_humidity`: unit `%`, range (0.0, 100.0), bufr [`B13003`, `2r`]
      - `surface_pressure`: unit `Pa`, range (87000.0, 109000.0), bufr [`B10004`, `sp`]
      - `wind_direction`: unit `degree`, range (0.0, 360.0), bufr [`B11001`, `10wd`]
      - `wind_speed`: unit `m/s`, range (0.0, 120.0), bufr [`B11002`]  (note: ECMWF uses 10u/10v components — adapter resolves into wind_speed before calling normalize)
      - `total_precipitation`: unit `kg m-2`, range (0.0, 500.0), bufr [`B13011`, `tp`]
      - `cloud_cover`: unit `%`, range (0.0, 100.0), bufr [`B20010`]
    - `def normalize_to_si(wmo_code: str, value: float, unit: str) -> tuple[float, str]`: looks up canonical SI unit for wmo_code, converts via Pint `(value * UREG(unit)).to(target_unit).magnitude`, returns `(float, target_unit_str)`
    - `def bufr_to_wmo_code(bufr_code: str) -> str | None`: reverse lookup; returns None if unknown
    - `def rh_from_dewpoint(t2m_kelvin: float, d2m_kelvin: float) -> float`: derives 2m relative humidity (%) from 2m air temperature and 2m dewpoint temperature using the Magnus formula. Convert both inputs to °C via Pint: `t_c = (t2m_kelvin * UREG.kelvin).to('degC').magnitude`; same for d_c. Then `rh = 100.0 * exp((17.625 * d_c) / (243.04 + d_c)) / exp((17.625 * t_c) / (243.04 + t_c))`. Clamp output to [0.0, 100.0]. This satisfies RESEARCH Q2 resolution (ECMWF IFS oper does not publish 2r directly) and is consumed by Plan 04 Task 2 EcmwfOpenAdapter for the 6th D-10 variable.

    Create `packages/core/climatepulse_core/normalize/qc.py`:
    - `def validate_range(wmo_code: str, value_si: float) -> QcFlag`: returns `QcFlag.OUT_OF_RANGE` if value falls outside `WMO_VARIABLES[wmo_code]["physical_range"]`; returns `QcFlag.MISSING` if value is None; else `QcFlag.GOOD`
    - `def handle_null_value(wmo_code: str, value: float | None) -> tuple[float, QcFlag]`: per Pitfall C (RESEARCH.md lines 1262-1265), if value is None return `(0.0, QcFlag.MISSING)` (sentinel-zero with MISSING flag; row is still inserted because `value DOUBLE PRECISION NOT NULL`)

    Create `packages/core/climatepulse_core/normalize/timezone.py`:
    - Use `zoneinfo` (stdlib) — `ZoneInfo("Europe/Rome")`
    - `def to_utc(naive_dt: datetime, tz_name: str = "Europe/Rome") -> datetime`: attaches tz via `.replace(tzinfo=ZoneInfo(tz_name))`, converts via `.astimezone(timezone.utc)`. Detect DST gap: if naive_dt falls in 02:00-03:00 on the spring-forward Sunday (last Sunday of March in Europe), raise `NonExistentTimeError`. Detect DST overlap: if naive_dt falls in 02:00-03:00 on the fall-back Sunday (last Sunday of October), raise `AmbiguousTimeError`. Define these two exception classes at module level.
    - `def to_utc_strict(naive_dt: datetime, fold: int = 0) -> tuple[datetime, QcFlag]`: wraps to_utc; on gap, advance by 1 hour and return `(advanced_dt, QcFlag.OUT_OF_RANGE)`; on ambiguous, use `naive_dt.replace(fold=fold)` (fold=0 → first occurrence = DST active = +02:00 offset).

    Create `tests/unit/test_normalizer.py`:
    - `from hypothesis import given, strategies as st`
    - `@given(st.floats(min_value=200.0, max_value=320.0, allow_nan=False, allow_infinity=False)) def test_celsius_kelvin_roundtrip(k_val)`: convert K → °C via `normalize_to_si('air_temperature', k_val - 273.15, 'degC')`, then `normalize_to_si('air_temperature', result - 273.15, 'degC')` (or reverse the round-trip more cleanly via direct Pint Quantity multiplications); assert `math.isclose(result, k_val, rel_tol=1e-9)`. Run hypothesis with `settings(max_examples=200)`.
    - Similar `@given` for hPa↔Pa (factor 100) and knots↔m/s (factor 0.5144444...)
    - Explicit test cases: `normalize_to_si('air_temperature', 0.0, 'degC')` returns `(273.15, 'K')`; `normalize_to_si('surface_pressure', 1013.25, 'hPa')` returns `(101325.0, 'Pa')`
    - `test_bufr_lookup`: `bufr_to_wmo_code('B12101') == 'air_temperature'`; `bufr_to_wmo_code('BXXXXX') is None`
    - `test_rh_from_dewpoint_known_values`: at T=20°C (293.15K) and Td=10°C (283.15K), RH ≈ 52.5% (Magnus formula known reference). Assert `pytest.approx(52.5, abs=0.5)`.
    - `test_rh_from_dewpoint_saturation`: at T == Td (e.g. T=Td=288.15K), RH must be exactly 100.0 (saturation by definition). Assert `math.isclose(rh, 100.0, abs_tol=0.01)`.
    - `test_rh_from_dewpoint_clamped`: pathological input Td > T (supersaturation edge case from noisy GRIB) → output clamped to 100.0, not >100.

    Create `tests/unit/test_qc.py`:
    - `test_out_of_range_temperature`: `validate_range('air_temperature', 500.0) == QcFlag.OUT_OF_RANGE`; `validate_range('air_temperature', 285.0) == QcFlag.GOOD`
    - `test_handle_null_value`: `handle_null_value('total_precipitation', None) == (0.0, QcFlag.MISSING)`; for non-None: returns `(value, GOOD)` if in range

    Create `tests/unit/test_timezone.py`:
    - `test_normal_date_converts_to_utc`: `to_utc(datetime(2026, 6, 15, 14, 30))` → tz-aware UTC, hour=12 (Europe/Rome is UTC+2 in CEST)
    - `test_dst_spring_forward_gap_raises`: `pytest.raises(NonExistentTimeError, to_utc, datetime(2026, 3, 29, 2, 30))` — D-26 explicit
    - `test_dst_fall_back_overlap_raises`: `pytest.raises(AmbiguousTimeError, to_utc, datetime(2026, 10, 25, 2, 30))` — D-26 explicit
    - `test_to_utc_strict_gap_advances`: `to_utc_strict(datetime(2026, 3, 29, 2, 30))` returns `(<2026-03-29 03:30 CEST as UTC = 01:30Z>, QcFlag.OUT_OF_RANGE)`
    - `test_to_utc_strict_fold_first`: `to_utc_strict(datetime(2026, 10, 25, 2, 30), fold=0)` returns first occurrence (CEST UTC+2 → 00:30Z)
    - `@given(st.datetimes(min_value=datetime(2024,1,1), max_value=datetime(2027,12,31))) def test_roundtrip_property(naive_dt)`: for any naive datetime, `to_utc_strict(naive_dt)[0].tzinfo == timezone.utc`; round-trip property: utc → Europe/Rome → utc returns the same UTC instant. Skip the DST 2-hour windows via `assume()` filter.
  </behavior>
  <verify>
    <automated>uv run pytest tests/unit/test_normalizer.py tests/unit/test_qc.py tests/unit/test_timezone.py -v -x --hypothesis-show-statistics</automated>
  </verify>
  <done>
    All property and explicit tests pass; hypothesis reports >100 examples for each `@given`; the explicit DST cases (2026-03-29 and 2026-10-25) per D-26 are both covered with the correct exception types; WMO_VARIABLES contains exactly 7 entries (ING-09).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PoliteHttpClient (UA + robots.txt + rate-limit + tenacity + ETag + snapshot) — ING-02 full</name>
  <files>packages/core/climatepulse_core/http/__init__.py, packages/core/climatepulse_core/http/client.py, packages/core/climatepulse_core/http/snapshot.py, packages/core/climatepulse_core/observability/__init__.py, packages/core/climatepulse_core/observability/health.py, tests/unit/test_snapshot.py, tests/unit/test_polite_http.py</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 842-890 PoliteHttpClient design; lines 895-924 JSON-LD snapshot schema; lines 925-928 snapshot file path; lines 855-863 USER_AGENT format)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-15/D-16/D-17 snapshot storage/format/retention; ING-02 full contract)
  </read_first>
  <behavior>
    - PoliteHttpClient.get_json(url) returns (data, etag) on 200, (None, etag) on 304, raises after 5 tenacity retries on 5xx
    - User-Agent header on every request matches "ClimatePulse/{version} (+https://github.com/federicocalo/climate-pulse; contact: fedcal01@gmail.com)"
    - check_robots(url) returns True for allowed paths, False for disallowed; result cached in aiocache 24h
    - rate-limit budget shared across instances when same redis_url+host: two concurrent clients pulling 31 requests/min total → at least one is throttled
    - On terminal failure (5xx after retries or timeout), client writes a JSON-LD snapshot to {SNAPSHOT_DIR}/{source_id}/{station_or_grid_id}/{ISO}.jsonld
    - SnapshotStore.rotate(max_age_days=30) deletes JSON-LD files older than 30 days per D-17
    - record_success(redis, source_id) writes `metrics:last_success:{source_id}` with current UTC ISO timestamp, TTL 86400 (24h)
    - is_stale(redis, source_id, cadence_seconds) returns True if key missing OR last_success older than 2*cadence_seconds (ING-14 canary threshold)
  </behavior>
  <action>
    Note: executor splits this TDD task into RED (write failing tests for snapshot + PoliteHttpClient + health helpers) + GREEN (implement) per TDD discipline.

    Create `packages/core/climatepulse_core/http/snapshot.py`:
    - `class SnapshotStore` with `__init__(self, snapshot_dir: str)` that ensures dir exists via `Path(snapshot_dir).mkdir(parents=True, exist_ok=True)`
    - `async def write_jsonld(self, source_id: str, station_or_grid_id: str, observed_at: datetime, observations: list[dict], snapshot_reason: str) -> Path`: builds the JSON-LD document per RESEARCH.md lines 898-922 (@context with WMO + schema.org + custom cp: vocab), writes to `{snapshot_dir}/{source_id}/{station_or_grid_id}/{observed_at.strftime('%Y-%m-%dT%H:%M:%SZ')}.jsonld` (D-16), creates parent dirs, returns the path
    - `async def read_latest(self, source_id: str, station_or_grid_id: str) -> dict | None`: returns most recent snapshot's parsed JSON, or None if no snapshots
    - `async def rotate(self, max_age_days: int = 30) -> int`: scans snapshot dir, deletes any `*.jsonld` whose mtime is older than `now - max_age_days`, returns count deleted (D-17 30-day retention)
    - File-path traversal guard: validate `source_id` and `station_or_grid_id` against `^[a-zA-Z0-9_-]+$` regex — reject any input with path separators (security mitigation T-03-04 below)

    Create `packages/core/climatepulse_core/observability/health.py`:
    - `async def record_success(redis_client, source_id: str) -> None`: `await redis_client.setex(f"metrics:last_success:{source_id}", 86400, datetime.now(timezone.utc).isoformat())`
    - `async def get_last_success(redis_client, source_id: str) -> datetime | None`: returns parsed UTC datetime or None
    - `async def is_stale(redis_client, source_id: str, cadence_seconds: int) -> bool`: True if get_last_success returns None OR age > 2*cadence_seconds (ING-14 canary)

    Create `packages/core/climatepulse_core/http/client.py`:
    - Module constant `USER_AGENT_TEMPLATE = "ClimatePulse/{version} (+https://github.com/federicocalo/climate-pulse; contact: fedcal01@gmail.com)"`; resolve version via `importlib.metadata.version("climatepulse-core")` with fallback `"0.1.0"`
    - `class PoliteHttpClient`:
      - `__init__(self, source_id: str, host: str, redis_url: str, snapshot_dir: str, rate_limit: tuple[int, str] = (30, "minute"))`: build `aioredis.Redis.from_url(redis_url, db=2)` (REDIS_DB_RATELIMIT); `RedisBucket.init([Rate(rate_limit[0], Duration.MINUTE if rate_limit[1]=='minute' else Duration.SECOND)], r, f"rl:{host}")`; store `self._limiter = Limiter(bucket)`; create `aiocache.Cache(Cache.REDIS, endpoint=..., db=3)` for ETag cache; build a `SnapshotStore(snapshot_dir)`; build a separate aiocache for robots.txt with TTL 86400
      - `async def get_json(self, url: str, etag: str | None = None) -> tuple[dict | None, str | None]`: decorated internally with `tenacity.AsyncRetrying(stop=stop_after_attempt(5), wait=wait_random_exponential(min=4, max=60), retry=retry_if_exception_type((httpx.HTTPStatusError, httpx.TimeoutException)), reraise=True)`. Inside: `await self._limiter.try_acquire(url)`; build headers with USER_AGENT + optional `If-None-Match`; httpx.AsyncClient GET with timeout=30; if 304 return `(None, etag)`; if 200 return `(resp.json(), resp.headers.get('ETag'))`; raise_for_status() on others. On exception bubble-out after all retries: write a snapshot via `SnapshotStore` with `snapshot_reason="source_5xx"` (single observation list with the error context) THEN re-raise. Caller decides to consume snapshot.
      - `async def get_bytes(self, url: str, etag: str | None = None) -> tuple[bytes | None, str | None]`: same retry pattern, returns response.content
      - `async def check_robots(self, url: str) -> bool`: parse host from url; fetch `https://{host}/robots.txt` (cached 24h in aiocache); `urllib.robotparser.RobotFileParser` to check allowed for our USER_AGENT and target path. Return True if no robots.txt found (HTTP 404) or if parser says allowed; False otherwise.

    Create `tests/unit/test_snapshot.py`:
    - `test_write_and_read_jsonld(tmp_path)`: write a snapshot, read latest, assert `@context` present and observations preserved
    - `test_path_traversal_rejected(tmp_path)`: `pytest.raises(ValueError, write_jsonld, source_id="../etc", ...)` blocks `..` injection
    - `test_rotate_deletes_old_files(tmp_path, monkeypatch)`: create 3 snapshot files with mtimes 10/20/40 days ago; rotate(max_age_days=30) deletes only the 40-day-old one; returns 1

    Create `tests/unit/test_polite_http.py`:
    - Mock `httpx.AsyncClient` via `respx` (RESEARCH.md line 102 — respx allowed for edge-case injection)
    - `test_user_agent_header_correct(respx_mock)`: assert request `User-Agent` matches the template (use `respx_mock.get(...).respond(...)` + inspect `call.request.headers["User-Agent"]`)
    - `test_get_json_returns_data_on_200(respx_mock)`: respond JSON `{"x": 1}`, ETag `"abc"`; assert `(data, etag) == ({"x":1}, "abc")`
    - `test_get_json_304_returns_none_data(respx_mock)`: respond 304 No Content; assert returns `(None, etag_passed_in)`
    - `test_get_json_500_retries_and_writes_snapshot(respx_mock, tmp_path)`: respond 500 always; tenacity tries 5 times; pytest.raises(...) bubbles. Assert `tmp_path/{source_id}/...` contains 1 `.jsonld` file
    - `test_check_robots_disallowed_returns_false(respx_mock)`: serve robots.txt `User-agent: *\nDisallow: /private`; check_robots(url with `/private`) returns False
    - `test_check_robots_allowed_returns_true(respx_mock)`: same robots.txt; check_robots(url with `/public`) returns True
    - For rate-limit testing, use a fake `RedisBucket` via monkeypatch or test against a `fakeredis.aioredis.FakeRedis`; assert 31 rapid `get_json` calls take at least 60s simulated time (skip the precise timing — assert `try_acquire` is called for each request via spy)

    Snapshot test for `record_success` + `is_stale` uses `fakeredis.aioredis.FakeRedis` (add to dev deps if not present): assert recording + stale detection at 2x cadence.
  </behavior>
  <verify>
    <automated>uv run pytest tests/unit/test_snapshot.py tests/unit/test_polite_http.py -v -x</automated>
  </verify>
  <done>
    All tests pass; PoliteHttpClient satisfies ING-02 (UA, robots.txt 24h cache, Redis rate-limit, tenacity 5x exponential backoff, ETag cache, snapshot fallback); SnapshotStore writes JSON-LD per D-16 path layout; `rotate()` enforces D-17 30-day retention; record_success/is_stale satisfy ING-14 canary metric.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: IdempotentWriter (asyncpg COPY + ON CONFLICT) + repos + dual-hypertable routing — ING-12 + D-23</name>
  <files>packages/core/climatepulse_core/storage/writer.py, packages/core/climatepulse_core/storage/repos.py, tests/integration/test_writer.py</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 273-280 Writer in architecture; Architectural Responsibility Map line 132)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-23 dual-hypertable routing; ING-12 ON CONFLICT)
  </read_first>
  <behavior>
    - Writer.write_batch with is_grid_based=False inserts into observations hypertable
    - Writer.write_batch with is_grid_based=True inserts into gridded_observations
    - Re-running write_batch with the same RawObservation list produces 0 new rows (idempotency)
    - Re-running with different value for same PK updates the row (ON CONFLICT DO UPDATE)
    - Records with QcFlag.MISSING are still inserted (Pitfall C)
    - record_success is called after successful write_batch (ING-14 wiring)
    - Repos.upsert_source/upsert_variable/upsert_station handle the metadata side; surrogate id cache is populated for hot-path COPY
  </behavior>
  <action>
    Note: executor splits this TDD task into RED (write failing integration tests against testcontainer) + GREEN (implement Writer + repos) per TDD discipline.

    Create `packages/core/climatepulse_core/storage/repos.py`:
    - `class MetadataRepo` with constructor `(self, db_pool: asyncpg.Pool)`
    - `async def upsert_source(self, source: Source) -> int`: `INSERT INTO sources(source_id,name,license,attribution,terms_url,enabled) VALUES (...) ON CONFLICT (source_id) DO UPDATE SET name=EXCLUDED.name, ... RETURNING id` — returns surrogate SmallInteger id
    - `async def upsert_variable(self, variable: Variable) -> int`: similar with wmo_code unique
    - `async def upsert_station(self, station: Station, source_pk: int) -> int`: ON CONFLICT (source_id, external_id) DO UPDATE
    - `async def load_sources_cache(self) -> dict[str, int]`: returns {source_id_str: pk_int}
    - `async def load_variables_cache(self) -> dict[str, int]`: returns {wmo_code: pk_int}
    - `async def load_stations_cache(self, source_pk: int) -> dict[str, int]`: returns {external_id: pk_int} for the given source

    Create `packages/core/climatepulse_core/storage/writer.py`:
    - `class IdempotentWriter` with `__init__(self, db_pool, redis_client, sources_cache: dict[str,int], variables_cache: dict[str,int], stations_cache: dict[str, dict[str,int]])` (stations_cache is keyed by source_id_str → {external_id: pk})
    - `async def write_batch(self, adapter: WeatherSourceAdapter, observations: list[RawObservation]) -> int`:
      - Branch on `adapter.is_grid_based`
      - For station-based: build tuples `(observed_at, station_pk, variable_pk, source_pk, value, qc_flag)` from observations using caches (skip rows where lookup fails — log warning + count as schema_violation, but proceed); use a temp table approach: `CREATE TEMP TABLE obs_staging (LIKE observations INCLUDING DEFAULTS) ON COMMIT DROP`; `await conn.copy_records_to_table('obs_staging', records=tuples, columns=[...])`; then `INSERT INTO observations (observed_at, station_id, variable_id, source_id, value, qc_flag) SELECT observed_at, station_id, variable_id, source_id, value, qc_flag FROM obs_staging ON CONFLICT (station_id, variable_id, observed_at, source_id) DO UPDATE SET value = EXCLUDED.value, qc_flag = EXCLUDED.qc_flag`. Wrap in a single transaction.
      - For grid-based: tuples `(valid_at, init_time, step_h, lat_idx, lon_idx, lat, lon, variable_pk, source_pk, value, qc_flag)`; staging table `LIKE gridded_observations`; INSERT...SELECT ON CONFLICT (lat_idx, lon_idx, variable_id, valid_at, source_id) DO UPDATE
      - After successful commit, call `await record_success(redis_client, adapter.source_id)` (ING-14)
      - Return row count from the INSERT (use RETURNING count(*) or asyncpg result tag)
    - `async def refresh_caches(self) -> None`: re-loads all caches; called by Writer factory at worker startup OR on cache-miss

    Create `tests/integration/test_writer.py`:
    - Uses `db_pool` + `seed_metadata` fixtures from Plan 02 conftest
    - `test_write_station_batch_inserts_rows`: build 5 RawObservations for the seeded station, write_batch with a stub adapter `is_grid_based=False`, assert `count(observations) == 5`
    - `test_write_station_batch_idempotent`: same observations written twice; final count is still 5 (UPDATE branch; no duplicates)
    - `test_write_station_batch_updates_on_conflict`: write value=285.0, then write same PK with value=290.0; count is 5 (no growth); SELECT value returns 290.0
    - `test_write_grid_batch_routes_to_gridded`: stub adapter with `is_grid_based=True`; build 4 grid RawObservations with lat_idx/lon_idx/valid_at/init_time/step_h populated; assert `count(gridded_observations) == 4` and `count(observations) == 0` for the grid source_id
    - `test_write_missing_value_inserts_with_qc_missing`: per Pitfall C, send a row with `qc_flag=MISSING` and `value=0.0` (sentinel from handle_null_value); assert it persists with `qc_flag=1`
    - `test_write_batch_records_success_metric`: use `fakeredis.aioredis`; after successful write_batch, assert `await fakeredis.get(f"metrics:last_success:{source_id}")` is not None
  </behavior>
  <verify>
    <automated>uv run pytest tests/integration/test_writer.py -v -x</automated>
  </verify>
  <done>
    All 6 tests pass; IdempotentWriter correctly routes station vs grid observations to the two hypertables per D-23; ON CONFLICT DO UPDATE semantics verified for both; ING-14 `last_success` metric recorded in Redis after each successful write; Pitfall C null-handling verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External HTTP source (ARPAE / ECMWF) → PoliteHttpClient | Untrusted JSON / GRIB content crosses |
| User-provided source_id string → SnapshotStore filesystem path | Path traversal risk if not validated |
| Adapter cache lookup → asyncpg COPY | Stale cache → orphaned FK rows; risk mitigated by ON CONFLICT |
| Redis key namespace `metrics:last_success:*` | Untrusted source_id concatenation could pollute key space |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ARPAE JSON / GRIB payloads → DB | mitigate | Validate at adapter boundary (Plan 04) with Pydantic; range-check in `normalize.qc.validate_range`; asyncpg parameterized INSERT (no SQL string concat) |
| T-03-02 | Information Disclosure | snapshot files on volume | accept | Snapshots contain only public open-data observations; no PII; volume permissions are container-internal |
| T-03-03 | DoS | PoliteHttpClient retries amplify failures | mitigate | tenacity `stop_after_attempt(5)` + `wait_random_exponential(min=4, max=60)` caps total retry time to ~10 min; per-host RedisBucket prevents thundering-herd across workers |
| T-03-04 | Tampering | SnapshotStore.write_jsonld path injection | mitigate | Reject source_id/station_id containing path separators or `..` via regex `^[a-zA-Z0-9_-]+$`; raise ValueError on violation |
| T-03-05 | Spoofing | User-Agent header could be spoofed by anyone proxying our endpoint | accept | Open-data; politeness is per-host, not per-identity; the ARPAE admin reading our UA gets a contact email |
| T-03-06 | Repudiation | Writer.write_batch silent failures | mitigate | Surrogate-cache miss logs a structlog warning with source_id + external_id; row skipped (not silently inserted as wrong station); count of skipped is part of return value |
| T-03-07 | EoP | Redis broker if exposed | mitigate | (Enforced in Plan 05 compose.yaml) Redis service has NO `ports:` mapping — only internal network |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | pyrate-limiter 4.1.0, aiocache 0.12.3, tenacity 9.1.4 — all `[OK]` from RESEARCH.md Package Legitimacy Audit (no `[ASSUMED]`/`[SUS]`/`[SLOP]` introduced in this plan) |
</threat_model>

<verification>
- `pytest tests/unit/test_adapter_registry.py` green (Task 1)
- `pytest tests/unit/test_normalizer.py tests/unit/test_timezone.py tests/unit/test_qc.py` green incl. hypothesis 200+ examples per `@given` (Task 2)
- `pytest tests/unit/test_polite_http.py tests/unit/test_snapshot.py` green (Task 3)
- `pytest tests/integration/test_writer.py` green against testcontainer TimescaleDB (Task 4)
- Both DST cases per D-26 explicitly covered (2026-03-29 02:30 and 2026-10-25 02:30) (Task 2)
- Writer routes by `is_grid_based` to the correct hypertable per D-23 (Task 4)
- ING-14 `last_success` metric is recorded via Redis SETEX after successful write_batch (Task 4)
- PoliteHttpClient satisfies ING-02 in full (Task 3)
</verification>

<success_criteria>
- ING-01 (ABC + registry), ING-02 (polite HTTP client), ING-09 (WMO normalizer 7 vars in SI), ING-10 (timezone + DST tests), ING-12 (idempotent writer), ING-14 (per-source health metric) — fully shipped
- D-23 dual-hypertable routing is encoded in Writer logic and verified
- D-26 hypothesis coverage in place (Normalizer round-trip + Timezone DST round-trip)
- Plan 04 has a complete API surface to subclass for ARPAE + ECMWF concrete adapters
</success_criteria>

<output>
Create `.planning/phases/01-foundation-slice-public-docs/01-03-SUMMARY.md` when done. Capture: the exact `WeatherSourceAdapter` abstractmethod signatures (so Plan 04 can mirror them), the final Pint UnitRegistry custom-units list, the snapshot path layout regex, and the Redis DB allocation table (broker/results/ratelimit/etag/metrics).
</output>

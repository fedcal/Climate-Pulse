---
phase: 01-foundation-slice-public-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - apps/api/pyproject.toml
  - apps/worker/pyproject.toml
  - packages/core/pyproject.toml
  - packages/migrations/pyproject.toml
  - .gitignore
  - .env.example
  - .pre-commit-config.yaml
  - .github/workflows/ci.yml
  - .github/workflows/docs.yml
  - mkdocs.yml
  - docs/index.md
  - docs/quickstart.md
  - docs/scraping-policy.md
  - README.md
autonomous: false
requirements: [DOC-01, DOC-02, DOC-03, DOC-05, OPS-07, OPS-08, OPS-09]
user_setup:
  - service: github-pages
    why: "GitHub Pages must be enabled with 'GitHub Actions' source before actions/deploy-pages@v4 succeeds (RESEARCH.md line 1149)"
    dashboard_config:
      - task: "Enable Pages with source 'GitHub Actions'"
        location: "Repository Settings -> Pages -> Build and deployment -> Source: GitHub Actions"

must_haves:
  truths:
    - "MkDocs Material site is live at federicocalo.github.io/climate-pulse"
    - "Every docs page shows the verbatim footer 'Climate Pulse · MIT License · federicocalo.dev' with federicocalo.dev as a clickable link"
    - "GitHub Actions deploys docs on every push to main without manual intervention"
    - "CI blocks any PR that breaks pytest/ruff/pyright or that gitleaks detects as containing a secret"
    - "The repository has the canonical uv workspace layout (apps/api, apps/worker, packages/core, packages/migrations)"
    - "A reader can read docs/quickstart.md and docs/scraping-policy.md from the deployed site"
  artifacts:
    - path: "mkdocs.yml"
      provides: "MkDocs Material config including verbatim copyright footer"
      contains: "Climate Pulse · MIT License · <a href='https://federicocalo.dev'>federicocalo.dev</a>"
    - path: ".github/workflows/docs.yml"
      provides: "Two-job MkDocs build + deploy-pages workflow per D-03"
      contains: "actions/deploy-pages@v4"
    - path: ".github/workflows/ci.yml"
      provides: "pytest + ruff + pyright + coverage 80/70 + gitleaks gates per OPS-08 + D-24"
      contains: "gitleaks-action"
    - path: "pyproject.toml"
      provides: "uv workspace root listing apps/* + packages/*"
      contains: "[tool.uv.workspace]"
    - path: ".pre-commit-config.yaml"
      provides: "ruff + pyright + gitleaks pre-commit hooks per OPS-07"
      contains: "gitleaks"
    - path: ".env.example"
      provides: "Placeholder env vars (POSTGRES_*, REDIS_URL, SNAPSHOT_DIR) - no real secrets"
      contains: "POSTGRES_PASSWORD=changeme"
    - path: "docs/scraping-policy.md"
      provides: "Public scraping policy for ARPA admin transparency per OPS-09"
      contains: "User-Agent"
    - path: "docs/quickstart.md"
      provides: "Quickstart guide with docker compose up + backfill command + psql verify per D-12"
      contains: "climatepulse backfill arpae"
  key_links:
    - from: ".github/workflows/docs.yml"
      to: "GitHub Pages environment"
      via: "actions/deploy-pages@v4 with environment github-pages"
      pattern: "environment:\\s*\\n\\s*name:\\s*github-pages"
    - from: "mkdocs.yml"
      to: "footer rendered on every page"
      via: "Material copyright field"
      pattern: "^copyright:"
    - from: ".github/workflows/ci.yml"
      to: ".pre-commit-config.yaml"
      via: "ruff + pyright + gitleaks invocation in CI job"
      pattern: "gitleaks"
---

<objective>
Establish the uv workspace skeleton + CI gates + MkDocs Material site live on GitHub Pages with the verbatim attribution footer. This plan delivers the publicly-visible "Climate Pulse exists" vertical slice independent of any backend work.

Purpose: Phase 1 hard requirement per PROJECT.md is "docs published to GitHub Pages from v0.1 with `federicocalo.dev` footer on every page" — non-negotiable. CI gates (lint, type, secrets) must also be in place before any code is committed.

Output: pyproject (uv workspace), per-package pyprojects, MkDocs config with footer, two GitHub Actions workflows (CI + docs deploy), pre-commit config, .env.example, scraping-policy + quickstart + index docs pages. After this plan, pushing to `main` deploys docs to `federicocalo.github.io/climate-pulse` and CI blocks broken/secret-leaking PRs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md
@.planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md
@.planning/phases/01-foundation-slice-public-docs/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold uv workspace + package skeletons + .gitignore + .env.example</name>
  <files>pyproject.toml, apps/api/pyproject.toml, apps/api/climatepulse_api/__init__.py, apps/worker/pyproject.toml, apps/worker/climatepulse_worker/__init__.py, packages/core/pyproject.toml, packages/core/climatepulse_core/__init__.py, packages/migrations/pyproject.toml, packages/migrations/climatepulse_migrations/__init__.py, .gitignore, .env.example, README.md</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (sections: Recommended Project Structure lines 311-386, Standard Stack lines 141-216, Installation block lines 195-215)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-15 SNAPSHOT_DIR default)
    - CLAUDE.md (Technology Stack pins)
  </read_first>
  <action>
    Create the uv workspace root `pyproject.toml` with `[tool.uv.workspace] members = ["apps/api", "apps/worker", "packages/core", "packages/migrations"]`, `requires-python = ">=3.12,<3.13"`, project name `climate-pulse` (root is non-publishable). Add `[tool.ruff]` (line-length 100, target-version py312, select common rule sets E,F,I,UP,B,N), `[tool.ruff.lint.per-file-ignores]` for tests, `[tool.pyright]` (pythonVersion "3.12", strict true on packages/core, basic elsewhere), `[tool.pytest.ini_options]` with `asyncio_mode = "auto"`, `testpaths = ["tests"]`, `addopts = "--cov=packages --cov=apps --cov-report=xml --cov-report=term-missing"`, `[tool.coverage.run] source = ["packages", "apps"]`.

    Create per-package pyprojects with the dependency split MANDATED by D-22 (libeccodes ONLY in worker, NOT in api):
    - `packages/core/pyproject.toml`: name `climatepulse-core`, deps `httpx==0.28.*`, `tenacity==9.1.*`, `aiocache==0.12.*`, `pyrate-limiter==4.1.*`, `pint==0.25.*`, `pydantic>=2.9`, `pydantic-settings==2.14.*`, `sqlalchemy[asyncio]==2.0.*`, `asyncpg==0.31.*`, `structlog==25.5.*`, `redis<6.0`. NO ecmwf/cfgrib here.
    - `apps/api/pyproject.toml`: name `climatepulse-api`, deps `fastapi==0.136.*`, `uvicorn[standard]==0.47.*`, `climatepulse-core` (workspace path dep). EXPLICITLY no cfgrib/ecmwf-opendata.
    - `apps/worker/pyproject.toml`: name `climatepulse-worker`, deps `celery[redis]==5.6.3`, `redis<6.0`, `climatepulse-core` (workspace path dep), `ecmwf-opendata==0.3.*`, `cfgrib==0.9.15.*`, `xarray==2026.4.*`, `numpy>=2.1`. This is the ONLY package that pulls cfgrib.
    - `packages/migrations/pyproject.toml`: name `climatepulse-migrations`, deps `alembic==1.18.*`, `sqlalchemy-timescaledb==0.4.*`, `asyncpg==0.31.*`, `sqlalchemy[asyncio]==2.0.*`.

    Root dev-deps under `[dependency-groups] dev = [ ... ]` (PEP 735 — uv supports natively): `pytest==8.3.*`, `pytest-asyncio==1.3.*`, `pytest-cov==7.1.*`, `hypothesis==6.152.*`, `testcontainers[postgres]==4.14.*`, `fakeredis>=2.23` (required by Plan 03 PoliteHttpClient tests + Plan 05 heartbeat tests — must be in dev deps from Wave 0), `vcrpy==8.1.*`, `pytest-recording`, `respx>=0.21`, `ruff==0.15.*`, `pyright==1.1.*`, `pre-commit==4.6.*`, `mkdocs-material==9.7.*`, `mkdocstrings[python]==1.0.*`.

    Create `__init__.py` files (empty is fine) for each package's Python module directory: `apps/api/climatepulse_api/__init__.py`, `apps/worker/climatepulse_worker/__init__.py`, `packages/core/climatepulse_core/__init__.py`, `packages/migrations/climatepulse_migrations/__init__.py`.

    Create `.gitignore`: standard Python (`__pycache__/`, `*.pyc`, `.venv/`, `dist/`, `build/`, `*.egg-info/`), uv (`uv.lock` — KEEP this in git per uv recommendation, so do NOT ignore it), pytest (`.pytest_cache/`, `.coverage`, `htmlcov/`, `coverage.xml`), mkdocs (`site/`), env files (`.env`, `.env.*` but NOT `.env.example`), IDE (`.idea/`, `.vscode/`), OS (`.DS_Store`), GRIB scratch (`*.grib*`, `*.idx`), snapshot dev volume (`snapshots/`).

    Create `.env.example` with placeholders only (no real secrets — gitleaks must NOT flag this file): `POSTGRES_DB=climatepulse`, `POSTGRES_USER=climatepulse`, `POSTGRES_PASSWORD=changeme`, `DATABASE_URL=postgresql+asyncpg://climatepulse:changeme@timescale:5432/climatepulse`, `REDIS_URL=redis://redis:6379`, `SNAPSHOT_DIR=/var/lib/climatepulse/snapshots` (default per D-15), `LOG_LEVEL=INFO`, `ENVIRONMENT=development`.

    Create `README.md` with project name, one-paragraph elevator pitch from PROJECT.md, badge placeholders (CI status, docs status, coverage), license MIT, link to deployed docs at `https://federicocalo.github.io/climate-pulse`, and an explicit "Do NOT scale the `beat` service" warning paragraph referencing Pitfall F.

    Run `uv sync --all-packages --all-groups` to materialize `uv.lock` and verify the workspace resolves.
  </action>
  <verify>
    <automated>uv sync --all-packages --all-groups &amp;&amp; uv run python -c "import climatepulse_core, climatepulse_api, climatepulse_worker, climatepulse_migrations" &amp;&amp; test -f uv.lock &amp;&amp; test -f .env.example &amp;&amp; ! grep -E '(password|secret|token|key)\s*=\s*[A-Za-z0-9]{16,}' .env.example</automated>
  </verify>
  <done>
    `uv.lock` exists; `uv sync --all-packages --all-groups` succeeds; all four workspace packages import; `.env.example` contains only placeholder values (no real secrets); README has the "Do NOT scale beat" warning; libeccodes/cfgrib appears ONLY in `apps/worker/pyproject.toml` per D-22 (verifiable by `grep -l cfgrib apps/api/pyproject.toml packages/core/pyproject.toml` returning empty).
  </done>
</task>

<task type="auto">
  <name>Task 2: MkDocs Material config + docs pages + verbatim footer (DOC-01/02/03/05 + OPS-09)</name>
  <files>mkdocs.yml, docs/index.md, docs/quickstart.md, docs/scraping-policy.md</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 1124-1149 mkdocs.yml footer pattern; lines 1066-1122 GitHub Pages workflow)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-05/D-06 verbatim footer string; D-12 quickstart command shape; D-13 no Phase 2+ leak)
  </read_first>
  <action>
    Create `mkdocs.yml` with EXACTLY these fields:
    - `site_name: Climate Pulse`
    - `site_url: https://federicocalo.github.io/climate-pulse`
    - `repo_url: https://github.com/federicocalo/climate-pulse` (D-01)
    - `repo_name: federicocalo/climate-pulse`
    - `copyright: "Climate Pulse · MIT License · <a href='https://federicocalo.dev'>federicocalo.dev</a>"` — this string is VERBATIM per D-05/D-06; use the middle-dot character `·` (U+00B7), not hyphens or bullets; do not paraphrase
    - `theme.name: material`, palette with `primary: blue` + `accent: light-blue`, features `navigation.sections` + `content.code.copy`
    - `nav:` listing `Home: index.md`, `Quickstart: quickstart.md`, `Scraping Policy: scraping-policy.md`
    - `plugins:` `- search`, `- mkdocstrings: handlers: python: paths: [packages/core]`
    - NO navbar `Author` link / no social icon per D-08
    - NO `extra.version` block (mike versioning is Phase 5, D-04)

    Create `docs/index.md`: one-paragraph overview lifted from PROJECT.md "What This Is", followed by an "Architecture at a glance" section with the high-level data flow ARPA + ECMWF → Celery worker → TimescaleDB (TWO hypertables) → minimal FastAPI `/healthz` + `/readyz`. Mention v0.1 status. Do NOT advertise any other API endpoint (D-13: no Phase 2+ leak). Link to the deployed quickstart page.

    Create `docs/quickstart.md` with these sections in order:
    1. "Prerequisites" — Docker Engine 25+ and Docker Compose v2.30+ on Linux/macOS
    2. "Clone and start" — `git clone https://github.com/federicocalo/climate-pulse && cd climate-pulse && cp .env.example .env && docker compose up -d`
    3. "Verify health" — `curl http://localhost:8000/healthz` returns `{"status":"ok"}`; `curl http://localhost:8000/readyz` returns 200 once DB+Redis+worker are up (note this can take ~60s as TimescaleDB initializes)
    4. "Run the backfill" — EXACT command shape from D-12: `docker compose exec worker climatepulse backfill arpae --from $(date -d '7 days ago' +%F) --to $(date +%F)` (the `--from`/`--to` are ISO dates; positional `arpae` is the source id)
    5. "Verify rows" — `docker compose exec timescale psql -U climatepulse -d climatepulse -c "select count(*), source_id from observations group by source_id"` should return a non-zero row count
    6. "Idempotency check" — re-run the backfill command from step 4; the count from step 5 MUST NOT change. This is the user-facing idempotency contract per CONTEXT.md "specifics".
    7. "What's next" — link to scraping-policy.md ONLY; do NOT mention `/v1/stations` or any Phase 2+ endpoint (D-13).

    Create `docs/scraping-policy.md` per OPS-09: explain Climate Pulse's politeness commitments to ARPA admins: User-Agent string format `ClimatePulse/{version} (+https://github.com/federicocalo/climate-pulse; contact: fedcal01@gmail.com)`, default rate limit (30 req/min per host via pyrate-limiter Redis backend), robots.txt RFC 9309 compliance (fetched + cached 24h), ETag/If-None-Match cache via aiocache, snapshot fallback (no hammer-on-error). Provide a clear "contact us if our crawler is misbehaving" mailto link to `fedcal01@gmail.com`. Page must be linked from the nav (already added above) so ARPA admins find it without searching.
  </action>
  <verify>
    <automated>uv run mkdocs build --strict --site-dir site_test &amp;&amp; grep -c "Climate Pulse · MIT License · <a href='https://federicocalo.dev'>federicocalo.dev</a>" mkdocs.yml | grep -q '^1$' &amp;&amp; grep -q "climatepulse backfill arpae" docs/quickstart.md &amp;&amp; grep -q "User-Agent" docs/scraping-policy.md &amp;&amp; ! grep -E "/v1/(stations|observations|sources|ws)" docs/quickstart.md &amp;&amp; rm -rf site_test</automated>
  </verify>
  <done>
    `mkdocs build --strict` exits 0 (zero warnings); the exact verbatim copyright string with middle-dot `·` is present in `mkdocs.yml`; quickstart contains the exact `climatepulse backfill arpae` command shape from D-12; scraping-policy mentions User-Agent + rate limit + robots.txt + contact email; quickstart does NOT mention any Phase 2 endpoint (verified by grep -v for `/v1/...` paths).
  </done>
</task>

<task type="auto">
  <name>Task 3: Pre-commit config + GitHub Actions CI + Docs deploy workflow (OPS-07/08 + DOC-02 + D-03/D-24)</name>
  <files>.pre-commit-config.yaml, .github/workflows/ci.yml, .github/workflows/docs.yml</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 1066-1149 docs.yml two-job pattern + mkdocs.yml; lines 169-184 testing deps; lines 184-192 GitHub-native action versions)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-03 GitHub-native actions; D-24 80/70 coverage gate)
    - .planning/PROJECT.md (constraints — ubuntu-22.04 + python 3.12.7 default per Claude's Discretion)
  </read_first>
  <action>
    Create `.pre-commit-config.yaml` with these hooks in order:
    - `repos: - repo: https://github.com/astral-sh/ruff-pre-commit rev: v0.15.14 hooks: [{id: ruff-check, args: [--fix]}, {id: ruff-format}]`
    - `- repo: https://github.com/RobertCraigie/pyright-python rev: v1.1.409 hooks: [{id: pyright}]`
    - `- repo: https://github.com/gitleaks/gitleaks rev: v8.21.2 hooks: [{id: gitleaks}]` (use a recent stable gitleaks tag; the `gitleaks-action@v2` is the GitHub Action variant used in CI workflow — different artifact)
    - `- repo: https://github.com/pre-commit/pre-commit-hooks rev: v5.0.0 hooks: [{id: trailing-whitespace}, {id: end-of-file-fixer}, {id: check-merge-conflict}, {id: check-yaml}, {id: check-toml}]`

    Create `.github/workflows/ci.yml` named `CI`, triggered on `push: branches: [main]` and `pull_request: branches: [main]`. Single job `test` on `ubuntu-22.04` with `python-version: "3.12.7"`. Steps in order:
    1. `actions/checkout@v4` with `fetch-depth: 0` (gitleaks needs full history)
    2. `astral-sh/setup-uv@v3` with `version: "latest"`
    3. `actions/setup-python@v5` with python-version 3.12.7
    4. `uv sync --all-packages --all-groups`
    5. `uv run ruff check .`
    6. `uv run ruff format --check .`
    7. `uv run pyright`
    8. `uv run pytest -v --cov=packages --cov=apps --cov-report=xml --cov-fail-under=80` — root global gate 80% per D-24
    9. Adapter-only coverage gate: `uv run pytest packages/core/climatepulse_core/adapters --cov=packages/core/climatepulse_core/adapters --cov-fail-under=70` per D-24 (lower bar acknowledges scraping/mocking difficulty). Use `continue-on-error: false`.
    10. `gitleaks/gitleaks-action@v2` with `env: GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}` (free for public repos)

    The CI job MUST install Docker if integration tests need it. Use `services:` block only for trivial services; testcontainers manages its own. Add a job-level note via `env:` reserved for `PYTHONUNBUFFERED: "1"`.

    Create `.github/workflows/docs.yml` named `Deploy Documentation`. Use the EXACT two-job structure from RESEARCH.md lines 1066-1122:
    - Top-level: `on: push: branches: [main]`, `workflow_dispatch: {}`; `permissions: contents: read, pages: write, id-token: write`; `concurrency: group: pages, cancel-in-progress: false`
    - Job `build` on `ubuntu-latest`: checkout v4 with `fetch-depth: 0`, setup-python v5 with `3.12.7`, install docs deps via `pip install mkdocs-material==9.7.* "mkdocstrings[python]==1.0.*"` (lightweight — no full workspace install needed), `mkdocs build --strict`, `actions/configure-pages@v5`, `actions/upload-pages-artifact@v3 with: path: ./site`
    - Job `deploy` (needs: build) on `ubuntu-latest` with `environment: name: github-pages url: ${{ steps.deployment.outputs.page_url }}` and step `actions/deploy-pages@v4` with `id: deployment`

    Do NOT use `peaceiris/actions-gh-pages` (D-03 forbids it). Do NOT use `mike` (D-04 defers to Phase 5).
  </action>
  <verify>
    <automated>uv run pre-commit run --all-files || true ; grep -q "actions/deploy-pages@v4" .github/workflows/docs.yml &amp;&amp; ! grep -q "peaceiris" .github/workflows/docs.yml &amp;&amp; ! grep -q "mike deploy" .github/workflows/docs.yml &amp;&amp; grep -q "gitleaks" .github/workflows/ci.yml &amp;&amp; grep -q "cov-fail-under=80" .github/workflows/ci.yml &amp;&amp; grep -q "cov-fail-under=70" .github/workflows/ci.yml &amp;&amp; grep -q "environment:" .github/workflows/docs.yml &amp;&amp; grep -q "name: github-pages" .github/workflows/docs.yml</automated>
  </verify>
  <done>
    `pre-commit run --all-files` runs all 4 hook groups; CI workflow contains gitleaks-action@v2, ruff-check, ruff-format, pyright, pytest with --cov-fail-under=80 + adapter --cov-fail-under=70; docs workflow uses GitHub-native deploy-pages@v4 + configure-pages@v5 + upload-pages-artifact@v3 with `environment: github-pages`; peaceiris is NOT used anywhere; mike is NOT invoked.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>uv workspace skeleton + MkDocs Material site config + GitHub Actions CI + docs deploy workflow.</what-built>
  <how-to-verify>
    Phase 1 docs deploy requires one-time human GitHub setup that cannot be automated:
    1. On the GitHub repo settings page (github.com/federicocalo/climate-pulse/settings/pages), set **Build and deployment > Source = GitHub Actions** (RESEARCH.md line 1149 — without this, `actions/deploy-pages@v4` fails with "Branch 'gh-pages' doesn't exist").
    2. Push the branch with this plan's changes to `main`.
    3. Observe the `Deploy Documentation` workflow run green on Actions tab.
    4. Visit https://federicocalo.github.io/climate-pulse and confirm:
       - The homepage loads (index.md content visible).
       - The footer at the bottom of every page reads literally `Climate Pulse · MIT License · federicocalo.dev` with `federicocalo.dev` rendered as a clickable link to `https://federicocalo.dev` (verbatim per D-05).
       - The nav contains Home, Quickstart, Scraping Policy entries (3 pages).
       - The Quickstart page contains the literal command `climatepulse backfill arpae --from ... --to ...` (D-12).
    5. Open a throwaway PR that adds `API_KEY = "ghp_abcdefghijklmnopqrstuvwxyz0123456789"` to any file. Confirm CI gitleaks job fails and blocks the PR. Close the PR without merging.
  </how-to-verify>
  <resume-signal>Type "approved" once docs site is live with verbatim footer and gitleaks blocks the test PR. If the footer text doesn't render verbatim or Pages source isn't set, describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem → git remote | Developer commits may inadvertently include secrets |
| GitHub Actions runner → GitHub Pages | Build artifact deploys to public site (public asset) |
| Public docs site → readers | Read-only; readers cannot mutate; quickstart commands are runnable locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env.example` + repo files | mitigate | gitleaks pre-commit (.pre-commit-config.yaml) + gitleaks-action@v2 in CI block any push containing secrets; `.env` is gitignored; `.env.example` contains only `changeme` placeholders |
| T-01-02 | Tampering | GitHub Actions workflow | mitigate | Use `environment: github-pages` (D-03) — deploy step requires environment protection rule; `permissions: contents: read` minimizes write scope |
| T-01-03 | Information Disclosure | Quickstart docs | mitigate | Do NOT publish any Phase 2 endpoint in docs (D-13); reviewed via grep in Task 2 acceptance |
| T-01-04 | Repudiation | Docs deploys | accept | All deploys go through `environment: github-pages` audit log; per-deploy GitHub commit SHA recorded; sufficient for a public open-data project |
| T-01-05 | Denial of Service | CI minutes | accept | Public repo gets unlimited free GitHub Actions minutes; gitleaks scan is bounded |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | All pip packages in Task 1 carry `[OK]` from RESEARCH.md Package Legitimacy Audit (lines 222-251) except vcrpy which is `[SUS]` false positive verified legitimate (RESEARCH.md line 243); no `[ASSUMED]`/`[SLOP]` packages → no blocking-human checkpoint required |
</threat_model>

<verification>
- `uv sync --all-packages --all-groups` succeeds (Task 1)
- All four workspace packages are importable in Python 3.12.7 (Task 1)
- `mkdocs build --strict` produces zero warnings (Task 2)
- mkdocs.yml `copyright:` field contains the verbatim string with middle-dot per D-05 (Task 2)
- `.github/workflows/docs.yml` uses GitHub-native `actions/deploy-pages@v4` (D-03) NOT peaceiris (Task 3)
- CI workflow enforces `--cov-fail-under=80` global and `--cov-fail-under=70` for adapters per D-24 (Task 3)
- After human one-time GitHub Pages source = GitHub Actions setting, `federicocalo.github.io/climate-pulse` is live (Task 4 checkpoint)
- A PR that contains a fake secret is blocked by gitleaks-action@v2 (Task 4 checkpoint)
</verification>

<success_criteria>
- ROADMAP Phase 1 Success Criteria #3 satisfied: "MkDocs Material documentation site is live on GitHub Pages at the project URL, auto-deployed on every push to `main`, and every page shows a `federicocalo.dev` clickable footer."
- ROADMAP Phase 1 Success Criteria #5 satisfied: "CI (GitHub Actions) blocks any PR that breaks pytest, ruff, pyright, or leaks a secret detected by gitleaks." (pytest gate exists; subsequent plans add tests that exercise it.)
- DOC-01 / DOC-02 / DOC-03 / DOC-05 fully shipped.
- OPS-07 / OPS-08 / OPS-09 fully shipped.
- Walking Skeleton: dev environment for docs lights up on every push to main.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-slice-public-docs/01-01-SUMMARY.md` when done. Capture: actual mkdocs.yml copyright string (verify byte-for-byte vs D-05), CI job IDs, link to first successful docs deploy run, and any deviation from the planned package version pins.
</output>

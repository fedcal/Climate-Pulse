---
phase: 01-foundation-slice-public-docs
plan: 05
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - apps/worker/climatepulse_worker/celery_app.py
  - apps/worker/climatepulse_worker/tasks/__init__.py
  - apps/worker/climatepulse_worker/tasks/ingest.py
  - apps/worker/climatepulse_worker/tasks/maintenance.py
  - apps/worker/climatepulse_worker/heartbeat.py
  - apps/api/climatepulse_api/main.py
  - apps/api/climatepulse_api/settings.py
  - apps/api/climatepulse_api/routers/__init__.py
  - apps/api/climatepulse_api/routers/health.py
  - infra/Dockerfile.api
  - infra/Dockerfile.worker
  - infra/compose.yaml
  - .env.example
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - tests/unit/test_celery_app.py
  - tests/unit/test_healthz.py
  - tests/integration/test_compose_smoke.py
autonomous: false
requirements: [ING-11, OPS-01, API-10]

must_haves:
  truths:
    - "`docker compose up -d` from project root brings up timescale + redis + api + worker + beat services; all five reach `service_healthy` within 60s; subsequent `docker compose ps` shows all five Up (healthy)"
    - "GET http://localhost:8000/healthz returns 200 with `{\"status\":\"ok\"}` immediately on container start"
    - "GET http://localhost:8000/readyz returns 200 with `{\"status\":\"ready\"}` once TimescaleDB + Redis + at least one Celery worker heartbeat is present; otherwise 503 with error map per dependency"
    - "Celery worker process consumes from queues `ingest`, `normalize`, `alerts`, `dlq` (4 queues) per ING-11"
    - "Beat service runs with `deploy.replicas: 1` per Pitfall #6; compose.yaml has an inline comment 'DO NOT scale beat > 1'"
    - "Beat schedule registers `tasks.ingest.run_source(source_id='arpa_emilia')` every 15 minutes (D-19), `tasks.ingest.run_source(source_id='ecmwf_open')` every 6 hours, `tasks.maintenance.rotate_snapshots` daily (D-17)"
    - "Worker sets Redis key `celery:worker:heartbeat` with SETEX 120 'ok' on startup AND periodically (every 30s) so readyz can detect it"
    - "Worker container image installs `libeccodes0` + `libeccodes-tools` (D-21/D-22); API container image does NOT install libeccodes (D-22 — verified by `docker image inspect` or by absence in Dockerfile.api)"
    - "REQUIREMENTS.md Traceability table moves API-10 from Phase 2 → Phase 1 per D-14"
    - "ROADMAP.md Phase 1 Requirements line includes API-10 in the comma-separated list"
    - "Quickstart end-to-end demo from D-9 passes: fresh `docker compose up` → 10 min wait → ARPAE rows in observations + ECMWF rows in gridded_observations → docs live with footer (Plan 01) → backfill CLI idempotent"
  artifacts:
    - path: "apps/worker/climatepulse_worker/celery_app.py"
      provides: "Celery app instance + queues config + Beat schedule per ING-11"
      contains: "task_queues"
    - path: "apps/worker/climatepulse_worker/tasks/ingest.py"
      provides: "run_source Celery task wrapping adapter.fetch + writer.write_batch; routes SCHEMA_VIOLATION rows to dlq"
      contains: "@app.task"
    - path: "apps/worker/climatepulse_worker/tasks/maintenance.py"
      provides: "rotate_snapshots daily task per D-17"
      contains: "rotate"
    - path: "apps/worker/climatepulse_worker/heartbeat.py"
      provides: "Worker startup + periodic heartbeat writing Redis key celery:worker:heartbeat"
      contains: "celery:worker:heartbeat"
    - path: "apps/api/climatepulse_api/main.py"
      provides: "FastAPI app factory; mounts health router; uses pydantic-settings; no other endpoints per D-14"
      contains: "FastAPI"
    - path: "apps/api/climatepulse_api/routers/health.py"
      provides: "/healthz + /readyz endpoints per API-10"
      contains: "/readyz"
    - path: "infra/compose.yaml"
      provides: "Compose v2 stack with healthchecks per OPS-01"
      contains: "deploy:\n      replicas: 1"
    - path: "infra/Dockerfile.api"
      provides: "Slim API image (NO libeccodes per D-22)"
    - path: "infra/Dockerfile.worker"
      provides: "Worker image with libeccodes0 + libeccodes-tools per D-21/D-22"
      contains: "libeccodes0"
  key_links:
    - from: "apps/worker/climatepulse_worker/celery_app.py"
      to: "Redis (broker DB 0)"
      via: "broker_url = redis://redis:6379/0"
      pattern: "broker_url"
    - from: "infra/compose.yaml"
      to: "Pitfall #6 beat singleton enforcement"
      via: "deploy.replicas: 1 on beat service"
      pattern: "replicas:\\s*1"
    - from: "apps/api/climatepulse_api/routers/health.py"
      to: "Worker heartbeat Redis key"
      via: "redis.get('celery:worker:heartbeat')"
      pattern: "celery:worker:heartbeat"
    - from: "apps/worker/climatepulse_worker/celery_app.py"
      to: "Beat schedule (D-19 + D-17)"
      via: "beat_schedule dict with arpa_emilia 15min + ecmwf_open 6h + rotate_snapshots daily"
      pattern: "beat_schedule"
---

<objective>
Wire the Phase 1 ingestion machinery into Celery (worker + dedicated singleton Beat + 4 queues per ING-11), expose the FastAPI minimal `/healthz` + `/readyz` per D-14/API-10, and ship the Docker Compose stack with healthchecks (OPS-01). Update REQUIREMENTS.md + ROADMAP.md to formalize the D-14 anticipation of API-10 from Phase 2 → Phase 1. Final acceptance demo per D-9 closes the Walking Skeleton.

Purpose: This is the convergence plan. Everything from Plans 02/03/04 — schema, core, adapters, CLI — comes together inside Compose. After this plan, a self-hoster's `docker compose up` proves the entire architectural spine: external API → adapter → polite HTTP → normalize → asyncpg COPY into two hypertables → CAGGs → /readyz green → docs live.

Output: Celery app + tasks + heartbeat; FastAPI minimal app + health router; two Dockerfiles + compose.yaml; updates to .env.example; REQUIREMENTS.md + ROADMAP.md edits per D-14; smoke tests; human checkpoint validating the end-to-end acceptance demo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md
@.planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md
@.planning/phases/01-foundation-slice-public-docs/SKELETON.md
@.planning/phases/01-foundation-slice-public-docs/01-01-SUMMARY.md
@.planning/phases/01-foundation-slice-public-docs/01-02-SUMMARY.md
@.planning/phases/01-foundation-slice-public-docs/01-03-SUMMARY.md
@.planning/phases/01-foundation-slice-public-docs/01-04-SUMMARY.md
@packages/core/climatepulse_core/adapters/base.py
@packages/core/climatepulse_core/storage/writer.py
@apps/worker/climatepulse_worker/cli/backfill.py
@.env.example
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Celery app + 4 queues + Beat schedule + ingest/maintenance tasks + heartbeat (ING-11 + D-17 + D-19)</name>
  <files>apps/worker/climatepulse_worker/celery_app.py, apps/worker/climatepulse_worker/tasks/__init__.py, apps/worker/climatepulse_worker/tasks/ingest.py, apps/worker/climatepulse_worker/tasks/maintenance.py, apps/worker/climatepulse_worker/heartbeat.py, tests/unit/test_celery_app.py</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 977-1060 compose worker/beat config + line 973-974 worker heartbeat pattern; Architectural Responsibility Map lines 134-135)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-17 daily rotate task; D-19 15min ARPAE; D-20 dlq routing for schema_violation)
    - apps/worker/climatepulse_worker/cli/backfill.py (Plan 04 — reuse run_backfill internals for tasks)
    - packages/core/climatepulse_core/adapters/base.py (registry + adapter contract)
  </read_first>
  <behavior>
    - Celery app `app = Celery("climatepulse_worker", broker=settings.redis_url + "/0", backend=settings.redis_url + "/1")` (DB 0 broker, DB 1 results per RESEARCH.md lines 884-889)
    - task_queues includes Queue("ingest"), Queue("normalize"), Queue("alerts"), Queue("dlq")
    - task_routes maps `tasks.ingest.*` → `ingest`, `tasks.maintenance.*` → `ingest`, `tasks.dlq.*` → `dlq` (alerts queue is reserved for Phase 5; declared empty here per ING-11 requirement to have 4 queues)
    - beat_schedule contains 3 entries: arpa_emilia every 900s, ecmwf_open every 21600s, rotate_snapshots crontab(hour=3, minute=0) (daily at 03:00 UTC)
    - ingest.run_source(source_id) calls async run_backfill logic for [now-window, now]; on exception, retries via `bind=True, max_retries=3, default_retry_delay=60`; rows with QcFlag.SCHEMA_VIOLATION are routed (re-queued) to `dlq` queue via `apply_async(queue='dlq')`
    - maintenance.rotate_snapshots calls `SnapshotStore(snapshot_dir).rotate(max_age_days=30)` per D-17
    - Worker startup signal `worker_ready` sets `celery:worker:heartbeat` SETEX 120 'ok'; periodic task every 30s renews it
  </behavior>
  <action>
    Create `apps/worker/climatepulse_worker/celery_app.py`:
    - `from celery import Celery; from celery.schedules import crontab; from kombu import Queue`
    - Import `get_settings` from `climatepulse_core.settings`
    - `settings = get_settings()`
    - `app = Celery("climatepulse_worker")`
    - `app.conf.update(broker_url=f"{settings.redis_url}/0", result_backend=f"{settings.redis_url}/1", task_serializer="json", accept_content=["json"], result_serializer="json", timezone="UTC", enable_utc=True, task_track_started=True, task_acks_late=True, worker_prefetch_multiplier=1, broker_connection_retry_on_startup=True)`
    - `app.conf.task_queues = [Queue("ingest"), Queue("normalize"), Queue("alerts"), Queue("dlq")]` — ING-11 mandates 4 queues
    - `app.conf.task_default_queue = "ingest"`
    - `app.conf.task_routes = {"climatepulse_worker.tasks.ingest.*": {"queue": "ingest"}, "climatepulse_worker.tasks.maintenance.*": {"queue": "ingest"}}`
    - `app.conf.beat_schedule = { "arpa-emilia-15min": {"task": "climatepulse_worker.tasks.ingest.run_source", "schedule": 900.0, "args": ("arpa_emilia",)}, "ecmwf-open-6h": {"task": "climatepulse_worker.tasks.ingest.run_source", "schedule": 21600.0, "args": ("ecmwf_open",)}, "rotate-snapshots-daily": {"task": "climatepulse_worker.tasks.maintenance.rotate_snapshots", "schedule": crontab(hour=3, minute=0)}, "worker-heartbeat-30s": {"task": "climatepulse_worker.tasks.maintenance.heartbeat_tick", "schedule": 30.0} }` — 4 scheduled entries (3 functional + 1 heartbeat per WARNING 3 committed strategy)
    - Auto-discover tasks: `app.autodiscover_tasks(["climatepulse_worker.tasks"])`
    - Connect `celery.signals.worker_ready` to call `set_heartbeat_sync(settings.redis_url)` — the committed startup half of the heartbeat strategy.

    Create `apps/worker/climatepulse_worker/heartbeat.py` — SINGLE committed strategy (no alternatives):
    - `import redis`
    - `HEARTBEAT_KEY = "celery:worker:heartbeat"`
    - `HEARTBEAT_TTL = 120` (seconds — must exceed beat-scheduled refresh interval by ≥4x for resilience to one missed refresh)
    - `HEARTBEAT_REFRESH_INTERVAL = 30` (seconds — Beat-scheduled cadence)
    - `def set_heartbeat_sync(redis_url: str) -> None`: synchronous Redis call using `redis.from_url(redis_url, db=0).setex(HEARTBEAT_KEY, HEARTBEAT_TTL, "ok")`. Used by BOTH the `worker_ready` signal (startup) AND the `tasks.maintenance.heartbeat_tick` Beat task (every 30s).

    The committed heartbeat strategy is exactly TWO components — NO alternatives:
    1. **Startup signal**: connect `celery.signals.worker_ready` in `celery_app.py` (handler calls `set_heartbeat_sync(settings.redis_url)`). Fires once per worker process at startup.
    2. **Periodic Beat task**: define `@shared_task(name="climatepulse_worker.tasks.maintenance.heartbeat_tick") def heartbeat_tick() -> None: set_heartbeat_sync(get_settings().redis_url)` in `tasks/maintenance.py`. Schedule via `app.conf.beat_schedule["worker-heartbeat-30s"] = {"task": "climatepulse_worker.tasks.maintenance.heartbeat_tick", "schedule": 30.0}`.

    REMOVED — DO NOT IMPLEMENT (these alternatives are explicitly forbidden by WARNING 3 resolution):
    - `task_postrun` signal — too coupled to task throughput; quiet periods would let the key expire
    - `worker_heartbeat` signal — broker-internal timing, not a stable refresh source
    - `worker_process_init` per-process variant — Beat task already covers refresh; per-process init adds no benefit

    The Beat task `heartbeat_tick` is scheduled via Beat (singleton, `deploy.replicas: 1`) and runs INSIDE a worker process — when Beat dispatches the task, any healthy worker picks it up and renews the key. Even if 5 workers are running, the key is refreshed at most once per 30s window (no race; SETEX is atomic).

    Create `apps/worker/climatepulse_worker/tasks/ingest.py`:
    - `from celery.utils.log import get_task_logger; from celery import shared_task; import asyncio; from datetime import datetime, timedelta, timezone`
    - Import adapter modules at top of file (so @register fires): `from climatepulse_core.adapters import arpa_emilia, ecmwf_open  # noqa: F401`
    - `logger = get_task_logger(__name__)`
    - `@shared_task(bind=True, name="climatepulse_worker.tasks.ingest.run_source", autoretry_for=(Exception,), retry_kwargs={"max_retries": 3, "countdown": 60}, retry_backoff=True, retry_backoff_max=300, retry_jitter=True)`
    - `def run_source(self, source_id: str) -> int`: open async loop; build asyncpg pool + redis client; build adapter via `get_adapter(source_id)`; build window: from `now - 2*adapter.cadence_seconds` to `now` (overlap window to catch missed runs); call `run_backfill_window(...)` similar to Plan 04's run_backfill but for a single window; return rows_written. Catch any RawObservation with `qc_flag == QcFlag.SCHEMA_VIOLATION` during write — collect them and `dlq_route.apply_async(args=(source_id, obs_dict_list), queue='dlq')` (where `dlq_route` is a placeholder task in this module that simply logs the offender — full DLQ replay CLI is Phase 5).
    - `@shared_task(name="climatepulse_worker.tasks.ingest.dlq_route")` `def dlq_route(source_id, observations) -> None`: structlog.warning("dlq.received", source=source_id, count=len(observations)); persist to a log file or Redis sorted set keyed by date for Phase 5 visibility (implementation: `redis.lpush(f"dlq:{source_id}", json.dumps({"observations":observations, "received_at": now_iso}))` with LTRIM to last 1000).

    Create `apps/worker/climatepulse_worker/tasks/maintenance.py`:
    - `@shared_task(name="climatepulse_worker.tasks.maintenance.rotate_snapshots")`
    - `def rotate_snapshots() -> int`: build SnapshotStore from settings.snapshot_dir; run asyncio.run(store.rotate(max_age_days=30)); structlog.info("snapshots.rotated", count=count); return count. (D-17 retention.)
    - `@shared_task(name="climatepulse_worker.tasks.maintenance.heartbeat_tick")`
    - `def heartbeat_tick() -> None`: per WARNING 3 committed heartbeat strategy, calls `set_heartbeat_sync(get_settings().redis_url)` from `climatepulse_worker.heartbeat`. Scheduled every 30s by Beat; runs inside a worker process. Renews `celery:worker:heartbeat` Redis key so FastAPI `/readyz` (Plan 05 Task 2) sees the worker as alive.

    Create `tests/unit/test_celery_app.py`:
    - `test_four_queues_defined`: `app = import('climatepulse_worker.celery_app').app`; assert `len(app.conf.task_queues) == 4`; assert queue names == {"ingest","normalize","alerts","dlq"}
    - `test_beat_schedule_arpa_emilia_15min`: assert `app.conf.beat_schedule["arpa-emilia-15min"]["schedule"] == 900.0`
    - `test_beat_schedule_ecmwf_open_6h`: assert `app.conf.beat_schedule["ecmwf-open-6h"]["schedule"] == 21600.0`
    - `test_beat_schedule_rotate_snapshots_daily`: assert `app.conf.beat_schedule["rotate-snapshots-daily"]["schedule"]` is a crontab with hour=3 minute=0
    - `test_run_source_task_registered`: assert `"climatepulse_worker.tasks.ingest.run_source" in app.tasks`
    - `test_rotate_snapshots_task_registered`: assert `"climatepulse_worker.tasks.maintenance.rotate_snapshots" in app.tasks`
    - `test_adapters_registered_on_import`: importing `tasks.ingest` triggers @register; `get_adapter("arpa_emilia")` works; `get_adapter("ecmwf_open")` works
    - `test_heartbeat_sets_redis_key(fakeredis)`: monkeypatch redis.from_url to return fakeredis; call set_heartbeat_sync; assert fakeredis has `celery:worker:heartbeat` with TTL > 100 and < 121
    - `test_heartbeat_tick_task_registered`: assert `"climatepulse_worker.tasks.maintenance.heartbeat_tick" in app.tasks` (the periodic refresh half of the committed heartbeat strategy)
    - `test_beat_schedule_has_heartbeat_tick`: assert `app.conf.beat_schedule["worker-heartbeat-30s"]["schedule"] == 30.0`
    - `test_worker_ready_signal_connected`: import celery_app module and assert `set_heartbeat_sync` is registered as a `worker_ready` signal receiver
  </behavior>
  <verify>
    <automated>uv run pytest tests/unit/test_celery_app.py -v -x</automated>
  </verify>
  <done>
    All Celery app tests pass; 4 queues defined per ING-11; beat schedule has 3 entries matching D-17/D-19; run_source and rotate_snapshots tasks are auto-discovered and registered; heartbeat helper sets the Redis key correctly; adapter @register decorators fire on import of tasks.ingest.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FastAPI minimal app — /healthz + /readyz with DB + Redis + Celery worker checks (D-14 + API-10)</name>
  <files>apps/api/climatepulse_api/main.py, apps/api/climatepulse_api/settings.py, apps/api/climatepulse_api/routers/__init__.py, apps/api/climatepulse_api/routers/health.py, tests/unit/test_healthz.py</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 930-974 FastAPI /healthz + /readyz pattern; line 973 heartbeat detection via Redis key)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-14 minimal API; API-10 anticipated to Phase 1; D-13 no Phase 2+ leak)
  </read_first>
  <behavior>
    - GET /healthz returns 200 {"status":"ok"} unconditionally (liveness — process is up)
    - GET /readyz returns 200 {"status":"ready"} when DB.SELECT 1 succeeds AND Redis.ping() succeeds AND Redis.get("celery:worker:heartbeat") returns "ok"
    - GET /readyz returns 503 with body {"detail": {"db":"...", "redis":"...", "celery":"no worker heartbeat"}} listing failing components when ANY check fails
    - No other endpoints registered — no /v1/* surface (D-14)
    - App startup creates asyncpg pool + redis client; shutdown closes both
    - GET / (root) returns 404 (FastAPI default — confirm no auto-docs leak the absent /v1 surface)
  </behavior>
  <action>
    Create `apps/api/climatepulse_api/settings.py`:
    - `class ApiSettings(BaseSettings)` with `database_url: str`, `redis_url: str = "redis://localhost:6379"`, `environment: str = "development"`; `@lru_cache def get_api_settings()`. (Separate from core Settings because api doesn't need snapshot_dir.)

    Create `apps/api/climatepulse_api/routers/health.py`:
    - `from fastapi import APIRouter, Depends, HTTPException; from fastapi.responses import JSONResponse`
    - `router = APIRouter(tags=["health"])`
    - `@router.get("/healthz", status_code=200) async def liveness() -> dict`: return `{"status": "ok"}`. NO dependency injection — must succeed even if DB/Redis down.
    - `@router.get("/readyz", status_code=200) async def readiness(request: Request)`: pull `db_pool` and `redis_client` from `request.app.state`; run checks in parallel via `asyncio.gather(*[...], return_exceptions=True)`:
      - DB check: `async with db_pool.acquire() as conn: await asyncio.wait_for(conn.fetchval("SELECT 1"), timeout=2.0)`
      - Redis check: `await asyncio.wait_for(redis_client.ping(), timeout=2.0)`
      - Celery check: `pong = await redis_client.get("celery:worker:heartbeat"); assert pong == b"ok" or pong == "ok"`
    - Build error dict: `errors = {}` populated when each check raises; if `errors`: raise `HTTPException(status_code=503, detail=errors)`; else return `{"status": "ready"}`

    Create `apps/api/climatepulse_api/main.py`:
    - `from fastapi import FastAPI; from contextlib import asynccontextmanager; import asyncpg; from redis.asyncio import Redis`
    - `@asynccontextmanager async def lifespan(app: FastAPI)`:
      - `settings = get_api_settings()`
      - Build asyncpg pool: `app.state.db_pool = await asyncpg.create_pool(settings.database_url.replace("postgresql+asyncpg://", "postgresql://"), min_size=1, max_size=5)`
      - Build redis client: `app.state.redis_client = Redis.from_url(settings.redis_url, decode_responses=False)`
      - yield
      - Teardown: `await app.state.db_pool.close()`; `await app.state.redis_client.close()`
    - `app = FastAPI(title="Climate Pulse", version="0.1.0", lifespan=lifespan, openapi_url=None, docs_url=None, redoc_url=None)` — IMPORTANT: openapi/docs/redoc URLs are NONE in Phase 1 per D-13 (no Phase 2+ leak; full OpenAPI 3.1 spec is API-09 in Phase 2)
    - `app.include_router(health.router)`
    - Add root handler `@app.get("/") def root(): return {"status": "Climate Pulse minimal API. See https://federicocalo.github.io/climate-pulse"}` — gives a friendly redirect-equivalent without exposing a non-existent /v1 surface

    Create `tests/unit/test_healthz.py` using `fastapi.testclient.TestClient` + `pytest-httpx` (or use the `app.dependency_overrides` mechanism):
    - `test_healthz_returns_200_with_status_ok`: TestClient(app).get("/healthz") → 200 + {"status":"ok"}
    - `test_healthz_works_without_dependencies`: monkeypatch app.state to have None db_pool + None redis_client; /healthz still 200 (liveness, doesn't touch deps)
    - `test_readyz_all_healthy_returns_200`: monkeypatch app.state.db_pool with a stub whose acquire().__aenter__.fetchval returns 1; monkeypatch redis_client with fakeredis where `celery:worker:heartbeat` is set to "ok"; assert 200 + {"status":"ready"}
    - `test_readyz_db_down_returns_503_with_db_error`: stub db pool that raises ConnectionError on fetchval; assert 503 + detail.db is non-empty
    - `test_readyz_no_celery_heartbeat_returns_503`: db + redis OK but heartbeat key missing → 503 + detail.celery == "no worker heartbeat"
    - `test_redoc_and_docs_are_disabled`: TestClient(app).get("/docs") → 404; .get("/redoc") → 404; .get("/openapi.json") → 404. Verifies D-13 no Phase 2 leak.
    - `test_no_v1_routes_registered`: `assert not any(route.path.startswith("/v1") for route in app.routes)` — guards against accidental Phase 2 leak
  </behavior>
  <verify>
    <automated>uv run pytest tests/unit/test_healthz.py -v -x</automated>
  </verify>
  <done>
    All health tests pass; /healthz works without dependencies (true liveness); /readyz checks all 3 (DB + Redis + Celery heartbeat) and returns structured error per failing component; /docs and /redoc and /openapi.json are all 404 (D-13 enforcement); no /v1/* routes leaked.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dockerfiles (API slim, Worker with libeccodes) + compose.yaml with healthchecks + beat singleton (OPS-01 + D-22)</name>
  <files>infra/Dockerfile.api, infra/Dockerfile.worker, infra/compose.yaml, .env.example</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 977-1063 compose.yaml; lines 540-545 Worker Dockerfile libeccodes install; lines 213-214 system dep comment)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-21 cfgrib + libeccodes0; D-22 libeccodes in worker ONLY; D-15 snapshot volume mount; OPS-01 healthchecks)
    - .env.example (Plan 01 — extend with any Compose-specific vars)
  </read_first>
  <action>
    Create `infra/Dockerfile.api`:
    - Base `python:3.12.7-slim-bookworm`
    - WORKDIR `/app`
    - Install uv: `RUN pip install --no-cache-dir uv==0.4.*`
    - Copy `pyproject.toml`, `uv.lock`, `packages/core/`, `apps/api/` (the minimum needed — leverage Docker layer cache: copy lockfiles first, install, then copy source)
    - `RUN uv sync --frozen --package climatepulse-api --no-dev` (only the api package + its deps; explicitly NOT --all-packages — keeps the image slim per D-22)
    - EXPLICIT comment: `# D-22: This image MUST NOT install libeccodes. cfgrib lives in the worker only.`
    - EXPOSE 8000
    - HEALTHCHECK CMD `curl -fsS http://localhost:8000/healthz || exit 1` (with interval 10s, timeout 5s, retries 5, start-period 15s)
    - CMD `["uv", "run", "uvicorn", "climatepulse_api.main:app", "--host", "0.0.0.0", "--port", "8000"]`

    Create `infra/Dockerfile.worker`:
    - Base `python:3.12.7-slim-bookworm`
    - WORKDIR `/app`
    - System deps FIRST (separate Docker layer for caching): `RUN apt-get update && apt-get install -y --no-install-recommends libeccodes0 libeccodes-tools curl && rm -rf /var/lib/apt/lists/*` — D-21 mandates this; curl is for the healthcheck
    - Install uv: `RUN pip install --no-cache-dir uv==0.4.*`
    - Copy `pyproject.toml`, `uv.lock`, `packages/core/`, `packages/migrations/`, `apps/worker/`
    - `RUN uv sync --frozen --package climatepulse-worker --no-dev` — pulls cfgrib + ecmwf-opendata + xarray (per Plan 01 worker pyproject)
    - HEALTHCHECK CMD `celery -A climatepulse_worker.celery_app inspect ping -t 5 || exit 1` (returns 0 if at least one worker responds)
    - Default CMD (overridden by compose for beat): `["uv", "run", "celery", "-A", "climatepulse_worker.celery_app", "worker", "-Q", "ingest,normalize,alerts,dlq", "-c", "4", "--loglevel=INFO"]`

    Create `infra/compose.yaml` per RESEARCH.md lines 980-1060 with these services (verbatim service names per RESEARCH.md, locked):

    ```yaml
    name: climate-pulse

    services:
      timescale:
        image: timescale/timescaledb:2.17.2-pg16   # pinned per Pitfall E
        environment:
          POSTGRES_DB: ${POSTGRES_DB}
          POSTGRES_USER: ${POSTGRES_USER}
          POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        volumes:
          - timescale_data:/var/lib/postgresql/data
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
          interval: 10s
          timeout: 5s
          retries: 5
          start_period: 30s
        # Do NOT expose ports publicly — internal network only

      redis:
        image: redis:7.4-alpine
        command: ["redis-server", "--appendonly", "yes"]
        volumes:
          - redis_data:/data
        healthcheck:
          test: ["CMD", "redis-cli", "ping"]
          interval: 5s
          timeout: 3s
          retries: 5
        # Do NOT expose ports publicly — internal network only

      migrate:
        build:
          context: ..
          dockerfile: infra/Dockerfile.worker   # reuses worker image (has alembic + asyncpg)
        command: ["uv", "run", "alembic", "-c", "packages/migrations/alembic.ini", "upgrade", "head"]
        environment:
          DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@timescale:5432/${POSTGRES_DB}
        depends_on:
          timescale:
            condition: service_healthy
        restart: "no"   # one-shot

      api:
        build:
          context: ..
          dockerfile: infra/Dockerfile.api
        environment:
          DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@timescale:5432/${POSTGRES_DB}
          REDIS_URL: redis://redis:6379
          ENVIRONMENT: production
        ports:
          - "8000:8000"   # ONLY public-exposed service
        depends_on:
          timescale:
            condition: service_healthy
          redis:
            condition: service_healthy
          migrate:
            condition: service_completed_successfully
        restart: unless-stopped

      worker:
        build:
          context: ..
          dockerfile: infra/Dockerfile.worker
        environment:
          DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@timescale:5432/${POSTGRES_DB}
          REDIS_URL: redis://redis:6379
          SNAPSHOT_DIR: /var/lib/climatepulse/snapshots
        volumes:
          - snapshots_data:/var/lib/climatepulse/snapshots   # D-15
        depends_on:
          timescale:
            condition: service_healthy
          redis:
            condition: service_healthy
          migrate:
            condition: service_completed_successfully
        restart: unless-stopped

      beat:
        build:
          context: ..
          dockerfile: infra/Dockerfile.worker   # SAME image as worker
        command: ["uv", "run", "celery", "-A", "climatepulse_worker.celery_app", "beat", "--loglevel=INFO"]
        environment:
          REDIS_URL: redis://redis:6379
          DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@timescale:5432/${POSTGRES_DB}
        depends_on:
          redis:
            condition: service_healthy
          worker:
            condition: service_started   # don't gate on worker healthy — beat can start independently
        restart: unless-stopped
        # CRITICAL: NEVER scale beat > 1. Pitfall #6 (RESEARCH.md lines 1278-1281).
        # If you `docker compose up --scale beat=2`, ARPA gets 2× requests, gridded_observations
        # gets duplicate rows, DLQ fills. The deploy.replicas:1 below enforces this in swarm mode;
        # in compose mode we rely on the comment + README warning (Plan 01 Task 1).
        deploy:
          replicas: 1

    volumes:
      timescale_data:
      redis_data:
      snapshots_data:
    ```

    Extend `.env.example` (already created in Plan 01) — ensure it contains every variable compose.yaml references. If any missing (e.g. SNAPSHOT_DIR was already there), no edit needed. If missing: edit (do NOT recreate from scratch).

    Note about network topology (Claude's Discretion per CONTEXT.md): compose v2 creates a default project network `climate-pulse_default` — single bridge network is simplest for a single-host self-hosted deploy per RESEARCH.md line 1062. No explicit `networks:` section needed.
  </action>
  <verify>
    <automated>docker compose -f infra/compose.yaml config 2>&amp;1 | tee /tmp/compose_config.txt &amp;&amp; grep -q "replicas: 1" /tmp/compose_config.txt &amp;&amp; grep -q "service_healthy" /tmp/compose_config.txt &amp;&amp; grep -q "service_completed_successfully" /tmp/compose_config.txt &amp;&amp; grep -q "snapshots_data:/var/lib/climatepulse/snapshots" /tmp/compose_config.txt &amp;&amp; ! grep -q "libeccodes" infra/Dockerfile.api</automated>
  </verify>
  <done>
    `docker compose config` validates the compose file syntax; beat service has `deploy.replicas: 1` (Pitfall #6); api depends_on includes `migrate: service_completed_successfully` so the schema is in place before api opens connections; snapshots_data volume mounts on worker only at the D-15 path; Dockerfile.api does NOT install libeccodes (D-22 verified by grep absent).
  </done>
</task>

<task type="auto">
  <name>Task 4 (MUST execute BEFORE Task 5 per WARNING 2): Update REQUIREMENTS.md (move API-10 Phase 2 → Phase 1) + ROADMAP.md per D-14</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current Traceability table, look at API-10 row + per-phase distribution table)
    - .planning/ROADMAP.md (current Phase 1 Requirements line + Phase 2 Requirements line)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-14 exact mandate — final paragraph)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`:
    1. In the Traceability table, change the row `| API-10 | Phase 2 | Pending |` to `| API-10 | Phase 1 | Pending |`
    2. Update the "Per-phase distribution" table at the bottom:
       - Phase 1 row: change count from `24` to `25`; change categories from `ING (10) + STO (6) + OPS (4) + DOC (4)` to `ING (10) + STO (6) + API (1) + OPS (4) + DOC (4)`
       - Phase 2 row: change count from `15` to `14`; change categories from `API (12) + OPS (2) + DOC (1)` to `API (11) + OPS (2) + DOC (1)`
       - Total row: stays at 67
    3. Add a one-line note at the end of the section: `*Updated 2026-05-23 per Phase 1 CONTEXT D-14: API-10 (/healthz + /readyz) anticipated to Phase 1 to enable Walking Skeleton acceptance demo.*`

    Edit `.planning/ROADMAP.md`:
    1. In the "Phase 1: Foundation Slice + Public Docs" section, append `, API-10` to the end of the `**Requirements:**` line so it reads: `**Requirements:** ING-01, ING-02, ING-03, ING-04, ING-09, ING-10, ING-11, ING-12, ING-13, ING-14, STO-01, STO-02, STO-03, STO-04, STO-05, STO-06, OPS-01, OPS-07, OPS-08, OPS-09, DOC-01, DOC-02, DOC-03, DOC-05, API-10`
    2. In the "Phase 2: Public REST API" section, remove `API-10, ` from the `**Requirements:**` line (the leading comma OR trailing comma — preserve list comma cleanliness)
    3. Add a new bullet under Phase 1 Success Criteria (after current #5): `6. The /healthz endpoint returns 200 with {"status":"ok"} on container start and /readyz returns 200 with {"status":"ready"} once TimescaleDB, Redis and Celery worker are all healthy (D-14 anticipation of API-10).`

    Do NOT touch any other section (don't reflow other tables, don't change the per-phase distribution narrative — leave it minimally edited).

    After both edits: `git diff .planning/REQUIREMENTS.md .planning/ROADMAP.md` should show ONLY the targeted changes (no whitespace churn).
  </action>
  <verify>
    <automated>grep -E "^\| API-10 \| Phase 1 \|" .planning/REQUIREMENTS.md &amp;&amp; ! grep -E "^\| API-10 \| Phase 2 \|" .planning/REQUIREMENTS.md &amp;&amp; grep "API-10" .planning/ROADMAP.md | grep -q "Foundation"</automated>
  </verify>
  <done>
    REQUIREMENTS.md Traceability shows API-10 mapped to Phase 1; Per-phase distribution shows Phase 1=25, Phase 2=14, total 67; ROADMAP.md Phase 1 Requirements line ends with `, API-10` and Phase 2 Requirements no longer lists API-10; Success Criteria #6 added under Phase 1.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Walking Skeleton stack: Compose v2 services (timescale + redis + migrate + api + worker + beat) + FastAPI minimal app exposing /healthz + /readyz + Celery worker with 4 queues + dedicated singleton Beat with the D-19 + D-17 schedule + adapter ingestion of ARPAE realtime feed + ECMWF Open Data + idempotent backfill CLI + two TimescaleDB hypertables receiving real data + MkDocs docs site live with verbatim footer (Plan 01) + CI gates (Plan 01) + REQUIREMENTS/ROADMAP updates per D-14.
  </what-built>
  <how-to-verify>
    This is the D-9 acceptance demo. Execute these steps on a fresh checkout (clean clone, no .env yet):
    1. `git clone https://github.com/federicocalo/climate-pulse && cd climate-pulse && cp .env.example .env`
    2. `docker compose -f infra/compose.yaml up -d --build`
    3. Wait until `docker compose ps` shows all five long-running services (timescale, redis, api, worker, beat) as `Up (healthy)`. `migrate` should be `Exited (0)`. Expected total time: under 5 minutes including image build (~10 min budget per D-9).
    4. `curl -sS http://localhost:8000/healthz` → returns `{"status":"ok"}` immediately
    5. `curl -sS http://localhost:8000/readyz` → returns 200 with `{"status":"ready"}` (if still 503 after 60s, run `docker compose logs worker | tail -50` to see why heartbeat isn't being set)
    6. Wait at least one ARPAE cadence cycle (15 min — or trigger immediately with: `docker compose exec worker uv run python -c "from climatepulse_worker.tasks.ingest import run_source; run_source.apply(args=('arpa_emilia',))"`)
    7. `docker compose exec timescale psql -U climatepulse -d climatepulse -c "SELECT count(*), source_id FROM observations GROUP BY source_id"` → expect at least one row with source_id mapping to arpa_emilia and a non-zero count
    8. Run the backfill: `docker compose exec worker uv run climatepulse backfill arpae --from $(date -d '7 days ago' +%F) --to $(date +%F)` → should complete without error and print per-day rows persisted
    9. Note the count from step 7 (e.g. N rows). Re-run step 7 — count should equal or exceed N (no decrease)
    10. Re-run step 8 — idempotency check: re-run step 7 immediately after; count MUST NOT change beyond what step 8 wrote in this second run if no new realtime data arrived. The KEY assertion: rerunning step 8 alone with the same dates produces zero NEW rows.
    11. ECMWF check: `docker compose exec timescale psql -U climatepulse -d climatepulse -c "SELECT count(*) FROM gridded_observations"` — ECMWF runs every 6 hours; after one cycle (or trigger manually with `run_source.apply(args=('ecmwf_open',))`) the count should be non-zero
    12. Verify Plan 01 docs are still live: visit https://federicocalo.github.io/climate-pulse and confirm the footer is intact (carryover from Plan 01)
    13. Verify D-22: `docker image inspect climate-pulse-api | grep -i eccodes` returns empty (api image does NOT include libeccodes); `docker image inspect climate-pulse-worker | grep -i eccodes` returns paths (worker image DOES)
  </how-to-verify>
  <resume-signal>Type "approved" once all 13 steps pass on a fresh clone. If any step fails, describe which step + the diagnostic output, and rollback before re-running the plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public network (port 8000) → FastAPI | Only public surface in Phase 1; D-14 limits attack surface to 2 endpoints |
| Compose service `api` → TimescaleDB | Trusted internal network; password in env var |
| Beat → Worker via Redis broker | Trusted internal; broker not exposed |
| Worker libeccodes0 → cfgrib decoder | Native binary parsing of external GRIB; treated as untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | /healthz could be hit by anyone (no auth) | accept | Liveness endpoint MUST be unauthenticated for load balancers; returns only `{"status":"ok"}` — no info disclosure |
| T-05-02 | Information Disclosure | /readyz error body lists "db", "redis", "celery" | accept | These three component names are public-knowledge architectural choices already documented in PROJECT.md; revealing which one is down does not aid an attacker |
| T-05-03 | Tampering | docker compose --scale beat=2 by operator | mitigate | `deploy.replicas: 1` enforces in swarm; inline comment in compose.yaml + README warning (Plan 01) for compose mode (Pitfall #6) |
| T-05-04 | Elevation of Privilege | Redis broker exposed on host network | mitigate | redis service has NO `ports:` mapping — internal bridge network only. Verified by `docker compose port redis 6379` returning empty |
| T-05-05 | Information Disclosure | TimescaleDB exposed on host | mitigate | timescale service has NO `ports:` mapping. Self-hoster uses `docker compose exec timescale psql` (the documented quickstart) |
| T-05-06 | Denial of Service | Public /readyz hammered to probe DB latency | accept | Phase 1 has no rate limit (Phase 2 API-08); /readyz checks have 2s timeout each so a probing attacker only consumes 6s/req. Acceptable for Phase 1 acceptance demo |
| T-05-07 | Tampering | OpenAPI /docs leaking absent /v1 routes | mitigate | `openapi_url=None, docs_url=None, redoc_url=None` in FastAPI(); test_redoc_and_docs_are_disabled asserts this (D-13 enforcement) |
| T-05-08 | Information Disclosure | Compose secrets (POSTGRES_PASSWORD) in env | mitigate | `.env` is gitignored (Plan 01); only `.env.example` with `changeme` placeholders ships. gitleaks pre-commit (Plan 01) blocks accidental leaks |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No NEW packages installed in Plan 05 beyond Plans 01/03/04; FastAPI/uvicorn/redis/celery all `[OK]` from RESEARCH.md Package Legitimacy Audit; Docker base image `python:3.12.7-slim-bookworm` is official PSF-maintained |
</threat_model>

<verification>
- `pytest tests/unit/test_celery_app.py` green: 4 queues, 3 beat schedule entries, run_source + rotate_snapshots tasks registered (Task 1)
- `pytest tests/unit/test_healthz.py` green: /healthz unconditional 200, /readyz checks all 3 deps, /docs + /redoc + /openapi.json all 404 (D-13) (Task 2)
- `docker compose -f infra/compose.yaml config` validates syntactically (Task 3)
- `infra/Dockerfile.api` does NOT contain `libeccodes` (D-22 verified — Task 3)
- `infra/Dockerfile.worker` DOES install `libeccodes0 libeccodes-tools` (D-21 — Task 3)
- compose.yaml beat service has `deploy.replicas: 1` and inline comment about Pitfall #6 (Task 3)
- REQUIREMENTS.md Traceability shows API-10 → Phase 1 (Task 4)
- ROADMAP.md Phase 1 Requirements includes API-10; Phase 2 no longer does (Task 4)
- D-9 acceptance demo passes on fresh clone (Task 5 checkpoint)
</verification>

<success_criteria>
- ROADMAP Phase 1 Success Criteria #1 satisfied: "A self-hoster can run `docker compose up` on a fresh machine and within 10 minutes have TimescaleDB ingesting real observations from ARPA Emilia-Romagna and ECMWF Open Data" — verified by Task 5 checkpoint
- ROADMAP Phase 1 Success Criteria #2 satisfied: "A developer can run `climatepulse backfill arpae --from … --to …` and verify idempotent rows" — Plan 04 + Task 5 step 10
- ROADMAP Phase 1 Success Criteria #4 satisfied: "A reader of the docs can follow the quickstart page to bring up the stack and run their first psql query against the hypertable" — Plan 01 docs/quickstart.md + Task 5 acceptance
- CONTEXT.md D-14 (API-10 anticipated to Phase 1) satisfied: /healthz + /readyz behave correctly per the D-14 SCOPE ADJUSTMENT mandate. ROADMAP.md Phase 1 Success Criteria #6 is ADDED by Task 4 of this plan to reflect the D-14 anticipation (i.e. Task 4 must execute BEFORE Task 5 acceptance demo so that the ROADMAP criterion list is accurate when the human checkpoint verifies it).
- ING-11 (Celery worker + 4 queues + dedicated Beat + DLQ), OPS-01 (Compose with healthchecks), API-10 (/healthz + /readyz) — fully shipped
- D-9, D-14, D-17, D-19, D-21, D-22 all encoded and verified
- Walking Skeleton: the entire architectural spine is proven end-to-end
</success_criteria>

<output>
Create `.planning/phases/01-foundation-slice-public-docs/01-05-SUMMARY.md` when done. Capture: actual time-to-healthy from `docker compose up` (must be < 10 min per D-9); per-source row counts after first acceptance cycle (ARPAE + ECMWF); the image size of Dockerfile.api vs Dockerfile.worker (proves D-22 slim split); the exact REQUIREMENTS.md diff for the API-10 migration; any deviation from the planned compose.yaml (e.g. testcontainer image tag adjustments noted from Plan 02 SUMMARY).
</output>

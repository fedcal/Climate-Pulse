---
phase: 01-foundation-slice-public-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/migrations/alembic.ini
  - packages/migrations/climatepulse_migrations/env.py
  - packages/migrations/climatepulse_migrations/versions/0001_init.py
  - packages/core/climatepulse_core/storage/__init__.py
  - packages/core/climatepulse_core/storage/models.py
  - packages/core/climatepulse_core/domain/__init__.py
  - packages/core/climatepulse_core/domain/models.py
  - tests/__init__.py
  - tests/integration/__init__.py
  - tests/integration/conftest.py
  - tests/integration/test_hypertable.py
  - tests/integration/test_idempotency.py
  - tests/integration/test_cagg.py
autonomous: true
requirements: [STO-01, STO-02, STO-03, STO-04, STO-05, STO-06]

must_haves:
  truths:
    - "Running Alembic migrations against a fresh TimescaleDB instance creates BOTH hypertables (observations + gridded_observations) with correct PKs and chunk intervals"
    - "Inserting the same (station_id, variable_id, observed_at, source_id) tuple twice produces ONE row, not two (idempotent ON CONFLICT)"
    - "After insertion + manual CAGG refresh, obs_hourly returns aggregated rows; same for gridded_hourly on gridded_observations"
    - "Hierarchical CAGG obs_daily is bucketed on obs_hourly (NOT on raw) per STO-03 and aggregates correctly after refresh"
    - "Hierarchical CAGG gridded_daily is bucketed on gridded_hourly (NOT on raw) for symmetry and aggregates correctly after refresh"
    - "Compression policy is registered with segmentby='station_id, variable_id' on observations and 'lat_idx, lon_idx, variable_id' on gridded_observations"
    - "Retention policies are registered (5y raw observations, 90d raw gridded_observations, 20y for both daily aggregates per STO-05 default) and follow ordering rule refresh_lag < compress_after < retention_after"
    - "Alembic include_name callback excludes _timescaledb_* internal tables and Timescale-auto-created indexes (Pitfall D)"
  artifacts:
    - path: "packages/migrations/climatepulse_migrations/versions/0001_init.py"
      provides: "Single initial migration that creates sources/variables/stations tables + BOTH hypertables + 4 CAGGs (obs_hourly, obs_daily, gridded_hourly, gridded_daily) + compression + retention policies (raw + 20y aggregates)"
      min_lines: 200
      contains: "create_hypertable"
    - path: "packages/migrations/climatepulse_migrations/env.py"
      provides: "Alembic env with include_name excluding _timescaledb_* per Pitfall D"
      contains: "_timescaledb"
    - path: "tests/integration/conftest.py"
      provides: "testcontainers TimescaleDB session fixture per D-25 (DockerContainer not PostgresContainer)"
      contains: "DockerContainer"
    - path: "tests/integration/test_hypertable.py"
      provides: "Test that BOTH hypertables exist with correct chunk_time_interval and PKs"
      contains: "gridded_observations"
    - path: "tests/integration/test_idempotency.py"
      provides: "Test that re-inserting the same (station_id, variable_id, observed_at, source_id) is a no-op"
      contains: "ON CONFLICT"
    - path: "tests/integration/test_cagg.py"
      provides: "Test obs_hourly, obs_daily (hierarchical), gridded_hourly, and gridded_daily (hierarchical) aggregate after refresh"
      contains: "refresh_continuous_aggregate"
    - path: "packages/core/climatepulse_core/domain/models.py"
      provides: "Domain dataclasses RawObservation, Observation, Station, Variable, Source, QcFlag enum"
      contains: "class RawObservation"
  key_links:
    - from: "packages/migrations/climatepulse_migrations/versions/0001_init.py"
      to: "observations hypertable"
      via: "op.execute create_hypertable"
      pattern: "create_hypertable\\(\\s*'observations'"
    - from: "packages/migrations/climatepulse_migrations/versions/0001_init.py"
      to: "gridded_observations hypertable"
      via: "op.execute create_hypertable"
      pattern: "create_hypertable\\(\\s*'gridded_observations'"
    - from: "packages/migrations/climatepulse_migrations/env.py"
      to: "Alembic autogen exclusion"
      via: "include_name callback"
      pattern: "def include_name"
---

<objective>
Build the entire storage layer: Alembic migrations that create the metadata tables + BOTH TimescaleDB hypertables (per D-23 deviation) + CAGGs + compression + retention policies, plus integration tests via testcontainers proving the schema works against a real TimescaleDB 2.17 instance.

Purpose: D-23 doubles ops complexity by mandating two hypertables (station-based `observations` + grid-based `gridded_observations`). This plan locks in that decision in DDL and verifies via real-DB integration tests that the entire policy chain (CAGG refresh → compression → retention) ordering is correct (Pitfall #3 from research/PITFALLS.md). It also seeds the domain models that Plan 03's adapter/normalizer/writer will use.

Output: One Alembic migration `0001_init.py` containing both hypertables + 2 CAGGs + 2 compression policies + 2 retention policies + `include_name` exclusion; domain dataclasses; testcontainers conftest; 3 integration test files. Plan 03 will import from `climatepulse_core.domain.models` and Plan 05 will run this migration in compose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md
@.planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md
@.planning/phases/01-foundation-slice-public-docs/SKELETON.md

<interfaces>
<!-- Contracts this plan creates that Plan 03 + Plan 04 + Plan 05 will consume -->

From packages/core/climatepulse_core/domain/models.py (created here):
```python
from dataclasses import dataclass
from datetime import datetime
from enum import IntEnum

class QcFlag(IntEnum):
    GOOD = 0
    MISSING = 1
    OUT_OF_RANGE = 2
    SCHEMA_VIOLATION = 3   # D-20 schema-drift handling
    STALE_SNAPSHOT = 4

@dataclass(frozen=True)
class Source:
    source_id: str          # 'arpa_emilia', 'ecmwf_open'
    name: str
    license: str
    attribution: str
    terms_url: str | None
    enabled: bool = True

@dataclass(frozen=True)
class Variable:
    wmo_code: str           # 'air_temperature', 'wind_speed'
    bufr_code: str | None   # 'B12101' for ARPAE; None for ECMWF
    unit_si: str            # 'K', 'm s-1', 'Pa'
    description: str | None = None

@dataclass(frozen=True)
class Station:
    source_id: str
    external_id: str
    name: str
    lat: float
    lon: float
    elevation_m: float | None = None
    metadata: dict | None = None

@dataclass(frozen=True)
class RawObservation:
    """Pre-normalization shape produced by adapters."""
    source_id: str
    observed_at: datetime   # MUST be tz-aware UTC
    station_external_id: str | None    # populated for station-based
    wmo_code: str
    value: float
    qc_flag: QcFlag = QcFlag.GOOD
    # Grid-based fields (populated when is_grid_based=True)
    lat: float | None = None
    lon: float | None = None
    lat_idx: int | None = None
    lon_idx: int | None = None
    init_time: datetime | None = None
    step_h: int | None = None
```

From packages/core/climatepulse_core/storage/models.py (created here):
```python
# SQLAlchemy 2.0 async declarative models for sources/variables/stations metadata.
# observations and gridded_observations are NOT modeled in ORM (hot path uses asyncpg COPY).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain dataclasses + SQLAlchemy metadata models</name>
  <files>packages/core/climatepulse_core/domain/__init__.py, packages/core/climatepulse_core/domain/models.py, packages/core/climatepulse_core/storage/__init__.py, packages/core/climatepulse_core/storage/models.py</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 552-596 observations DDL; lines 641-697 gridded_observations DDL; lines 786-836 ABC contract with RawObservation hint)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-20 QcFlag SCHEMA_VIOLATION; D-23 two hypertables)
  </read_first>
  <action>
    Create `packages/core/climatepulse_core/domain/models.py` with frozen dataclasses + `QcFlag(IntEnum)` exactly as shown in this plan's `<interfaces>` block. `QcFlag` MUST have these members with these integer values: `GOOD=0, MISSING=1, OUT_OF_RANGE=2, SCHEMA_VIOLATION=3, STALE_SNAPSHOT=4` (D-20 names `schema_violation`; D-15-17 names `stale_snapshot`). `RawObservation.observed_at` MUST be tz-aware (assertion in `__post_init__` not allowed on frozen — instead, document in docstring "MUST be tz-aware UTC; constructor does not enforce — adapter is responsible"). Add `Source.source_id` field type `str` (the human-readable id like `arpa_emilia`, not the surrogate SMALLSERIAL — that mapping happens via a `sources` cache loaded by the Writer).

    Create `packages/core/climatepulse_core/storage/models.py` with SQLAlchemy 2.0 async declarative models for ONLY the metadata tables that Alembic autogen will manage:
    - `Base(DeclarativeBase)` (typing-rich)
    - `class SourceRow(Base)`: `__tablename__ = "sources"`, fields per RESEARCH.md DDL lines 557-565 (id SmallInteger PK identity, source_id String unique, name, license, attribution, terms_url nullable, enabled Boolean default True)
    - `class VariableRow(Base)`: `__tablename__ = "variables"`, fields per RESEARCH.md lines 567-573 (id SmallInteger PK identity, wmo_code unique, bufr_code nullable, unit_si, description nullable)
    - `class StationRow(Base)`: `__tablename__ = "stations"`, fields per RESEARCH.md lines 575-585 (id BigInteger PK identity, source_id FK to sources.id SmallInteger, external_id, name, lat Double, lon Double, elevation_m Real nullable, metadata JSONB default `{}`; UniqueConstraint(source_id, external_id))
    - DO NOT create ORM models for `observations` or `gridded_observations` — those are raw-SQL hypertables managed by Alembic op.execute() in Task 2 and written via asyncpg COPY in Plan 03

    Add `__init__.py` exports so `from climatepulse_core.domain import RawObservation, Observation, Station, Variable, Source, QcFlag` works and `from climatepulse_core.storage import Base, SourceRow, VariableRow, StationRow` works.

    No Pint imports in domain (Pint lives in `normalize/wmo.py` — Plan 03). Keep this module dependency-light so it can be imported by both worker and api containers.
  </action>
  <verify>
    <automated>uv run python -c "from climatepulse_core.domain import RawObservation, Observation, Station, Variable, Source, QcFlag; from climatepulse_core.storage import Base, SourceRow, VariableRow, StationRow; assert QcFlag.SCHEMA_VIOLATION == 3; assert QcFlag.STALE_SNAPSHOT == 4; ro = RawObservation(source_id='arpa_emilia', observed_at=__import__('datetime').datetime.now(__import__('datetime').timezone.utc), station_external_id='abc', wmo_code='air_temperature', value=285.6); assert ro.qc_flag == QcFlag.GOOD"</automated>
  </verify>
  <done>
    Both `from climatepulse_core.domain import ...` and `from climatepulse_core.storage import ...` succeed; `QcFlag` integer values match D-20 (schema_violation=3) and D-15-17 (stale_snapshot=4); ORM models exist ONLY for sources/variables/stations — no Observation ORM class.
  </done>
</task>

<task type="auto">
  <name>Task 2: Alembic env + initial migration creating BOTH hypertables + CAGGs + policies (Pitfall D + Policy Ordering)</name>
  <files>packages/migrations/alembic.ini, packages/migrations/climatepulse_migrations/env.py, packages/migrations/climatepulse_migrations/versions/0001_init.py</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 554-636 observations DDL; lines 642-696 gridded_observations DDL; lines 705-780 Alembic include_name pattern; lines 1267-1271 Pitfall D)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-23 two hypertables; STO-04 segmentby; STO-05 retention 5y raw / 20y aggregates)
  </read_first>
  <action>
    Create `packages/migrations/alembic.ini` with standard layout: `script_location = climatepulse_migrations`, `sqlalchemy.url = ` (empty — env.py reads from DATABASE_URL env var), `file_template = %%(year)d_%%(month).2d_%%(day).2d_%%(hour).2d%%(minute).2d-%%(rev)s_%%(slug)s`, `prepend_sys_path = .`, `version_path_separator = os`.

    Create `packages/migrations/climatepulse_migrations/env.py` for ASYNC SQLAlchemy + asyncpg + TimescaleDB awareness:
    - Read `DATABASE_URL` from env (`os.environ["DATABASE_URL"]`); default to `postgresql+asyncpg://climatepulse:changeme@localhost:5432/climatepulse` for dev; if env var not set, raise loud
    - Import `Base` from `climatepulse_core.storage.models`; set `target_metadata = Base.metadata`
    - Define `include_name(name, type_, parent_names) -> bool` callback EXACTLY per RESEARCH.md lines 757-772:
      - For schema: allow None and "public"
      - For table: exclude names starting with `_timescaledb`
      - For index: exclude `{"observations_observed_at_idx", "gridded_observations_valid_at_idx"}` (Timescale auto-creates these on `create_hypertable`)
      - Default True
    - Use `create_async_engine(DATABASE_URL)` + `run_async` pattern for `run_migrations_online()`; configure with `target_metadata`, `include_name=include_name`, `include_schemas=True`, `compare_type=True`
    - Note in docstring at top of file: "Run migrations via `alembic -c packages/migrations/alembic.ini upgrade head`"

    Create `packages/migrations/climatepulse_migrations/versions/0001_init.py` with `revision = "0001_init"`, `down_revision = None`. The `upgrade()` function MUST execute in this exact order (sequence matters per Pitfall #3 — refresh_lag < compress_after < retention):

    1. `op.execute("CREATE EXTENSION IF NOT EXISTS timescaledb")` first
    2. `op.create_table("sources", ...)` with columns matching `SourceRow` (id SmallInteger PK identity, source_id String unique not null, name not null, license not null, attribution not null, terms_url nullable, enabled Boolean default true not null)
    3. `op.create_table("variables", ...)` matching `VariableRow`
    4. `op.create_table("stations", ...)` matching `StationRow` (BigInteger PK; FK to sources(id); UniqueConstraint(source_id, external_id))
    5. `op.create_table("observations", ...)` with columns per RESEARCH.md lines 587-596 — observed_at TIMESTAMPTZ NOT NULL, station_id BigInteger FK stations(id), variable_id SmallInteger FK variables(id), source_id SmallInteger FK sources(id), value DoublePrecision NOT NULL, qc_flag SmallInteger NOT NULL default 0, ingested_at TIMESTAMPTZ default now() — PRIMARY KEY (station_id, variable_id, observed_at, source_id) — STO-01 mandates time column in PK (Pitfall #2)
    6. `op.create_table("gridded_observations", ...)` per RESEARCH.md lines 642-656 — valid_at TIMESTAMPTZ, init_time TIMESTAMPTZ, step_h SmallInteger, lat_idx SmallInteger, lon_idx SmallInteger, lat Real, lon Real, variable_id SmallInteger FK, source_id SmallInteger FK, value DoublePrecision, qc_flag SmallInteger default 0, ingested_at TIMESTAMPTZ default now() — PRIMARY KEY (lat_idx, lon_idx, variable_id, valid_at, source_id)
    7. `op.execute("SELECT create_hypertable('observations', 'observed_at', chunk_time_interval => INTERVAL '7 days', if_not_exists => TRUE)")` — STO-01
    8. `op.execute("SELECT create_hypertable('gridded_observations', 'valid_at', chunk_time_interval => INTERVAL '1 day', if_not_exists => TRUE)")` — RESEARCH.md line 536 rationale (higher cardinality)
    9. Indexes via `op.execute("CREATE INDEX obs_var_time ON observations (variable_id, observed_at DESC)")`, `obs_station_time`, `gridded_var_time`, `gridded_coord_time`
    10. Compression settings + policy on observations: `ALTER TABLE observations SET (timescaledb.compress, timescaledb.compress_segmentby = 'station_id, variable_id', timescaledb.compress_orderby = 'observed_at DESC, source_id')`; then `SELECT add_compression_policy('observations', INTERVAL '7 days')` — STO-04
    11. Compression settings + policy on gridded_observations: segmentby `'lat_idx, lon_idx, variable_id'`, orderby `'valid_at DESC, source_id'`; `add_compression_policy('gridded_observations', INTERVAL '7 days')`
    12. Retention: `SELECT add_retention_policy('observations', INTERVAL '5 years')` — STO-05 default 5y raw
    13. Retention: `SELECT add_retention_policy('gridded_observations', INTERVAL '90 days')` — RESEARCH.md line 674 (ECMWF data is large)
    14. CAGG `obs_hourly` per RESEARCH.md lines 618-635 (bucket 1 hour on RAW observations, group by station/variable/source/bucket, WITH NO DATA, then `add_continuous_aggregate_policy('obs_hourly', start_offset => '3 days', end_offset => '1 hour', schedule_interval => '15 minutes')`) — STO-03 raw→hourly tier
    15. HIERARCHICAL CAGG `obs_daily` bucketed on `obs_hourly` (NOT on raw — STO-03 mandates hierarchical): `CREATE MATERIALIZED VIEW obs_daily WITH (timescaledb.continuous) AS SELECT time_bucket(INTERVAL '1 day', bucket) AS day_bucket, station_id, variable_id, source_id, avg(avg_value) AS avg_value, min(min_value) AS min_value, max(max_value) AS max_value, sum(sample_count) AS sample_count FROM obs_hourly GROUP BY 1, 2, 3, 4 WITH NO DATA`; then `add_continuous_aggregate_policy('obs_daily', start_offset => '7 days', end_offset => '1 day', schedule_interval => '1 hour')`. Add retention `add_retention_policy('obs_daily', INTERVAL '20 years')` per STO-05 default 20y for aggregates.
    16. CAGG `gridded_hourly` per RESEARCH.md lines 678-695 (bucket 6 hours on RAW gridded_observations, matching ECMWF cycle cadence; policy start_offset 3 days, end_offset 6 hours, schedule_interval 1 hour) — STO-03 raw→hourly tier for grid
    17. HIERARCHICAL CAGG `gridded_daily` bucketed on `gridded_hourly` (symmetric to obs_daily): `CREATE MATERIALIZED VIEW gridded_daily WITH (timescaledb.continuous) AS SELECT time_bucket(INTERVAL '1 day', bucket) AS day_bucket, lat_idx, lon_idx, variable_id, source_id, avg(avg_value) AS avg_value, min(min_value) AS min_value, max(max_value) AS max_value, sum(sample_count) AS sample_count FROM gridded_hourly GROUP BY 1, 2, 3, 4, 5 WITH NO DATA`; then `add_continuous_aggregate_policy('gridded_daily', start_offset => '7 days', end_offset => '1 day', schedule_interval => '1 hour')`. Add retention `add_retention_policy('gridded_daily', INTERVAL '20 years')` per STO-05.

    Policy ordering verification (Pitfall #3): observations raw end_offset 1h < compress 7d < retention 5y ✓; obs_daily end_offset 1d < retention 20y ✓; gridded raw end_offset 6h < compress 7d < retention 90d ✓; gridded_daily end_offset 1d < retention 20y ✓. Add a comment in the file noting this verification for all FOUR CAGGs.

    Note (phase boundary comment in migration): STO-03 mandates hierarchical CAGGs in Phase 1. Same applies symmetrically to `gridded_daily` per D-23 dual-hypertable parity. Do NOT defer either daily CAGG to Phase 2.

    `downgrade()` reverses in opposite order: DROP MATERIALIZED VIEW gridded_daily CASCADE, gridded_hourly CASCADE, obs_daily CASCADE, obs_hourly CASCADE (children before parents); drop_table gridded_observations, observations, stations, variables, sources (PostgreSQL CASCADE handles policy cleanup automatically when hypertable is dropped).

    Add a 1-line module docstring at top: "Initial schema: metadata + two hypertables (D-23) + CAGGs + compression + retention. Refresh < compress < retention verified."
  </action>
  <verify>
    <automated>uv run python -c "import importlib.util; spec = importlib.util.spec_from_file_location('m', 'packages/migrations/climatepulse_migrations/versions/0001_init.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m.revision == '0001_init'; assert m.down_revision is None; assert callable(m.upgrade) and callable(m.downgrade)" &amp;&amp; grep -c "create_hypertable" packages/migrations/climatepulse_migrations/versions/0001_init.py | grep -qE '^[2-9]' &amp;&amp; grep -q "gridded_observations" packages/migrations/climatepulse_migrations/versions/0001_init.py &amp;&amp; grep -q "_timescaledb" packages/migrations/climatepulse_migrations/env.py &amp;&amp; grep -q "add_compression_policy" packages/migrations/climatepulse_migrations/versions/0001_init.py &amp;&amp; grep -q "add_retention_policy" packages/migrations/climatepulse_migrations/versions/0001_init.py &amp;&amp; grep -q "obs_daily" packages/migrations/climatepulse_migrations/versions/0001_init.py &amp;&amp; grep -q "gridded_daily" packages/migrations/climatepulse_migrations/versions/0001_init.py</automated>
  </verify>
  <done>
    `0001_init.py` calls `create_hypertable` for BOTH `observations` AND `gridded_observations`; both have a compression policy + retention policy; FOUR CAGGs exist — `obs_hourly` and `gridded_hourly` (bucketed on raw) plus hierarchical `obs_daily` (bucketed on obs_hourly) and `gridded_daily` (bucketed on gridded_hourly) per STO-03; both daily CAGGs have 20y retention per STO-05; `env.py` `include_name` callback uses prefix matching for `_timescaledb*`, `_hyper_*` and the explicit set per RESEARCH Q5 resolution. The migration imports without errors at module load.
  </done>
</task>

<task type="auto">
  <name>Task 3: testcontainers TimescaleDB fixture + 3 integration tests (hypertable, idempotency, CAGG)</name>
  <files>tests/__init__.py, tests/integration/__init__.py, tests/integration/conftest.py, tests/integration/test_hypertable.py, tests/integration/test_idempotency.py, tests/integration/test_cagg.py</files>
  <read_first>
    - .planning/phases/01-foundation-slice-public-docs/01-RESEARCH.md (lines 1155-1198 testcontainers TimescaleDB pattern; lines 1273-1276 Pitfall E pinned image)
    - .planning/phases/01-foundation-slice-public-docs/01-CONTEXT.md (D-25 testcontainers MANDATORY in CI)
  </read_first>
  <action>
    Create empty `tests/__init__.py` and `tests/integration/__init__.py`.

    Create `tests/integration/conftest.py` exporting these pytest fixtures:
    - `@pytest.fixture(scope="session") def timescale_container()` using `DockerContainer("timescale/timescaledb:2.17.2-pg16")` (PIN — Pitfall E forbids `latest-pg16` in CI; pick a current stable tag; if `2.17.2-pg16` doesn't exist at execution time the executor should adjust to the closest available 2.17.x tag and note this in SUMMARY.md) — NOT `PostgresContainer` (Pitfall: PostgresContainer doesn't know Timescale extension per RESEARCH.md line 1197). Set env `POSTGRES_DB=test_climatepulse`, `POSTGRES_USER=test`, `POSTGRES_PASSWORD=test`, exposed port 5432, wait via `wait_for_logs(container, "database system is ready to accept connections", timeout=60)`. Yield the container.
    - `@pytest.fixture(scope="session") async def db_dsn(timescale_container)` returning the `postgresql://test:test@{host}:{port}/test_climatepulse` connection string.
    - `@pytest.fixture(scope="session") async def db_pool(db_dsn)` calling `asyncpg.create_pool(db_dsn)`, executing `CREATE EXTENSION IF NOT EXISTS timescaledb` on a connection, then running Alembic upgrade head via `subprocess.run(["uv", "run", "alembic", "-c", "packages/migrations/alembic.ini", "upgrade", "head"], env={**os.environ, "DATABASE_URL": f"postgresql+asyncpg://test:test@{host}:{port}/test_climatepulse"}, check=True)`. Yield pool; close on teardown.
    - `@pytest.fixture async def seed_metadata(db_pool)` that inserts ONE sources row (`source_id='test_src', name='Test', license='MIT', attribution='Test'`), ONE variables row (`wmo_code='air_temperature', bufr_code='B12101', unit_si='K'`), ONE stations row (`source_id=<id>, external_id='stn001', name='Test Stn', lat=44.6, lon=11.0`), and returns a dict `{source_id, variable_id, station_id}` of surrogate keys.

    Create `tests/integration/test_hypertable.py` with 4 tests (all `@pytest.mark.asyncio`):
    - `test_observations_hypertable_exists`: query `SELECT * FROM timescaledb_information.hypertables WHERE hypertable_name='observations'`; assert exactly 1 row; assert its `num_dimensions == 1`.
    - `test_gridded_observations_hypertable_exists`: same for `gridded_observations`; assert 1 row exists.
    - `test_observations_chunk_interval_7d`: query `SELECT integer_interval FROM timescaledb_information.dimensions WHERE hypertable_name='observations'`; assert it equals 7 days in microseconds (`7 * 86400 * 1_000_000`).
    - `test_gridded_chunk_interval_1d`: same query for `gridded_observations`; assert it equals 1 day in microseconds.

    Create `tests/integration/test_idempotency.py` with this exact test (ING-12 contract):
    - `test_on_conflict_do_nothing_via_pk(db_pool, seed_metadata)`:
      - Prepare a row: `(station_id, variable_id, observed_at='2026-05-23T00:00:00+00:00', source_id, value=285.5, qc_flag=0)`
      - Execute `INSERT INTO observations (station_id, variable_id, observed_at, source_id, value, qc_flag) VALUES (...) ON CONFLICT (station_id, variable_id, observed_at, source_id) DO UPDATE SET value = EXCLUDED.value, qc_flag = EXCLUDED.qc_flag` twice with the SAME tuple
      - Assert `SELECT count(*) FROM observations WHERE station_id=$1 AND variable_id=$2 AND observed_at=$3 AND source_id=$4` returns exactly 1
      - Insert a THIRD time with value=999; assert count is still 1; assert `SELECT value FROM observations WHERE ...` returns 999 (UPDATE happened)

    Create `tests/integration/test_cagg.py`:
    - `test_obs_hourly_aggregates_after_refresh(db_pool, seed_metadata)`: insert 6 observations 10 minutes apart at the same hour with values 280, 281, 282, 283, 284, 285. Call `await conn.execute("CALL refresh_continuous_aggregate('obs_hourly', '2026-05-23 00:00+00', '2026-05-23 02:00+00')")`. Query `SELECT avg_value, sample_count FROM obs_hourly WHERE bucket = '2026-05-23 00:00+00'`; assert `sample_count == 6` and `avg_value` is approximately `(280+281+282+283+284+285)/6 = 282.5` (use `pytest.approx`).
    - `test_obs_daily_hierarchical_aggregates_from_hourly(db_pool, seed_metadata)`: per STO-03, insert 24 observations spanning 24 different hours of a single day with known values; refresh `obs_hourly` first, then refresh `obs_daily` for the same day; query `SELECT avg_value, sample_count FROM obs_daily WHERE day_bucket = '2026-05-23 00:00+00'`; assert `sample_count == 24` and `avg_value` matches expected daily mean. CRITICAL: confirm `obs_daily` bucket source is `obs_hourly.bucket` not raw `observations.observed_at` (inspect `timescaledb_information.continuous_aggregates.view_definition`).
    - `test_gridded_hourly_aggregates_after_refresh(db_pool, seed_metadata)`: insert 3 grid-point observations into `gridded_observations` at the same 6-hour bucket; refresh `gridded_hourly`; assert sample_count and avg_value.
    - `test_gridded_daily_hierarchical_aggregates_from_hourly(db_pool, seed_metadata)`: symmetric to obs_daily — insert 4 cycles × 1 grid point spanning a day; refresh `gridded_hourly` then `gridded_daily`; assert daily aggregate exists and bucket source is `gridded_hourly.bucket` not raw.

    Add `pytest.ini_options` markers in pyproject root if not already there: `markers = ["integration: requires Docker for testcontainers TimescaleDB"]`. Tag all tests in `tests/integration/` with `@pytest.mark.integration`. Configure CI to run them via the same `pytest` invocation; rely on Docker being present in GitHub Actions runners by default (ubuntu-22.04 has Docker preinstalled).
  </action>
  <verify>
    <automated>uv run pytest tests/integration -v -x --no-header 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    `uv run pytest tests/integration` brings up the testcontainer TimescaleDB, runs Alembic upgrade head, executes all 4 hypertable tests + idempotency test + 4 CAGG tests (obs_hourly, obs_daily hierarchical, gridded_hourly, gridded_daily hierarchical); all pass. The testcontainer image tag is pinned (NOT `latest-pg16`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker process → TimescaleDB | Worker writes observations via parameterized SQL; SQL injection risk if untrusted strings concatenated |
| Alembic migration → TimescaleDB | Migrations run with admin privileges; mistake in DDL is irreversible without backup |
| Test runner → testcontainer | Test runner trusts the testcontainer image — image tampering risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | observations INSERT path | mitigate | All inserts in Plan 03's Writer use asyncpg parameterized queries (`$1, $2, ...`) or `copy_records_to_table`; no string interpolation of external values. Migration in this plan uses only static DDL — no user input |
| T-02-02 | Tampering | Alembic migration | mitigate | Single forward-only migration; `downgrade()` drops everything cleanly. CI runs migration against testcontainer on every PR via `db_pool` fixture, catching breaking changes |
| T-02-03 | Denial of Service | gridded_observations volume | mitigate | `chunk_time_interval=1 day` for gridded (RESEARCH.md line 536); compression after 7d; retention 90d caps growth. PK includes lat_idx+lon_idx to avoid full-table scans (Pitfall #2) |
| T-02-04 | Information Disclosure | Connection string `DATABASE_URL` | mitigate | env.py reads from env var, never logged; raise loud if missing; `.env` is gitignored (Plan 01) |
| T-02-05 | Repudiation | observations.ingested_at default now() | mitigate | Every row records server-side ingestion timestamp via column default; cannot be set by the writer (Plan 03 must not override it) |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan beyond Plan 01's set; testcontainers 4.14.2 carries `[OK]` from RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
- `0001_init.py` upgrade succeeds against fresh TimescaleDB 2.17 testcontainer (Task 3)
- Both `observations` and `gridded_observations` appear in `timescaledb_information.hypertables` (Task 3)
- Idempotency contract proved: ON CONFLICT DO UPDATE behaves correctly on repeated PK (Task 3)
- CAGG `obs_hourly` returns correct aggregates after manual refresh (Task 3)
- Policy ordering verified per Pitfall #3: refresh_lag < compress_after < retention_after for BOTH hypertables (inspection in 0001_init.py)
- `include_name` in env.py excludes Timescale-managed indexes (Pitfall D, Task 2)
</verification>

<success_criteria>
- ROADMAP Phase 1 Success Criteria #1 (storage portion) satisfied: TimescaleDB hypertables exist and are correctly configured.
- STO-01, STO-02, STO-03, STO-04, STO-05, STO-06 fully shipped — the DDL truth is locked in code and verified by integration tests.
- D-23 (TWO hypertables) is encoded in `0001_init.py` and tested.
- Pitfall #1 (chunk_time_interval), Pitfall #2 (PK includes time), Pitfall #3 (policy ordering), Pitfall D (include_name exclusion) are all addressed.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-slice-public-docs/01-02-SUMMARY.md` when done. Capture: exact testcontainer image tag used (e.g. `timescale/timescaledb:2.17.2-pg16` or closest available), policy ordering matrix (refresh_lag, compress_after, retention_after for each hypertable), and any deviation from the planned DDL. List integration tests added.
</output>

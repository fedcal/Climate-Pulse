---
phase: 02-public-rest-api
plan: "06"
type: execute
wave: 5
depends_on: ["02-01", "02-03", "02-04", "02-05"]
files_modified:
  - apps/api/climatepulse_api/main.py
  - apps/api/climatepulse_api/observability.py
  - apps/api/climatepulse_api/routers/metrics.py
  - apps/api/climatepulse_api/routers/__init__.py
  - mkdocs.yml
  - docs/api/stations.md
  - docs/api/observations.md
  - docs/api/sources.md
  - docs/api/meta.md
  - docs/api/health.md
  - docs/api/spec.md
  - docs/api/how-to-query.md
  - docs/api/rate-limits.md
  - docs/api/authentication.md
  - docs/api/changelog.md
  - docs/privacy.md
  - docs/runbooks/log-retention.md
  - tests/unit/test_observability_init.py
  - tests/unit/test_metrics_endpoint.py
  - tests/integration/test_mkdocs_build.py
autonomous: false
requirements: [OPS-04, OPS-10, DOC-04]
tags: [observability, otel, structlog, prometheus, mkdocstrings, docs, privacy, runbook, acceptance]
must_haves:
  truths:
    - "structlog renders JSON in prod (LOG_FORMAT=json default) and console in dev (LOG_FORMAT=console); every log event carries trace_id + span_id when an OTel span is active"
    - "OpenTelemetry SDK initialized BEFORE structlog configuration (research-locked order); OTLP/HTTP exporter active only when OTEL_EXPORTER_OTLP_ENDPOINT is set (default no-op per D-API-20)"
    - "OTel auto-instrumentation wraps FastAPI + asyncpg + Redis (D-API-22) without double-instrumenting on test client reuse (Pitfall 6)"
    - "GET /metrics returns 200 with prometheus exposition format text and is NOT in OpenAPI schema (D-API-21 — public but undocumented in spec)"
    - "MkDocs builds without errors and emits five API reference pages (stations/observations/sources/meta/health), one spec.md with Redoc embed + openapi.json download button, four guide pages (how-to-query/rate-limits/authentication/changelog), one privacy notice page, and one log-retention runbook"
    - "mkdocstrings paths config includes apps/api so :::climatepulse_api.routers.X works (Pitfall 7)"
    - "Each docs page renders the federicocalo.dev footer (existing mkdocs.yml copyright field unchanged — Phase 1 D-05/06 contract preserved)"
    - "The full Phase 2 acceptance demo (start docker compose, hit all five router groups via curl, run a Parquet export, verify rate-limit headers) PASSES as a human-verified checkpoint"
  artifacts:
    - path: "apps/api/climatepulse_api/observability.py"
      provides: "configure_structlog + add_otel_context processor + init_otel + Prometheus instrumentation (OPS-04)"
      contains: "def configure_structlog"
    - path: "apps/api/climatepulse_api/routers/metrics.py"
      provides: "GET /metrics router (prometheus exposition format, include_in_schema=False)"
      contains: "metrics"
    - path: "docs/api/spec.md"
      provides: "Redoc embed (CDN) + openapi.json download button (D-API-30)"
      contains: "redoc"
    - path: "docs/privacy.md"
      provides: "GDPR privacy notice (OPS-10) — what is logged, retention period, how to request deletion"
      contains: "GDPR"
    - path: "docs/runbooks/log-retention.md"
      provides: "Log retention runbook (OPS-10) — operator procedures for IP-truncated log retention + rotation"
      contains: "retention"
  key_links:
    - from: "apps/api/climatepulse_api/main.py lifespan"
      to: "apps/api/climatepulse_api/observability.py"
      via: "init_otel(settings) -> FastAPIInstrumentor.instrument_app(app) -> configure_structlog(settings.log_format) — strict order"
      pattern: "init_otel|configure_structlog"
    - from: "mkdocs.yml plugins.mkdocstrings.handlers.python.paths"
      to: "apps/api"
      via: "extended paths list includes apps/api so :::climatepulse_api.routers.X resolves"
      pattern: "apps/api"
---

<objective>
Three concurrent deliverables that close Phase 2:

1. **OPS-04 (observability):** structlog JSON/console renderer + OTel SDK init (OTLP/HTTP exporter env-gated per D-API-20) + auto-instrumentation for FastAPI/SQLAlchemy/asyncpg/Redis (D-API-22) + Prometheus `/metrics` endpoint (D-API-21). The init order is critical (OTel BEFORE structlog per RESEARCH Pitfall and Pattern 6) and is the single most subtle thing in this plan.

2. **DOC-04 (API reference docs):** Five mkdocstrings router pages + OpenAPI spec embed page (Redoc CDN + openapi.json download) + four additional guide pages (how-to-query, rate-limits, authentication v1.1+ placeholder, changelog with RFC 8594 Sunset policy) per D-API-28..31. Extend `mkdocs.yml` nav (does NOT rewrite — Phase 1 nav is preserved).

3. **OPS-10 (privacy + runbook):** Privacy notice docs page explaining what data we log (IP-truncated GDPR-compliant per D-API-12), retention period (configurable, default 30 days), and how to request deletion. Operator runbook for log rotation, IP-truncation verification, and DPA-ready audit trail.

4. **Acceptance checkpoint (Phase 2 done-condition):** A `checkpoint:human-verify` task that gates Phase 2 completion behind a documented manual demo — start the stack, hit each router group via curl, verify rate-limit headers, fetch a Parquet, browse /docs and the published docs site.

Purpose: Phase 2 ships when an operator can: (a) trace any 5xx end-to-end via trace_id from logs to OTLP collector (when configured), (b) scrape /metrics with a Prometheus, (c) point a Phase 3 dashboard developer at fedcal.github.io/Climate-Pulse for self-service onboarding, (d) answer any GDPR DPA inquiry from the privacy page + runbook.

Output: Single observability module, one minimal metrics router, twelve docs pages (or extensions), mkdocs.yml nav patch, and the gated checkpoint.

This plan is NOT autonomous (it contains a human-verify checkpoint) — `autonomous: false` in frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-public-rest-api/02-CONTEXT.md
@.planning/phases/02-public-rest-api/02-RESEARCH.md
@.planning/phases/02-public-rest-api/02-01-SUMMARY.md
@.planning/phases/02-public-rest-api/02-03-SUMMARY.md
@.planning/phases/02-public-rest-api/02-04-SUMMARY.md
@.planning/phases/02-public-rest-api/02-05-SUMMARY.md
@apps/api/climatepulse_api/main.py
@apps/api/climatepulse_api/settings.py
@apps/api/climatepulse_api/routers/health.py
@apps/api/climatepulse_api/routers/stations.py
@apps/api/climatepulse_api/routers/observations.py
@apps/api/climatepulse_api/routers/sources.py
@apps/api/climatepulse_api/routers/meta.py
@apps/api/climatepulse_api/middleware/gdpr_ip.py
@mkdocs.yml

<interfaces>
From plan 02-01:
- `ApiSettings` fields: `log_format: Literal["json","console"] = "json"`, `otel_exporter_otlp_endpoint: str | None = None`.
- Phase 2 dependencies already installed in pyproject (plan 02-01 Task 1): `structlog>=25.5`, `prometheus-client>=0.25`, `opentelemetry-distro>=0.62b0`, `opentelemetry-instrumentation-fastapi/sqlalchemy/asyncpg/redis>=0.62b0`, `opentelemetry-exporter-otlp-proto-http>=1.28`, `mkdocstrings[python]>=1.0.4`.

From plan 02-03, 02-04, 02-05:
- All routers exist and are decorated with `@limiter.limit(...)`; all path operations carry response_model + Field examples per D-API-27.
- main.py middleware stack and lifespan are stable; this plan ONLY adds the OTel init + structlog config (early in lifespan, BEFORE any router-served request) and the metrics router include.

From Phase 1:
- `mkdocs.yml` currently has `nav: [Home: index.md, Quickstart: quickstart.md, Scraping Policy: scraping-policy.md]` and `plugins.mkdocstrings.handlers.python.paths: [packages/core]`. This plan EXTENDS both — appends to nav, appends `apps/api` to paths.
- `copyright: "Climate Pulse · MIT License · <a href='https://federicocalo.dev'>federicocalo.dev</a>"` (D-05/06) is unchanged.
- Site URL: `https://fedcal.github.io/Climate-Pulse`.

OTel init order (research-locked, Pitfall 6 + Pattern 6):
1. `from opentelemetry import trace; from opentelemetry.sdk.trace import TracerProvider; from opentelemetry.sdk.trace.export import BatchSpanProcessor; from opentelemetry.exporter.otlp.proto.http.trace_exporter import OTLPSpanExporter`
2. If `settings.otel_exporter_otlp_endpoint`: build provider with OTLP exporter + BatchSpanProcessor; `trace.set_tracer_provider(provider)`.
3. `FastAPIInstrumentor().instrument_app(app)` — exactly ONCE; subsequent test-client recreations REUSE the same instrumented app (Pitfall 6).
4. `AsyncPGInstrumentor().instrument()`, `RedisInstrumentor().instrument()` — also exactly once.
5. NOW `configure_structlog(settings.log_format)` — the `add_otel_context` processor calls `trace.get_current_span()` which returns no-op IF tracer provider was not set; with the env-default no-op exporter, the processor harmlessly emits zero-trace-id (acceptable).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: observability.py module (structlog config + add_otel_context processor + init_otel + Prometheus instrumentor) + /metrics router + main.py wiring</name>
  <files>apps/api/climatepulse_api/observability.py, apps/api/climatepulse_api/routers/metrics.py, apps/api/climatepulse_api/main.py, apps/api/climatepulse_api/routers/__init__.py, tests/unit/test_observability_init.py, tests/unit/test_metrics_endpoint.py</files>
  <read_first>
    - .planning/phases/02-public-rest-api/02-RESEARCH.md (Pattern 6 structlog + OTel + init order + Pitfall 6 double-instrumentation)
    - apps/api/climatepulse_api/main.py (current lifespan)
    - apps/api/climatepulse_api/settings.py (log_format + otel_exporter_otlp_endpoint fields)
  </read_first>
  <behavior>
    - Test 1 (`test_structlog_renders_json_when_log_format_json`): `configure_structlog("json")` then `structlog.get_logger("x").info("hello", foo=1)` — the captured output is a single line of valid JSON containing `{"event":"hello","foo":1,"log_level":"info","timestamp":"..."}`.
    - Test 2 (`test_structlog_renders_console_when_log_format_console`): `configure_structlog("console")` — output is human-readable (contains `hello` without JSON braces).
    - Test 3 (`test_add_otel_context_no_active_span`): With no active OTel span, the processor returns the event_dict UNCHANGED (no trace_id key added). No exception.
    - Test 4 (`test_add_otel_context_with_active_span`): Within `tracer.start_as_current_span("test")`, the processor adds `trace_id` (32 hex chars) and `span_id` (16 hex chars) to the event_dict.
    - Test 5 (`test_init_otel_noop_when_endpoint_unset`): `init_otel(settings_with_no_endpoint)` does NOT call `set_tracer_provider` (proven via patch); FastAPIInstrumentor IS still called (auto-instrumentation is local — spans are produced but go to a no-op processor).
    - Test 6 (`test_init_otel_otlp_when_endpoint_set`): `init_otel(settings_with_endpoint="http://collector:4318/v1/traces")` builds an OTLPSpanExporter with that endpoint and registers a BatchSpanProcessor on a new TracerProvider.
    - Test 7 (`test_init_otel_idempotent`): Calling `init_otel(...)` twice does NOT double-instrument FastAPI (Pitfall 6) — use a module-level `_INSTRUMENTED = False` guard.
    - Test 8 (`test_metrics_endpoint_returns_prometheus_format`): GET /metrics returns 200 with `Content-Type` starting with `text/plain; version=0.0.4` (Prometheus exposition format default).
    - Test 9 (`test_metrics_endpoint_not_in_openapi`): The /metrics path does NOT appear in `app.openapi()["paths"]` (D-API-21 — public endpoint, but not in spec). Verifies `include_in_schema=False`.
    - Test 10 (`test_log_event_carries_trace_id_in_request`): Spin up TestClient(app), make GET /healthz, capture structlog events emitted during that request — the event_dict carries `trace_id` AND `span_id` (proves init order is correct: FastAPIInstrumentor created the span BEFORE structlog logged; add_otel_context picked it up).
  </behavior>
  <action>
    Create `apps/api/climatepulse_api/observability.py`:
    - Imports: `from opentelemetry import trace`; SDK + exporter + instrumentation libraries per RESEARCH Pattern 6.
    - Module guard: `_INSTRUMENTED: bool = False`.
    - `def add_otel_context(logger, method, event_dict)`: per RESEARCH Pattern 6 — `span = trace.get_current_span(); if span.get_span_context().is_valid: event_dict["trace_id"] = format(ctx.trace_id, "032x"); event_dict["span_id"] = format(ctx.span_id, "016x")`. Use `.is_valid` rather than `.is_recording` (more robust for non-recording but valid contexts).
    - `def configure_structlog(log_format: str) -> None` per RESEARCH Pattern 6 — processors: `merge_contextvars`, `add_log_level`, `TimeStamper(fmt="iso")`, `add_otel_context`, then either `JSONRenderer()` (json) or `ConsoleRenderer()` (console). Wire stdlib `logging` to structlog via `structlog.stdlib.LoggerFactory()` so existing `logging.getLogger(...)` calls (FastAPI, asyncpg) also route through structlog.
    - `def init_otel(app, settings) -> None`:
      - `global _INSTRUMENTED`; if `_INSTRUMENTED: return` (Pitfall 6 idempotency).
      - If `settings.otel_exporter_otlp_endpoint`: build `TracerProvider`, attach `BatchSpanProcessor(OTLPSpanExporter(endpoint=settings.otel_exporter_otlp_endpoint))`, `trace.set_tracer_provider(provider)`.
      - `FastAPIInstrumentor().instrument_app(app)`.
      - Try-import each of `AsyncPGInstrumentor`, `RedisInstrumentor`, `SQLAlchemyInstrumentor` (defensive — version mismatches per RESEARCH Open Question #3); for each that imports cleanly, call `.instrument()`. Failures emit a single structlog warning per instrumentor and continue.
      - Set `_INSTRUMENTED = True`.

    Create `apps/api/climatepulse_api/routers/metrics.py`:
    - `from prometheus_client import generate_latest, CONTENT_TYPE_LATEST, REGISTRY`
    - `router = APIRouter(tags=["Metrics"])`
    - `@router.get("/metrics", include_in_schema=False)` returning `Response(generate_latest(REGISTRY), media_type=CONTENT_TYPE_LATEST)`. D-API-21 — public path BUT NOT in OpenAPI schema.
    - Optional: register a `prometheus_client.Counter("http_requests_total", ["method","route","status"])` and a `prometheus_client.Histogram("http_request_duration_seconds", ["route"])` at module-level so future plans can `.inc()` / `.observe()` — Phase 2 keeps these zero-valued (FastAPIInstrumentor produces traces; explicit metrics for QPS/latency are Phase 5 LGTM stack scope per CONTEXT). Document this as "metrics scaffolding for Phase 5; OTel traces are the v1.0 observability primary".

    Update `apps/api/climatepulse_api/main.py`:
    - In the lifespan, AT THE TOP (before asyncpg pool / Redis init / cursor secret / sources cache / limiter), insert:
      - `settings = get_api_settings()` (already exists; reorder if needed)
      - `init_otel(app, settings)`
      - `configure_structlog(settings.log_format)`
    - Order is CRITICAL: any subsequent structlog log event (from asyncpg pool creation, Redis init, cursor secret seed, etc.) will then carry trace_id when FastAPIInstrumentor produces a request span; before any request comes in, the bind is a no-op.
    - Include the metrics router AFTER the four `/v1/*` routers: `from climatepulse_api.routers import metrics; app.include_router(metrics.router)`. Update `routers/__init__.py` to re-export `metrics`.

    Write `tests/unit/test_observability_init.py` and `tests/unit/test_metrics_endpoint.py`. For Test 10, use `structlog.testing.capture_logs()` around a TestClient request and inspect captured event_dict entries.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Climate Pulse" &amp;&amp; uv run pytest tests/unit/test_observability_init.py tests/unit/test_metrics_endpoint.py -x -q &amp;&amp; uv run python -c "from climatepulse_api.main import app; spec=app.openapi(); assert '/metrics' not in spec.get('paths',{}); print('METRICS OUT OF SPEC OK')" &amp;&amp; uv run python -c "from climatepulse_api.observability import _INSTRUMENTED; print('GUARD:', type(_INSTRUMENTED).__name__)"</automated>
  </verify>
  <done>All 10 tests pass; /metrics serves Prometheus exposition; /metrics is NOT in OpenAPI; init order is OTel -> structlog; double-instrumentation guarded.</done>
</task>

<task type="auto">
  <name>Task 2: API reference docs (mkdocstrings 5 router pages + spec.md Redoc + 4 guide pages) + privacy + runbook + mkdocs.yml nav extension</name>
  <files>mkdocs.yml, docs/api/stations.md, docs/api/observations.md, docs/api/sources.md, docs/api/meta.md, docs/api/health.md, docs/api/spec.md, docs/api/how-to-query.md, docs/api/rate-limits.md, docs/api/authentication.md, docs/api/changelog.md, docs/privacy.md, docs/runbooks/log-retention.md, tests/integration/test_mkdocs_build.py</files>
  <read_first>
    - mkdocs.yml (current nav + plugins.mkdocstrings.handlers.python.paths)
    - .planning/phases/02-public-rest-api/02-RESEARCH.md (Pattern 9 mkdocstrings + Pitfall 7 paths config)
    - apps/api/climatepulse_api/routers/* (the router source files this plan documents — pages reference module-level docstrings)
    - .planning/phases/02-public-rest-api/02-CONTEXT.md (D-API-28..31)
  </read_first>
  <behavior>
    - Test 1 (`test_mkdocs_build_clean`): Run `uv run mkdocs build --strict` in a temp directory; exit code 0; no warnings of class WARNING or ERROR (strict mode). Build output directory contains `api/stations/index.html`, `api/observations/index.html`, etc.
    - Test 2 (`test_mkdocstrings_finds_apps_api`): The built `api/stations/index.html` contains rendered docstrings from `climatepulse_api.routers.stations` (e.g. the module docstring or the `list_stations` function name). This verifies the `paths: [apps/api]` addition (Pitfall 7).
    - Test 3 (`test_redoc_embed_present`): `site/api/spec/index.html` contains a `<redoc spec-url="...openapi.json...">` element OR a `<script src=".*redoc.*"></script>` from the Redoc CDN.
    - Test 4 (`test_nav_extended_not_replaced`): The built site nav still includes the Phase 1 entries (Home, Quickstart, Scraping Policy) AND the new API Reference + API Guide sections (Pattern 9). Verify via grep on `sitemap.xml` or by parsing the generated nav.
    - Test 5 (`test_footer_present_on_new_pages`): The built `api/stations/index.html` contains the literal string `federicocalo.dev` (Phase 1 D-05/06 copyright contract preserved on every page).
  </behavior>
  <action>
    Extend `mkdocs.yml` (do NOT rewrite — Phase 1 contracts):

    1. Under `plugins.mkdocstrings.handlers.python.paths`, change `[packages/core]` to `[packages/core, apps/api]` (Pitfall 7). The list-form syntax already exists; just append.

    2. Under `nav`, AFTER the existing three entries, ADD per D-API-28/31:
       ```
       - API Reference:
         - Stations: api/stations.md
         - Observations: api/observations.md
         - Sources: api/sources.md
         - Meta: api/meta.md
         - Health: api/health.md
         - OpenAPI Spec: api/spec.md
       - API Guide:
         - How to Query: api/how-to-query.md
         - Rate Limits: api/rate-limits.md
         - Authentication: api/authentication.md
         - Changelog: api/changelog.md
       - Privacy: privacy.md
       - Runbooks:
         - Log Retention: runbooks/log-retention.md
       ```

    Create `docs/api/stations.md`:
    - H1: "Stations".
    - Per RESEARCH Pattern 9, use `:::climatepulse_api.routers.stations` with `options: show_root_heading: true, show_source: false, members: [list_stations, get_station]`.
    - Below the directive, add a "Try it" section with the SAME curl + httpx samples used in `x-codeSamples` from plan 02-05 (do NOT duplicate copy — reference the OpenAPI spec page for the canonical samples).
    - Cross-link to `../api/spec.md` for the interactive Redoc.

    Repeat the pattern for `docs/api/observations.md`, `docs/api/sources.md`, `docs/api/meta.md`, `docs/api/health.md` (health.md documents the Phase 1 module — `:::climatepulse_api.routers.health` with members `[liveness, readiness]`).

    Create `docs/api/spec.md` (D-API-30):
    - H1: "OpenAPI Spec".
    - Paragraph: "Climate Pulse exposes an OpenAPI 3.1 spec at `/openapi.json` (also browsable at `/docs` Swagger UI and `/redoc` Redoc UI)."
    - Redoc embed via CDN:
      ```html
      <redoc spec-url="https://fedcal.github.io/Climate-Pulse/openapi.json"></redoc>
      <script src="https://cdn.jsdelivr.net/npm/redoc@2/bundles/redoc.standalone.js"></script>
      ```
    - Download button: `[Download openapi.json](/Climate-Pulse/openapi.json){.md-button}`.
    - NOTE: For the docs site to actually serve `openapi.json`, the GitHub Action that builds + publishes docs (Phase 1 plan 01-01) needs to copy the spec from a running API into the docs site. For Phase 2 v1.0 the spec link initially 404s on the docs site (we don't have a deployed API yet — that's Phase 4 reverse proxy work). Document this limitation IN the spec.md page as a "Coming in Phase 4" note: "Until the production deployment lands in Phase 4, the spec is reachable only via a local `uvicorn` run (see Quickstart)."

    Create `docs/api/how-to-query.md` (D-API-31):
    - Practical tutorial: "Fetch 7 days of air_temperature for a station". Walk through `curl /v1/stations?country=IT` -> pick a station_id -> `curl /v1/observations?station_id=X&variable=air_temperature&start=...&end=...` -> show response shape -> show how cursor pagination works -> show how to switch to `?format=parquet`.
    - Include a short "Time zones" section explaining UTC + half-open intervals (D-API-04).
    - Include a "Resolution auto-routing" section explaining D-API-01 + D-API-02 transparently.

    Create `docs/api/rate-limits.md` (D-API-31):
    - Table of all routes -> budget per D-API-11.
    - 429 response shape with example RFC 7807 body + headers.
    - Python httpx backoff snippet using `Retry-After` header.

    Create `docs/api/authentication.md` (D-API-31):
    - "Climate Pulse v1.0 has no authentication." (D-API-10).
    - Paragraph explaining per-IP rate-limit philosophy + open-data principle.
    - "v1.1+ (EXT-07) will add an optional API key tier for higher rate-limit budgets. Existing per-IP behavior remains the free tier."

    Create `docs/api/changelog.md` (D-API-31):
    - Section "Versioning policy": "MAJOR version in URL path (`/v1/`, future `/v2/`); SemVer for non-breaking additions within a major; deprecation surfaced via RFC 8594 `Sunset:` HTTP header at least 6 months ahead of removal."
    - Section "v1.0.0 (2026-Q3, Phase 2 ship)": initial public release. Endpoints: list (mirrors OpenAPI tags).
    - Section "Reserved for /v2/": will be opened when a breaking change is unavoidable.

    Create `docs/privacy.md` (OPS-10):
    - GDPR notice: "What we log: IP addresses truncated to /24 (IPv4) or /48 (IPv6) per D-API-12; request path; response status; latency; trace_id. We do NOT log query parameter values, cursor tokens, or response bodies."
    - Lawful basis: "Legitimate interest (Art. 6(1)(f) GDPR) for security monitoring and abuse prevention."
    - Retention: "Logs retained 30 days by default (configurable via operator runbook); aggregated metrics retained indefinitely."
    - Subject rights: "Truncated IPs are considered personal data by CNIL/Garante (RESEARCH cited). To request log deletion containing your IP prefix, contact [contact email]."
    - Cookies: "Climate Pulse API issues no cookies. Docs site uses GitHub Pages default."

    Create `docs/runbooks/log-retention.md` (OPS-10):
    - Operator procedure to verify IP truncation in production: `docker compose logs api | jq 'select(.client_ip)' | head` and confirm last octet is always `.0`.
    - Log rotation policy: docker-compose default JSON file driver with `max-size=10m, max-file=30` (30 day rolling at 10MB chunks).
    - Procedure to purge logs containing a specific /24 prefix (rare GDPR request).
    - Cross-link to `docs/api/changelog.md` for the auth-tier evolution path.

    Write `tests/integration/test_mkdocs_build.py` with the 5 tests. Use `subprocess.run(["uv","run","mkdocs","build","--strict","--site-dir",tmp_path])` and inspect `tmp_path / "api/stations/index.html"` etc. Mark `@pytest.mark.integration`.
  </action>
  <verify>
    <automated>cd "/media/federicocalo/D1/prj/Climate Pulse" &amp;&amp; uv run mkdocs build --strict --site-dir /tmp/cp-docs-build &amp;&amp; ls /tmp/cp-docs-build/api/stations/index.html /tmp/cp-docs-build/api/spec/index.html /tmp/cp-docs-build/privacy/index.html /tmp/cp-docs-build/runbooks/log-retention/index.html &amp;&amp; grep -q "federicocalo.dev" /tmp/cp-docs-build/api/stations/index.html &amp;&amp; uv run pytest tests/integration/test_mkdocs_build.py -x -q -m integration</automated>
  </verify>
  <done>mkdocs build --strict exits 0; all 12 new docs pages render; mkdocstrings resolves apps/api router modules; footer present on every new page; integration test passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 2 acceptance demo (human-verify checkpoint)</name>
  <what-built>
    Phase 2 is feature-complete. All five router groups serve real data, rate limits are enforced, exports stream under 200MB RSS, OTel traces and Prometheus metrics flow, the docs site builds with 12 new pages.
  </what-built>
  <how-to-verify>
    Run this acceptance script from the repo root. Each step should succeed before moving to the next.

    1. **Start the stack from a clean state:**
       ```
       docker compose down -v
       docker compose up -d
       # Wait ~30s for healthchecks to settle.
       curl -fsS http://localhost:8000/healthz   # expect {"status":"ok"}
       curl -fsS http://localhost:8000/readyz    # expect {"status":"ready"}
       ```

    2. **Seed at least one backfill run so /v1/observations has data:**
       ```
       docker compose exec worker climatepulse backfill arpae --from $(date -d '7 days ago' +%F) --to $(date +%F)
       ```

    3. **Hit each router group:**
       ```
       curl -i 'http://localhost:8000/v1/stations?country=IT&limit=5'
       curl -i 'http://localhost:8000/v1/stations/1'
       curl -i 'http://localhost:8000/v1/meta/variables'
       curl -i 'http://localhost:8000/v1/sources'
       curl -i 'http://localhost:8000/v1/sources/status'   # always 200
       curl -i 'http://localhost:8000/v1/observations?station_id=1&variable=air_temperature&start=2026-01-01T00:00:00Z&end=2026-01-02T00:00:00Z'
       ```
       Verify each response carries headers `API-Version: 1.0` and `X-Request-ID: <uuid>`.

    4. **Verify rate-limit headers + 429 behavior:**
       ```
       for i in $(seq 1 65); do curl -s -o /dev/null -w "%{http_code} " http://localhost:8000/v1/stations; done; echo
       # Expect: 60x "200 ", then "429 429 429 429 429".
       curl -sS -i http://localhost:8000/v1/stations | grep -i 'x-ratelimit'
       # Expect: X-RateLimit-Limit: 60, X-RateLimit-Remaining: <int>, X-RateLimit-Reset: <unix>
       ```

    5. **Stream a Parquet export and verify file is valid:**
       ```
       curl -sS 'http://localhost:8000/v1/observations?station_id=1&variable=air_temperature&start=2026-01-01T00:00:00Z&end=2026-01-08T00:00:00Z&format=parquet' -o /tmp/cp.parquet
       uv run python -c "import pyarrow.parquet as pq; t=pq.read_table('/tmp/cp.parquet'); print(t.schema); print(t.num_rows)"
       # Expect: 10-column long-format schema per D-API-15; non-zero row count.
       ```

    6. **Browse interactive docs:**
       - http://localhost:8000/docs — Swagger UI renders, all endpoints listed, x-codeSamples visible as tabs.
       - http://localhost:8000/redoc — Redoc UI renders without errors.
       - http://localhost:8000/openapi.json | jq '.info' — version 1.0.0, contact and license blocks present.

    7. **Verify /metrics is public and serves Prometheus format:**
       ```
       curl -sS http://localhost:8000/metrics | head -20
       # Expect: lines like "# HELP ..." and "# TYPE ..." in Prometheus exposition format.
       curl -sS http://localhost:8000/openapi.json | jq '.paths."/metrics"'
       # Expect: null (D-API-21 — public but not in spec).
       ```

    8. **Verify structlog JSON output carries trace_id:**
       ```
       docker compose logs api --tail 50 | grep -E '"trace_id"' | head
       # Expect: JSON log lines with non-zero trace_id field on at least the recent /v1/* requests.
       ```

    9. **Verify GDPR IP truncation in logs:**
       ```
       docker compose logs api --tail 100 | grep -E '"client_ip' | head
       # Expect: every client IP ends in .0 (IPv4 /24) or :: (IPv6 /48 — likely none from local testing).
       ```

    10. **Verify docs site builds and footer is present:**
        ```
        uv run mkdocs serve  # in another terminal; open http://localhost:8000 in browser
        ```
        Visit `/api/stations`, `/api/observations`, `/api/spec`, `/privacy`, `/runbooks/log-retention`. Confirm each page shows the footer `Climate Pulse · MIT License · federicocalo.dev`.

    11. **(Optional, if OTel collector available)** Set `OTEL_EXPORTER_OTLP_ENDPOINT=http://<collector>:4318/v1/traces` in `.env`, restart compose, hit endpoints, verify spans arrive at the collector with `service.name=climatepulse-api`.

    PASS criteria: All 10 mandatory steps succeed without errors; rate-limit headers + 429 demonstrated; Parquet readable; docs render with footer; trace_id in JSON logs.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps succeed; otherwise list the failing step(s) for follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet -> /metrics endpoint | Prometheus metrics public per D-API-21; risk of internal-info leak (process_cpu_seconds_total etc.) |
| OTLP exporter -> external collector | Trace data crosses network if endpoint configured; could leak request paths/headers |
| Docs site -> public consumers | Privacy notice + runbook contents are public; should not contain operator-internal URLs or credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Information Disclosure | /metrics exposes process internals (uptime, GC stats, request counts) | accept | D-API-21 explicit decision: open-data philosophy; production deployment (Phase 4 Caddy) MAY filter via reverse proxy if operator chooses; documented in `docs/api/rate-limits.md` as a known surface |
| T-02-06-02 | Information Disclosure | OTLP traces may carry request paths revealing internal endpoints if a future router accidentally registers a non-public path | mitigate | Phase 2 ships only the documented public /v1/* + /healthz + /readyz + /metrics + /docs + /redoc + /openapi.json; no internal admin endpoints exist; future plans MUST gate any internal endpoint behind a hostname check |
| T-02-06-03 | Tampering | mkdocs build pulls Redoc from CDN; CDN compromise could inject JS | accept | Redoc CDN (cdn.jsdelivr.net) is widely trusted; SRI hashes optional (would need version-pinning runbook); v1.0 ships without SRI, Phase 5 ops hardening MAY add SRI |
| T-02-06-04 | Information Disclosure | Privacy notice contact email = personal email of operator | accept | Operator decision (D-API-24 already exposes `fedcal01@gmail.com` in OpenAPI contact block); consistent surfacing |
| T-02-06-05 | Denial of Service | OTel auto-instrumentation adds ~5% latency overhead (D-API-22) | accept | Decision locked; observability benefit outweighs cost; Phase 5 may add sampling if needed |
| T-02-06-06 | Information Disclosure | structlog JSON renderer dumps any `bind()`-ed value; risk of accidentally binding a secret | mitigate | OPS-10 runbook documents allowlist of safe binds (request_id, client_ip_truncated, path, status, latency_ms, source_id); any new structlog.bind() call in subsequent phases must check against this list |
| T-02-06-07 | Tampering | OTel double-instrumentation on test client reuse | mitigate | Pitfall 6 mitigation: `_INSTRUMENTED` module guard + Test 7 verifies idempotency |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_observability_init.py tests/unit/test_metrics_endpoint.py -x -q` returns 0 failures.
- `uv run pytest tests/integration/test_mkdocs_build.py -x -q -m integration` returns 0 failures.
- `uv run mkdocs build --strict` exits 0 (no WARNING or ERROR).
- `curl -sS http://localhost:8000/metrics | head -5` shows Prometheus format `# HELP ...` lines.
- `curl -sS http://localhost:8000/openapi.json | jq '.paths."/metrics"'` returns `null` (D-API-21).
- Human checkpoint Task 3 returns "approved".
</verification>

<success_criteria>
- structlog JSON in prod / console in dev; every log event during a request carries trace_id + span_id.
- OTel SDK init BEFORE structlog config; FastAPIInstrumentor not double-applied.
- /metrics endpoint serves Prometheus exposition format, public, NOT in OpenAPI schema.
- MkDocs builds with --strict; 12 new docs pages render with footer; mkdocstrings resolves apps/api modules.
- Privacy notice + log-retention runbook published.
- All previous Phase 2 plans' tests still pass (regression guard).
- Human acceptance demo passes all 10 mandatory steps.
</success_criteria>

<output>
Create `.planning/phases/02-public-rest-api/02-06-SUMMARY.md` recording: final OTel package versions resolved (vs ASSUMED 0.62b0 in RESEARCH), which optional instrumentors (SQLAlchemy/AsyncPG/Redis) loaded cleanly vs warned, mkdocs build duration, the docs site URL the acceptance demo verified, and any /metrics exposure-policy notes for Phase 4 reverse-proxy filtering.

Also create `.planning/phases/02-public-rest-api/02-VERIFICATION.md` summarizing the human-checkpoint outcome (status: passed | failed | partial; failing-step list; gap closure recommendations).
</output>
